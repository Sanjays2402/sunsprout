# Sunsprout autonomous cron — STATE

> You are Cake. Read me FIRST every tick.

## STATUS

**STATUS:** Tick #39 complete. 170 autoship features now on `main`. SEVENTH consecutive FRONTEND-FOCUS tick (Sanjay's standing override). Clean batch (not a resume): origin/main + local main both at #38 (30384c6), shipped 5 fresh slices 30384c6..a4c5038, gated once, pushed. Theme = "one consistent shell — your stuff, your shortcuts, no dead ends": built the missing inventory surface + unified the panel-family navigation/empty-state vocabulary + made the planner personal. 5 slices: panel-tab-strip (cb6aed4 — new pure game/panel-tabs.ts tabStripLayout/cycleTabIndex/tabAtPoint + thin ui/panel-tab-strip.ts widget; retrofit the lore panel's 6 tabs through it pixel-for-pixel, seeding the bag panel's tabs for free), bag-panel (56713d3 — new pure game/bag.ts classifies the flat inventory Record into 7 glanceable tabs Seeds/Crops/Fish/Gems/Forage/Kitchen/Supplies with human labels + per-unit gold value resolved from the catalogs; ui/bag-panel.ts renders via the shared tab strip; wired on Tab as a world-blocking modal that suppresses the gameplay-verb block so w/s scroll without watering, Tab preventDefault'd, documented in controls), help-filter (d5961ee — controls.ts bindingMatchesFilter/groupMatchesFilter/matchingGroupIndices/matchingBindingCount; the ? overlay becomes an input-capturing modal that takes typed letters as a live filter, dims non-matching groups+rows without shifting layout, green "filter: water_ (1/41)" chip, Backspace/Esc handling), panel-empty (622673b — new pure game/panel-empty.ts PANEL_EMPTY_STATES registry + loreEmptyState() resolver, ui/empty-state.ts thin two-line widget; fixes the REAL bug where a filtered-empty lore Rumors tab drew NOTHING; retrofit money-log + quest-log off hard-coded one-liners), almanac-personal (a4c5038 — buildAlmanac folds the player's own heart-4 hangout invites in as a new 'personal' kind sorted first within a day-tie; rose rail + M tag in the panel + HUD chip promotion; all four entry points took an OPTIONAL trailing player arg so every existing caller/test is untouched, reuses the proven 28-day wrap). NO fragile absolute-day math (the time model wraps day 1-7 per season with no absolute counter, so the compost/wedding-countdown variant of the almanac-self-milestones idea was deliberately SKIPPED in favour of the reliable (season,day) hangout dates).

**IMPORTANT WORKFLOW CHANGE:** As of tick #8, the prompt commits DIRECTLY to `main` and pushes to `origin/main`, NOT to `feature/autoship`. The quality gate (`npx tsc --noEmit && npm run build && npm test`) at end of batch is what protects main — never push red code.

Active branch: `main`
Default branch: `main` (push here every tick — contribution graph)
Cron identity: `Cake (cron) <51058514+Sanjays2402@users.noreply.github.com>`

## OPERATING RULES (do not violate)

1. Work on `main`. Commit directly to main and push to origin/main every tick.
2. No PRs (`gh pr create` BANNED). No tags. No `gh release`.
3. No emoji in any git artifact (commit messages, branch names, file contents touched by cron). Telegram delivery may use emoji.
4. One feature slice = one commit. ~5 commits per tick.
5. Gate ONCE per batch at the end (`npx tsc --noEmit`, `npm run build`, `npm test`). Push only if green.
6. Quality is the floor. If only 3 features are genuinely solid, ship 3 and say so. Never pad.

## ROADMAP (cozy-life essentials + depth)

Each row is a real user-facing capability — logic module + tests + UI/wiring. Pick the next 5 unstarted rows per tick.

- [x] **Save/Load to localStorage** — JSON snapshot of player + world + hearts + time; auto-save on day rollover; manual save with `K`; load on boot if present. (ad5fad6)
- [x] **Sleep + Day Summary overlay** — press `B` near the farmhouse to advance to dawn; modal overlay shows yesterday's gold gain, harvest count, hearts gained. (965f0fc)
- [x] **Weather system (sunny / rain / storm)** — per-day deterministic weather; rain auto-waters every crop at day rollover; rain particles in render; forecast strip in HUD. (6213168)
- [x] **NPC Birthdays + birthday banner** — each NPC has a (season, day) birthday; 8× gift multiplier on the day; banner in HUD when it's somebody's birthday today. (75ba0ec)
- [x] **Sprinkler placeable** — buyable item; place on a tilled tile; auto-waters orthogonal neighbours at day rollover; visible procedural sprite. (1c03c70)
- [x] **Forage spawns** — wild berries / mushrooms / herbs appear on grass each morning, vanish at dusk; pickable for cheap gold + cooking ingredients; deterministic per (season, day). (329d38e)
- [x] **Animal coop (chickens)** — purchase a coop kit (600g) and chickens (200g, cap 4); coop placed on grass with N; daily egg into the coop; collect with E, fill with I. (59d5ed7)
- [x] **Egg + forage recipes** — five new dishes (Farm Omelet, Pumpkin Custard, Mushroom Skillet, Berry Tart, Sage Tea) chained off the coop and forage loops. (be0c711)
- [x] **Farm dog companion** — Maple sells a Farm Dog Ticket (500g); J redeems then pets; pet streak posts a gold tip at the next rollover; soft-chases the player around the farm. (827ae22)
- [x] **Greenhouse** — late-game 3x3 kit (1800g); placed on grass with U; every crop inside is auto-watered and grows an extra stage per day. (f41d16f)
- [x] **Crop quality tiers** — silver / gold star crops at high water-streak; 1.5x / 2x sell price; per-tier inventory buckets + tiered well sale; harvest toast announces the tier. (dc6da66)
- [x] **Tool upgrades (copper / iron / gold)** — Maple sells upgrades at the shop (150/450/1200g); copper widens till/water to a 3-tile line, iron to 5, gold adds a forward bonus tile; keybinds `,` and `.`. (b100dd2)
- [x] **Festival days** — Spring (s0d7) Planting Fair (half-price seeds) and Fall (s2d7) Harvest Festival (1.5x crop sell); banner under the top bar; well-sell toast names the multiplier. (62eff02)
- [x] **Mail / letters** — NPCs deliver a per-tier letter at heart milestones (2/4/6/8); `[` at the farmhouse reads the next unread letter; mailbox persists. (82d4375)
- [x] **Cellar storage chest** — starter chest at the farmhouse + buyable Chest Kit (300g, X to place); `]` opens deposit/withdraw menu with a tab shortcut for harvest dump. (50443d9)
- [x] **Recipe codex panel** — `R` opens a panel listing every recipe; locked/known/cooked tiers; ingredients + cook count + last-day; cooking marks recipes known + cooked into a persistent history. (5a09261)
- [x] **Crop journal panel** — `;` toggles a per-crop journal with lifetime tally, best season, growth-day forecast, current-tier tally + planted/watered/harvested totals. (78ea1af)
- [x] **Achievements panel** — `V` opens a 15-badge milestone tracker with scroll + persistence; checked every frame so milestones surface immediately. (6707f0f)
- [x] **Money log** — `Q` opens a 20-row gold ledger with reasons + per-row signed deltas + a footer total; persists; every gold change posts an entry. (b712a59)
- [x] **Settings panel** — `\\` opens a panel for autoSave toggle, night-tint cycling (0/30/60/100%), HUD scale (1.0/1.25/1.5x), reduce-motion toggle, and a two-step reset-save. (ba82aa4)
- [x] **Winter quiet-season pass** — outdoor crops refuse to grow in Winter (greenhouse essential); snow particles overlay; Hot Cocoa joins the cookbook. (7589e6d)
- [x] **Pickaxe upgrades + drop bias** — Maple sells copper/iron/gold/diamond upgrades (200/600/1500/3500g, `/` keybind); per-tier gem-weight bias shifts rolls toward rare gems as you climb. (18f6028)
- [x] **Quest log panel** — `'` toggles a centred panel listing active + completed quests with hints, progress bars, and pre-formatted reward lines. (7d376af)
- [x] **NPC scheduled events** — heart-4 candidates post evening hangout invites with a (season, day) date + 18-20h window; walking onto the meeting spot fires +120g + heart points. (9b0e148)
- [x] **Rooftop cat companion** — Maple sells a Kitten Ticket (250g); `-` near the farmhouse redeems then pets; cat perches on the farmhouse roof; pet streak tips morning gold (8g/day, cap 14). (a57147b)
- [x] **Fishing rod upgrades** — Maple sells copper/iron/gold rods (250/700/1800g, `=` keybind at the shop); per-tier bite-window scale + fish-pick bias makes pike & trout meaningful catches. (e36f20d)
- [x] **Stamina / energy system** — daily 100-point pool drains on till/water/mine/cast; refills at dawn + on sleep; Z sips the best available drink (cocoa +35 > tea +20). (1897764)
- [x] **Travelling merchant cart** — Pip parks at the village square on day 3 of every season (09-18h); E opens a premium catalog with rare seeds, ready-to-drink flasks, and a brass lantern cosmetic. (991bbbc)
- [x] **Auto-restock seed kit** — buyable 600g kit re-buys the player's last-planted seed up to 5 every dawn from the shop's gold; spend tagged in the money log. (c3331d4)
- [x] **Wallpaper / floor cosmetics for the farmhouse** — three wallpapers + three floors sold at Pip's cart (260-420g); apply auto-applies; decorPalette() retints the farmhouse renderer only. (78d5e68)
- [x] **NPC marriage move-in** — after the wedding the spouse stands south of the farmhouse door during day + inside at night; dawn gift + per-spouse greeting line for E-press dialogue. (8527af9)
- [x] **Bookshelf / lore panel** — backtick toggles a 5-tab bestiary (fish/gems/forage/crops/folk) that auto-discovers entries as the player encounters them. (2d0a556)
- [x] **Quest-of-the-week board** — village notice board south of the well posts one rotating fetch quest per season; E reads / turns in; nine catalogued tasks. (332b335)
- [x] **Seed extractor** — Maple sells a 350g kit; L consumes one of the largest harvest stack and grants 1-2 seeds of the same crop (deterministic alternating yield). (f0fb391)
- [x] **Friendship tournament** — day 6 of every season the village runs a contest at the well (flower show / fishing derby / harvest weigh-in / cook-off); bronze/silver/gold ribbons + gold prizes; one entry per season. (487425a)
- [x] **Maple's Shop menu** — adjacent to the shop, E opens a tabbed (Seeds/Kits/Tickets/Misc) buy menu built from SHOP_ITEMS; singletons drop off the list once owned; closes the dev-console-only-buy gap that blocked half the kits. (0fcb284)
- [x] **Carpenter's bench** — village fixture south-east of the well; E opens a craft menu; scarecrow / brass lantern / stone fence / coop deluxe upgrade recipes turn gold + mid-tier gems into placeables. (70e3e88)
- [x] **Outdoor scarecrow** — crafted at the bench; { keybind plants on a grass tile next to your field; crops within Chebyshev 3 ripen one tier higher at harvest (normal->silver, silver->gold). (48806fb)
- [x] **Animal yield variety (fancy eggs)** — chickens roll for a fancy egg each dawn at 8% basic / 18% deluxe; fancy eggs sell at 3x; `}` applies a Coop Deluxe Upgrade Kit (700g + 2 iron) crafted at the bench. (1c41787)
- [x] **Owl post** — `~` near the farmhouse opens the owl menu; pay 40g to dispatch the village owl with your best gift for the chosen candidate; honors the per-day gift gate and never charges on a failed send. (e93a9b5)
- [x] **Crop ribbon journal** — track the heaviest single-day harvest per crop; surfaced under the streak row in the ; panel with season tag (e.g. "ribbon: 4 in a day - Fall d4"). (11136b6)
- [x] **Animal happiness** — coops carry 0..100 happiness (collect +5 / feed +3, decays 1/day, caps once-per-day); adds up to +6pp to the fancy-egg roll. Dog + cat petStreak gets a compounding flat tip (+1/+2/+3 at 8/12/14). (4e9da70)
- [x] **Seasonal storm event** — once-per-season deterministic storm on a day in [2..6]; outdoor crops lose a streak day, forage clears, greenhouse-protected crops are safe. (278d3b3)
- [x] **Late-night fishing perk** — 22-04h casts apply a per-fish bias on top of rod-tier (minnow x0.4, trout x1.75, pike x2.25); compounds with gold-rod bias. (ccf8dc1)
- [x] **Stamina-tea cookbook** — Berry Tonic / Mushroom Broth / Sunflower Elixir join the cookbook; all three slot into STAMINA_RESTORE so drinkBest tiers them above tea + cocoa. (9807913)
- [x] **Bath house** — late-game fixture NE of the plaza at (30,7); E for a 200g soak that lifts the stamina cap by +30 for 3 days; tops the pool immediately + drops the cap back at dawn after expiry. (163549f)
- [x] **Fish pond at the farm** — stock the carved 4x4 pond with the most-abundant fish in your bag via `>`; daily yield of the same species (minnow 2/day, others 1/day) caps at POND_MAX_PENDING=6. (3aa9bb7)
- [x] **Weekday market discount** — deterministic per-(season,day) 20% off one rotating SHOP_ITEMS row; banner + DEAL tag + strikethrough base price in the shop menu. (b3e19eb)
- [x] **Town board reputation** — completedCount drives five tiers (Newcomer/Regular/Trusted/Pillar/Cornerstone); reward multipliers 1.0x..1.75x; rep banner in the board hint, base + bonus logged separately. (8e956dc)
- [x] **NPC moveable schedules** — each schedule slot can carry a walkTo waypoint; NPC eases between (x,y) and walkTo on a cosine wave (default 2h period). Every NPC now has at least one walking slot. (f659bac)
- [x] **Hatchery** — bench-craftable 220g+2copper basket; place next to a coop; load a fancy egg via `6`; 5-day incubation hatches a chick into the nearest free coop. pendingChicken flag holds the chick when every coop is full so the player can free a slot and press `6` to claim. (78b0cf6)
- [x] **Compost bin** — 140g Maple's shop kit; place on grass with `7`; deposit normal-tier `<crop>_harvest` via `7` adjacent; 3 days later mints floor(crops/4) fertilizer bags; apply a bag to a crop in front for +2 waterStreak. Silver/gold harvests stay in the bag. (8ea90a6)
- [x] **NPC chat barks** — short per-(day,hour) deterministic line floats above an NPC's head when the player walks within 2 tiles; 12s per-NPC cooldown; bubble fades after 3s. Pure ambient layer separate from the E-press dialogue catalog. (f4e7957)
- [x] **Bath house winter discount** — Winter (season 3) drops the soak cost from 200g to 150g via bathPriceFor(time); BathOutcome carries pricePaid + discounted; money log + flavor line distinguish the rate. (17d6494)
- [x] **Storm shelter** — bench-craftable 400g+1iron one-shot lean-to; protects every crop within Chebyshev-1 from the next storm; consumed on impact; stormFlavorLine now mentions sheltered + consumed counts. (2f29045)
- [x] **Storm shelter co-op** — two shelters within Chebyshev-2 widen each one's effective coverage from a 3x3 to a 5x5 for the next storm; both still consumed. effectiveRadius() snapshots before mutating shelters[] so removing one half of a pair doesn't shrink the survivor mid-loop. (d4d6b9a)
- [x] **Fertilizer-of-the-week** — per-season rare day (deterministic via 32-bit hash) routes compost batches into RARE_FERTILIZER_INVENTORY_KEY worth +4 streak vs regular +2; applyFertilizer prefers rare first; dawn toast names the rare delta. (4aac877)
- [x] **Hatchery rare-chicken roll** — HERITAGE_HATCH_RATE=18% per hatched chick (deterministic rollHeritage(tx,ty,day)); coop.heritage[] flag per-slot; coopTick adds HERITAGE_FANCY_BONUS=0.15 to per-chicken fancy rate; pendingHeritage survives full-coop hatches. (d1f9812)
- [x] **Pond species rotation** — reseedPond() lets the player swap species when the pond is stocked-but-empty AND a different fish is in the bag; interactPond auto-tries it; status line hints the swap path. (04b4a6e)
- [x] **Bath house spa pass** — 700g punch card at Pip's cart carries SPA_PASS_PUNCHES=4 free soaks; takeBath auto-redeems then prefers punches over gold; bathFlavorLine names the spa-pass path; persists through SaveSnapshot.player.spaPass. (e311998)
- [x] **Rare-fertilizer HUD glance** — compostStatusLine() gained optional season + dayOfSeason; surfaces "RARE day in N days (day X)." pre-rare, "one batch on the rare-day track." on staged batches, "(N RARE)" tag on ready bags; rareDayCountdownLine() helper; deposit toast tags rare-day batches. (0b5bec1)
- [x] **Weather forecast accuracy upgrade** — new barometer.ts module + Brass Barometer at Pip's cart (300g, singleton); hasBarometer() + weatherDayAfterTomorrow() with +2 calendar carry; weather-strip widens 220→320px and adds "After" pill when owned. (d8f6836)
- [x] **Roving merchant rumor** — new cart-rumor.ts; deterministic per-(season+1) headliner pick from non-decor CART_CATALOG rows; rumorToastLine() rides dawn toast on Pip's arrival day; rumorFooterLine() draws above cart menu's hint. (e267935)
- [x] **Heritage breeding** — BREEDER_EGG_INVENTORY_KEY + HERITAGE_BREEDER_RATE=0.4; coops with ≥2 heritage chickens roll one fancy egg into PlacedCoop.breederEggs on a deterministic breederRoll(tx,ty,day); breeder eggs preferred by hatchery.loadEgg and force incubatingHeritage=true. (20085fd)
- [x] **Spa pass loyalty card** — BathState gained totalSoaks + soapsGifted; takeBath gifts a PERFUMED_SOAP_INVENTORY_KEY cosmetic every SOAP_PER_SOAKS=10 lifetime soaks; expectedSoaps vs soapsGifted gap prevents double-gifting on reload; bathLoyaltyLine() helper. (87fa150)
- [x] **Perfumed Soap usage** — hearts.tasteOf returns 'loved' for PERFUMED_SOAP_GIFT_KEY across every candidate (mirrors BOUQUET_KEY); pickBestGift recognises it as universal loved; owl post can carry it; new tests/perfumed-soap-gift.test.ts. (d2d3c82)
- [x] **Breeder egg trade-in** — Pip auto-buys breeder eggs at BREEDER_TRADEIN_PRICE = FANCY_EGG_SELL_PRICE*2 (72g/egg) on cart-menu open; tradeBreederEggs() gates on cartOpen + nearCart; money log tags it; breederTradeInLine() singular/plural copy. (e08d80e)
- [x] **Cart rumor reward** — 5% gold-back on the headliner Pip teased last visit; new currentSeasonHeadliner(season) = rumorItemForSeason(season+3); RUMOR_REBATE_PCT=0.05; rumorRebateAmount() floors; cart-menu draws a "TEASED -Ng" chip on matching rows; money log records the rebate. (1c0bfe8)
- [x] **Barometer storm warning** — barometerStormWarning(player, time) surfaces "Storm tomorrow|in 2 days — line up shelters now." when a barometer is mounted AND a storm sits in the 2-day forecast horizon; surfaced on the dawn-toast tail AND the village board interaction. (0d9ad5a)
- [x] **Pond decor upgrade** — Stone-Rim Pond Kit at Pip's cart 450g singleton; POND_MAX_PENDING_RIM=10 raises pending cap from 6 -> 10; pondMaxFor(player?) + hasPondRim() helpers; pondTick + pondStatusLine took optional `player` arg with backwards-compat defaults to base cap. (7da4405)
- [x] **Pond fish ribbon** — track heaviest single-day pending count per species; PondState.ribbons map keyed by FishKey; recordPondRibbon() pure comparator; pondTick gained optional 4th `time` arg; pondStatusLine appends "[ribbon: N in a day - Season dD]" tag when stocked species has a record. (30d38a1)
- [x] **Bench recipe shopping list** — recipeShoppingList(player, recipe) returns "need Xg more, need N more <gem>" plain-language string for un-craftable recipes; bench-menu draws it in amber-orange on dimmed rows. (32d1afb)
- [x] **Spa pass refill at Pip's cart** — new 500g Spa Pass Refill catalog row (vs 700g full pass); credits SPA_PASS_PUNCHES directly into the spa-pass pool with no inventory key; canRefillSpaPass gates on no-bag-pass + zero-punches + totalSoaks>0; cart-rumor pool filters the refill row out. (c37849e)
- [x] **Pet treat** — gift a stamina tea to the dog/cat AFTER the daily pet to bump streak +1; treatDog/treatCat consume cheapest tea first; gated by owned+adjacent+already-petted-today+below-cap. (69026c8)
- [x] **Storm shelter free trial** — first storm of a save auto-spawns a shelter on the lowest-(ty,tx) outdoor crop so the player learns the system; seedTrialShelter() in storm-shelter.ts; game.ts dawn block runs it BEFORE maybeFireStorm on the storm-scheduled day only. (42902a2)
- [x] **Pond fish dawn-bite hint** — when a pond will overflow tomorrow (pending+yield > cap), the dawn toast nags the player to collect. pondWouldOverflowTomorrow + pondOverflowWarning helpers; game.ts layers the nag as a "* tail" on the dawn headline; honours the stone-rim cap. (12e5dc9)
- [x] **Bath house seasonal towel cosmetic** — 5-soak-per-season streak gifts a Spring/Summer/Fall/Winter Towel; BathState gains per-season soak counters; bathFlavorLine names the gifted towel by season. (34d06cf)
- [x] **Mining cart return** — per-run gem haul tally that resets on sleep; recordMined on each strike; haulYesterdayLine surfaces "yesterday's haul: 2 copper, 1 ruby (worth 156g)" as a third optional dawn-toast tail; persistence snapshots both the running tally + last run's recap. (aab5349)
- [x] **Tournament personal-best dawn nudge** — pairs the existing tournamentDawnLine with PB + today's count + next tier ("Carrying 6 - silver needs 8. (PB: silver at 12.)"); personalBestFor / nextTierToClear / scoreTodayFor / tournamentNudgeLine helpers. (78ad507)
- [x] **Cart rumor history** — RUMOR_HISTORY_CAP=4 ring buffer of past headliners + bought flag; recordRumorVisit on cart open, recordRumorBuy on the matching buy; cart-menu footer shows "Headliners: 2/4 bought."; persisted across reloads. (96803cd)
- [x] **Mine haul achievement** — `lifetimeCounts` map on `MineHaulState` that never resets on sleep; LIFETIME_MINING_MILESTONE=100 lights up the new `cave-veteran` badge; persistence carries lifetimeCounts with older-save backfill. (71b0f71)
- [x] **Lore-panel Rumors tab** — LORE_CATEGORIES gains 'Rumors'; buildLoreRows walks getRumorHistory entries newest-first; loreCompletion filters Rumors out so the bestiary % isn't dragged around. (66fdd82)
- [x] **Compost recycle (compost compost)** — ApplyOutcome 'applied' gains recycledGold; 1g per regular bag / 3g per rare bag credited directly to player.gold; engine logs "compost recycle" + "(+Ng recycled)" tail on the toast. (68ff1f3)
- [x] **Breeder egg cookbook tie-in** — PREMIUM_SELL_MULTIPLIER=1.5; canCookPremium / cookPremium swap one breeder egg for one regular egg in any egg-bearing recipe; premium dish lives in `dish-<key>-premium`; sellAllDishes bundles both stacks; premiumCookLine codex hook. (0811dc9)
- [x] **Stamina-tea double batch** — DOUBLE_BATCH_INGREDIENT_MULT=2 + DOUBLE_BATCH_DISH_YIELD=3; canCookDoubleBatch + cookDoubleBatch gated to STAMINA_TEA_KEYS; bonus dish piles into the same `dish-<key>` slot so drinkBest path is unaware; doubleBatchLine codex formatter. (d64a422)
- [x] **Premium dish codex section** — RecipeCodexRow widened with hasPremium/premiumLine/premiumSellPrice/premiumCookedCount/premiumOwned; new premiumCookCounts map + recordPremiumCook/totalPremiumDishesCooked; codex header surfaces "N premium" tally + per-row pink chip + dim premium markup line. (3a394ea)
- [x] **Lifetime mining tally HUD glance** — loreTabFooter(player, category) pure formatter; Gems tab surfaces "career: N gems / Ng" with locale thousands separators; older-save backfill verified. (9e9b389)
- [x] **Double-batch inn UI hook** — CookingMenu.confirm() takes optional mode='single'|'double'; Shift+Enter routes through cookDoubleBatch; new 'not-eligible-double' branch + widened 'cooked'/'missing' with mode+yield; cook history bumps outcome.yield times. (d97114d)
- [x] **Compost lifetime recycle ledger** — CompostLedgerState (lifetimeRecycledGold + lifetimeBagsApplied) bumped from applyFertilizer; new `compost-master` achievement at 100g milestone; compostLedgerLine surfaces "compost master: 128g recycled across 47 bags." in the crop journal. (85ccbad)
- [x] **Rumors panel filter** — RUMOR_FILTERS three-way cycle + applyRumorFilter pure helper; LorePanel.rumorFilter resets on open; `f` keypress cycles all/bought/skipped on the Rumors tab; chip + close hint surface only on Rumors; global fishing `f` guarded against the panel. (af25717)
- [x] **Breeder's Bowl achievement** — BREEDERS_BOWL_MILESTONE=25 + breedersBowlMilestoneReached predicate on totalPremiumDishesCooked; 18th badge in the catalog. (f6db347)
- [x] **Cart rumor streak** — buy 3 headliners in a row stacks a 5g/step discount on the next headliner (capped at 10g to align with RUMOR_HISTORY_CAP=4); rumorStreakLine chip + cart-menu footer + game.ts refund post-buy mirroring rebate. (20832ba)
- [x] **Mining run goal toast** — mid-run callout when haul crosses 3/6/10 gems; crossedMilestone pure detector returning the HIGHEST crossed tier; appended as a "  -  <line>" tail to the strike toast so the existing grade+gold message isn't clobbered. (4f50fb8)
- [x] **Stamina-tea trade-in at Pip's cart** — exchange 3 cheap stamina teas (herb-tea + berry-tonic, cheapest-first) for 1 Hot Cocoa; auto-fires on cart open right after tradeBreederEggs; high-restore teas intentionally non-eligible — trade is convenience, not stamina arbitrage. (69d41cc)
- [x] **Owl post stamp book** — OwlStampBook tracks per-NPC lifetime owl dispatches; bumped INSIDE dispatchOwl on the only path that charges the fee; lore Folk tab description gains " Owl posts: N." tail when nonzero; persisted with older-save backfill. (de5ead6)
- [x] **Fluent-with-the-owl achievement** — OWL_FLUENT_MILESTONE=25 one-liner predicate over totalOwlStamps; 19th badge; same lazy-ledger pattern as cave-veteran / compost-master / breeders-bowl. (1ad5725)
- [x] **Pulper achievement** — PULPER_MILESTONE_BAGS=500 one-liner over compostLedger.lifetimeBagsApplied; 20th badge; ladders cleanly with compost-master (100g recycled = "found the loop" -> 500 bags = "ran the loop"). (669215a)
- [x] **Mining run gold callout** — MINING_RUN_GOLD_MILESTONES=[100,250,500] parallel to count tiers; crossedGoldMilestone + goldMilestoneToastLine; engine appends as a second "  -  <line>" tail to the strike toast AFTER the count-tier tail. Pure-iron grinds and ruby spikes both light up. (9152855)
- [x] **Stamina-tea trade-in HUD glance** — staminaTeaTradeInLine three-bucket formatter in cart.ts; cart-menu draws a chip ABOVE the rumor-streak chip (y-78) surfacing "trade ready: 3 teas -> Hot Cocoa." / "1 tea - need 2 more for a Hot Cocoa trade." / empty. (73e33ee)
- [x] **Inn forage trade-in** — new src/game/inn-trade.ts; 3 surplus berry+herb -> 1 Sage Tea; auto-fires on cooking-menu open at the inn; mirrors the cart-side trade-in shape from the other direction. Cooking-menu UI gains a chip at y-38 above the keybind footer. (1098227)
- [x] **Compost halfway nudges** — compostLedgerLine surfaces two mutually-exclusive "to badge" tails: "(Xg to the badge)" while 50<=lifetimeRecycledGold<100, "(N bags to the pulper badge)" while 100<=lifetimeBagsApplied<500. Pulper nudge starts the moment compost-master nudge ends; both auto-disappear once the badge is earned. (0ed5e51)
- [x] **Cart-tea-tradein inn cross-nudge** — staminaTeaTradeInLine appends " - or trade N forage at the inn for Sage Tea" when player is short on teas at the cart but carries enough forage to mint a Sage Tea. Suppressed in the trade-ready branch + when forage<COST. Closes the loop on the trade-in trio HUD cross-references. (938012f)
- [x] **Owl fluency tiers** — per-NPC OWL_FLUENCY_TIERS=[5,15,25]->[occasional,regular,favorite] composed onto owlStampLine; lore Folk row surfaces e.g. "Owl posts: 18 (regular pen pal)."; 1-4 plain-count noise floor unchanged. Top tier matches OWL_FLUENT_MILESTONE so a global fluency run lights up at least one per-NPC favorite tag. (2f6d1ba)
- [x] **Mining haul ribbon** — MineHaulState.bestRun{count,countDay,gold,goldDay} captured at sleep via resetMineHaul(player, day); count + gold promoted INDEPENDENTLY. haulYesterdayLine appends three variants of the record-fell suffix; haulBestRunLine passive formatter; Gems-tab footer composition gains " - best run: N gems / Xg (day D)" tail; persistence + engine sleep wired. (3657aed)
- [x] **Owl post fee discount** — OWL_POST_TIER_DISCOUNT_PCT keyed off the fluency tier label (10/20/30%) so favorite-courier paths pay 28g instead of 40g per send. dispatchOwl reads owlPostFeeFor once for ... [truncated]
- [x] **Mining haul ribbon achievement (Deep Vein)** — DEEP_VEIN_COUNT=20 OR DEEP_VEIN_GOLD=1000 single-run thresholds; deepVeinMilestoneReached reads bestRun off the existing MineHaulState; 21st badge in the catalog using the same lazy-ledger pattern as cave-veteran / compost-master / breeders-bowl / pulper. Either-axis OR gating so a quantity grind (20 copper) and a value grind (one ruby trip) both light it up. (9e0d6ad)
- [x] **Mining haul ribbon dawn-nag** — RECORD_GAP_NAG_MIN_PCT=50 floor; recordGapNagLine() appends a "- N% of your best run" tail to haulYesterdayLine on close non-record runs; cap at 99% keeps the brag tail + nag tail mutually exclusive; picks max(countPct,goldPct) so a near-gold run still surfaces. Silent on fresh saves / empty runs / record-breaking runs / below-floor runs. (c5257ec)
- [x] **Compost halfway dawn nudges (master + pulper)** — compostHalfwayDawnNudge() one-shot helper with two sticky boolean flags on CompostLedgerState (masterNudgeDawnFired + pulperNudgeDawnFired); wired into engine dawn-toast chain as a fourth tail after pondOverflow + haulRecap. Survives reload via persistence. Master priority over pulper when both simultaneously eligible (hand-craftable but unreachable in normal play). Surfaces "Halfway to Compost Master - 32g to go." / "Halfway to Pulper - 314 bags to go." (09585b8)
- [x] **Owl-menu per-NPC fee chip** — owlPostFeeChip() pure helper drawn on each owl-menu row in GOLD when discounted / HINT-dim at full price. Surfaces "32g (-8g, regular)" using the SHORT tier word (occasional/regular/favorite) without the "pen pal" / "courier" suffix so the chip stays under 30 chars. Closes the UX gap where the tier discount was invisible in the menu (only visible in the post-dispatch toast). (e08ae4c)
- [x] **Tournament career recap** — TournamentCareer aggregate + tournamentCareer walker + tournamentCareerLine four-wording renderer + tournamentNudgeWithCareer composite. Engine swaps tournamentNudgeLine for the with-career variant on the tournament-dawn line so the toast carries IMMEDIATE strategy ("Carrying 6 - silver needs 8") + LONG-TERM view ("Career: 3 entries - 1 gold / 1 silver. Best: 18 (Spring Flower Show).") in one breath. Recap parses entries-map key shape `${season}-${kind}` to recover bestKind without a new field. (f097a53)
- [x] **Dawn-toast composer refactor** — assembleDawnToast(headline, tails[]) pure helper retires the engine's growing chain of `if (tail) headline = ${headline} · ${tail}` ternaries. Observation #2 retired BEFORE the chain hit 6 branches. Engine swaps the four-line ladder for one helper call with the same three tails in the same order; runtime byte-output unchanged. New tails (e.g. deep-vein dawn brag this tick) just append to the array. (8921f72)
- [x] **Deep Vein dawn brag** — one-shot celebratory dawn-toast tail naming the gems / gold of the record run the morning AFTER bestRun crossed either deep-vein threshold for the first time. Two sticky flags on MineHaulState (deepVeinBragPending armed inside resetMineHaul on fresh crossing, deepVeinBragFired set inside the composer on first read). Three wordings depending on which axis tripped the bar (count-only / gold-only / both at once). Both flags round-trip through persistence with `=== true` coercion. Surfaced as the 4th tail on the assembleDawnToast composition. (7f80a8f)
- [x] **Festival Regular achievement** — 22nd badge keyed off tournamentCareer().entries == 4, the natural ceiling of the (season,kind) entries map ("you've entered every kind at least once — full calendar of festival attendance"). Tuned at 4 = Spring flower-show + Summer fishing-derby + Fall harvest-weigh-in + Winter cook-off; anything higher would be unreachable since enterTournament locks out re-entry in the same slot. Lazy-ledger pattern, no new persisted counter. (b8e5700)
- [x] **Mine haul ribbon journal page** — bestRun gained countComposition + goldComposition Partial<Record<GemKey,number>> fields captured at sleep time. bestRunCompositionLine + bestRunSplitCompositionLine + loreTabDetailLine helpers. Lore Gems-tab footer grew a secondary detail line below the career + ribbon header surfacing "made of: 4 copper, 3 ruby, 2 emerald" on single-record saves and "count run: 8 copper, 4 iron - gold run: 1 ruby, 1 emerald" on split-record saves. Panel grows by 14px when the detail line is non-empty so the career footer doesn't shift on non-Gems tabs. Persistence deep-copies the composition maps so the snapshot doesn't share refs with live state. (edcb4c9)
- [x] **Owl-post fluency tier-color chip on owl-menu** — bronze/silver/gold chip drawn just LEFT of NPC name on each owl-menu row, mirroring the lore Folk row chip; shares OWL_FLUENCY_TIER_COLOR palette. Also adds pending-chain preview ("chain: N") in the hearts label when previewChainLength >= 2. Closes observation #5's chip pattern loop. (e982c25)
- [x] **Tournament Champion achievement** — 23rd badge keyed off tournamentCareer().ribbons.gold >= 3; orthogonal to festival-regular; lazy-ledger pattern, no new persisted field. (c052c32)
- [x] **Owl-post chain bonus** — N consecutive days of same-recipient dispatchOwl stacks a heart-points multiplier (1.0/1.1/1.2/1.3 at length 1/2/4/7); chain breaks on day skip or recipient swap; chainLength + chainMultiplier surface on OwlPostOutcome; engine setToast appends "Letter chain xN (+M%)." Also fixed bug where money log over-reported owl-post fees by ignoring tier discount. (34d15dd)
- [x] **Lore Folk row owl-post chain indicator** — "Chain: N days." tail on the lore Folk row description when player is riding a consecutive-day owl-post chain with that specific NPC AND length >= 2. Per-NPC isolation; swap/skip drops the old tail. (2634aca)
- [x] **Compost-master + pulper journal-line refactor** — extract the journal-line passive nudge logic into a generic ladderNudge() helper so a third nudge tier (e.g. a "you've earned all compost badges" sash) doesn't keep stacking ternaries on compostLedgerLine. Gets ahead of refactor pressure same way last tick's assembleDawnToast did. New LadderNudgeRung interface; compostLedgerLine + compostHalfwayDawnNudge both walk the same rung shape. (6b5bc05)
- [x] **Owl-post chain dawn celebration** — when the active chain crosses a new tier (length 2 -> 1.1x, length 4 -> 1.2x, length 7 -> 1.3x), the dawn toast surfaces a one-shot "Your owl chain is in the regular tier now (+10%)." tail on the morning AFTER the tier crossing. Sticky flag pattern same as the deep-vein dawn brag. (2b7d43e)
- [x] **Owl-menu chain-bonus payout chip** — when the player's pending chain would land at a >=2 tier on Enter, surface a small "+10% hearts" tag on that row in green. Reuses chainBonusMultiplier; pure visual. (1b498f2)
- [x] **Compost-master sash achievement** — a 3rd compost-ladder rung at lifetimeRecycledGold >= 250; lands as a one-line array push into the ladderNudge rung set in compostLedgerLine + compostHalfwayDawnNudge, validating last tick's refactor on the spot. (0ea7e14)
- [x] **Pulper rare-bag bonus tier** — rare bags are worth 4x streak; the lifetimeBagsApplied counter doesn't distinguish them. A 4th "rare master" achievement tracks lifetimeRareBagsApplied separately and gates at 100 rare bags. (e9fde58)
- [x] **Champion-tier owl menu sort** — owl-menu rows sort active-chain NPCs first so the player can see at a glance which friendships they're actively maintaining. (739dd4b)
- [x] **One-shot brag generic helper** — extract the sticky-flag dawn-brag pattern (deep-vein + chain-tier + sash + rare-master) as oneShotBrag(carrier, pendingKey, firedKey, render). Pure module; works on any carrier with optional pending/fired fields. Refactored deepVeinDawnBrag + chainTierDawnBrag onto the helper as zero-behavior-change validations. 16 new tests for the helper itself. (4fdd583)
- [x] **Compost-master-sash dawn brag** — one-shot dawn-toast tail the morning AFTER lifetimeRecycledGold crosses 250g sash milestone for the first time. New sashBragPending + sashBragFired flags on CompostLedgerState; recordApplied snapshots pre-bump gold and arms only on fresh crossing. Persistence round-trips both flags. 6th tail on assembleDawnToast composition. (9cefb89)
- [x] **Rare-master dawn brag** — symmetric one-shot dawn-toast tail when lifetimeRareBagsApplied crosses 100. Reads separate rare-bag counter so regular-bag grind doesn't fire it. 7th tail on assembleDawnToast. Chained test verifies sash + rare-master independence. (4ff8eac)
- [x] **Rare-master journal-line nudge** — 4th rung in compostLedgerLine ladderNudge + 3rd rung in compostHalfwayDawnNudge for [50, 100) rare bags. Reads separate rare-bag counter so the rung is orthogonal to the gold/total-bags rungs. Priority master/sash/rare-master/pulper; rare-master sits before pulper because rare bags are harder to grind. New RARE_MASTER_NUDGE_MIN_BAGS=50 + rareMasterNudgeDawnFired sticky flag. Validates ladderNudge refactor a third time. (5ff8aa1)
- [x] **Owl-menu chain-target row halo** — subtle 2px sage-green outline on the active-chain-target row drawn in addition to the row-position hoist. Halo color matches BONUS_GREEN chip so the two visual cues read as the same "active streak" language. When the row is also selected the halo overrides the BORDER outline for one strong indicator. Length-1 chains light up. New isActiveChainTarget(player, npcId) pure helper. (f05132b)
- [x] **Dawn-toast tail grouping refactor** — assembleDawnToast accepts EITHER the flat array (legacy) OR a grouped {system?, achievement?} object. System tails join with " · ", achievement tails join with each other using " • " inside one " · "-attached cluster so a morning that lands 3+ brags reads as ONE celebration burst rather than 3 peer phrases. Empty groups skip cleanly; flat-array call shape preserved for back-compat. Engine swaps the 7-tail dawn through the grouped form: system=[pondOverflow,haulRecap,compostNudge] + achievement=[deepVein,chainTier,sash,rareMaster]. (57909ea)
- [x] **Owl chain-recipient brag** — one-shot dawn-toast tail the morning AFTER the active chain reaches OWL_CHAIN_RECIPIENT_BRAG_LENGTH=25 consecutive days with one specific NPC. Per-recipient sticky semantics — each NPC earns the brag once, but a DIFFERENT recipient hitting the same threshold later fires its own brag. Pending field carries the npcId so a chain reset between the crossing day and the next dawn still surfaces the right name. Slots as the 8th tail on the achievement cluster validating the tail-grouping refactor on the same tick. (29fd938)
- [x] **Mine haul split-record dawn brag** — one-shot tail the morning AFTER MineHaulState.bestRun first transitions from same-day record into split-record state (countDay !== goldDay). wasSplit pre-update + nowSplit post-update arm only on the FIRST transition; splitRecordBragFired audit gates re-firing so merge-back-then-split-again does NOT re-fire. Saves already split before the flag existed do NOT spuriously arm. (d6c367e)
- [x] **Owl-menu chain countdown chip** — compact "Nd streak" chip drawn next to the chain-bonus chip on the active-chain-target row. activeChainCountdownChip pure helper. Length-1 streaks DO get the chip filling the "streak exists but no bonus yet" gap. Chain-break detection mirrors recordOwlChain. Drawn in BONUS_GREEN so halo + bonus chip + streak chip all read as one "active streak" language. (f3d95be)
- [x] **Vein Connoisseur achievement** — 26th badge rewards BREADTH. VEIN_CONNOISSEUR_PER_GEM=10 lifetime of EVERY gem type (copper, iron, silver, gold, ruby). veinConnoisseurMilestoneReached walks GEM_KEYS, lazy-ledger pattern via existing lifetimeCounts. Orthogonal to cave-veteran (lifetime-100-total): vein-connoisseur is unshortcuttable with a one-gem grind. (9f4159a)
- [x] **Toast queue (stack same-frame notifications)** — pure ToastQueue FIFO replaces the single toast string + fade timer that clobbered all but the last writer when two messages fired the same frame. Each entry lives its own TTL; renderer draws up to 3 newest-first as a stack (freshest pill bright-bordered, older ones dim + fade independently); duplicate-text repeats refresh the newest age rather than walling identical pills; toastAlpha owns the fade curve. Legacy this.toast/this.toastFade removed. (ade101c)
- [x] **Controls help overlay (`?`)** — controls.ts single source of truth for ~40 keybinds grouped Move/Farm/Gather/Animals/Upgrade/Home/Panels; splitControlColumns balances two columns; HelpOverlay renders a centered card with rounded key-caps. Test guards no-emoji + that the new wayfinding keys (?,9,0) + every panel toggle stay catalogued. (9482b1d)
- [x] **Sun/moon sky dial HUD** — sky-dial.ts maps time onto sun-by-day / moon-by-night on a horizon-to-horizon arc (arcT 0..1 across the active window, altitude a sine hump peaking at zenith, night arc wraps midnight monotonically); widget draws the dotted arc + pixel sun/crescent + phase caption + "Nh Nm left" daylight readout, right-aligned below the weather strip. daylightMinutesLeft + phaseLabelFor pure helpers. (eea66be)
- [x] **Village minimap (`9`)** — minimap.ts projects the 40x30 world into a colour grid + derives labelled landmark markers from world.buildings + the cave; MinimapPanel blits the grid + glyph pips (Home/Shop/Inn/Well/Cave) + a pulsing you-are-here dot + a two-row legend; projectTile keeps tiles + markers pixel-aligned at any map size. (a44b2e6)
- [x] **Almanac event planner (`0`)** — almanac.ts aggregates every dated event (festivals, NPC birthdays, Pip's cart, friendship tournament) within a 14-day horizon into one soonest-first list, reusing the 28-day calendar wrap; AlmanacPanel renders colour-railed rows with kind tag + title + detail + Today/Tomorrow/in-N-days countdown + Spring-7 date stamp + a calm empty state. (9ac8252)
- [x] **Almanac highlight on the HUD** — almanacHighlight()/highlightChipText() pure helpers reuse buildAlmanac's soonest-first ordering; new almanac-chip.ts draws a one-line right-column pill under the sky dial ("Tomorrow: Maple's birthday") only when an event lands Today/Tomorrow, with a kind-keyed colour rail + green "Today" text; quiet stretches render nothing. (c7d5179)
- [x] **Minimap quest/landmark pings** — minimapPings(player,time) derives pulsing rings from existing predicates (canTurnIn for the board, cartOpen for Pip, tournamentOpen && !alreadyEntered for the well); MinimapPanel.draw now threads player+time and renders expanding rings (over pips, under the you-are-here dot) + a solid inner dot; empty array on a calm day. (56a7485)
- [x] **Crop-quality heatmap overlay** — qualityHeat(streak,growthStages) maps the streak->quality ladder onto five readable bands (dry/building/almost/silver/gold) with care-ramped alpha; new quality-heatmap.ts washes every crop tile in world-space (camera-projection callback, same pattern as drawBarks) + a bottom-left legend, shown while the crop journal (`;`) is open (no new keybind). (22cade2)
- [x] **Sky-dial weather tie-in** — skyWeatherStyle(weather) pure render-hint deriver ({dimmed,cloud,storm}); the sky-dial widget dims+greys the sun/moon and tucks a small cloud puff (darker for storms) over it on rain/storm via weatherToday(), so the dial reads the sky not just the clock. (7c908fe)
- [x] **Hotbar seed-count low warning** — seedWarnLevel(count) none/low/empty + seedWarnPulse(level,nowMs) deterministic sine breathe (empty pulses faster + floors brighter) in new pure hotbar.ts; HUD widget gained an optional nowMs arg (defaults 0) and pulses an amber border on low/empty seed slots + tints the empty count badge amber, riding over the selection border. (3c8becd)
- [x] **Toast category colour rail** — ToastKind ('info'|'money'|'hearts'|'achievement') + TOAST_KIND_COLOR palette + toastKindColor() fallback; ToastEntry carries a kind, push() takes an optional kind, a duplicate refresh adopts the latest; classifyToast(text) heuristic infers the kind so setToast classifies centrally; renderer draws a 3px left rail per pill. (3c32330)
- [x] **HUD scale honoured by right-column widgets** — pure game/hud-layout.ts rightColumnLayout(scale) returns each widget's scaled y+height; weather-strip/sky-dial/almanac-chip take an optional hudScale + scale internals (incl. sun/moon/cloud via a u() helper); at 1.0x reproduces the historical fixed positions exactly. (236e457)
- [x] **Reduce-motion for minimap + hotbar cues** — pure playerDotRingAlpha(pulseMs,reduceMotion) + pingRing(...) + seedWarnSteady/seedWarnIntensity; under reduceMotion the minimap player-ring holds steady, the ping drops its expanding ring (inner dot grows 2->3px), the hotbar warning holds a steady amber instead of breathing. Threaded settings.reduceMotion through drawHUD + minimapPanel.draw. (c7800b0)
- [x] **Crop-heatmap field summary** — pure qualityHeatCounts(samples) + qualityHeatSummary(samples) tally crops into the five bands and render a compact urgency-ordered line ("3 dry, 1 about to silver-star"); drawn as a header row in the heatmap legend, hidden on an empty field. (6510022)
- [x] **Minimap ping reason legend** — pure pingLegend(pings) de-dups active pings into {color,reason} rows; MinimapPanel derives pings once (shared by rings + legend), grows the panel only when pinging, and drawPingLegend renders a ring swatch + plain-language reason per row so the pulse meaning isn't hue-only. (abf1091)
- [x] **Animated crop sway / wind** — pure game/crop-sway.ts computes a sub-2px sine lean for the canopy of mid/ripe crops (stage gated; seeds/sprouts flat 0); per-tile phase so neighbours ripple out of sync; renderer.draw threads nowMs+reduceMotion (defaulted, back-compat); reduce-motion returns 0 so the field calms like rain/snow. (7fcc8f5)
- [x] **Festival/birthday confetti micro-interaction** — pure game/confetti.ts deterministic ~2.6s pixel burst (staggered spawn, per-piece fall+sine drift, fade over final third) in the cozy HUD palette; engine diffs celebrationDayKey frame-to-frame to arm one burst on arrival at a festival/birthday dawn; skipped wholesale under reduce-motion. (cb6f1d5)
- [x] **Toast queue reduce-motion** — pure toastAlphaFor(entry,reduceMotion); under calm mode the pills hold full opacity their whole life then cut over TOAST_CUT_MS=80ms instead of the 600ms fade; engine swaps toastAlpha for toastAlphaFor threaded with settings.reduceMotion. The last animated HUD surface that ignored reduceMotion — calm-mode audit now complete. (7152d61)
- [x] **Chimney smoke (lived-in farmhouse)** — pure game/chimney-smoke.ts; responsive hearth: hearthLit() lights the wisp only in Winter / the cold dark hours (<8h or >=18h) / when weather waters crops; chimneySmoke particle column rises+drifts+fades; engine derives the roof apex from drawBuilding geometry; reduce-motion returns a frozen 3-puff thread so the home still reads as lit. (672da0d)
- [x] **Seasonal banner over Maple's shop** — pure game/shop-banner.ts owns the per-season ShopBannerStyle (palette + 5x5 motif bitmap + label, symmetric+tested); render/shop-banner-sprite.ts is the thin draw layer (striped cloth on a cross-beam); engine hangs it over the shop roofline keyed off time.season; out-of-range seasons wrap safely. (7ca5a24)

### Next ideas (FRONTEND-FIRST refill — Sanjay's override is active; pick the next 5 here)

Per the standing FRONTEND-FOCUS override, every roadmap item below is real UI/UX/visual/gameplay-UI work. Touch engine/sim backend only when a frontend feature truly needs it (keep it minimal + wired straight to the UI).

- [x] **Toast icons / kind tinting** — give the toast pills a left-rail accent tinted by category (gold for money, green for hearts, violet for achievements) so a busy stack is scannable by colour. Extend ToastQueue.push with an optional kind; pure render. (3c32330)
- [x] **Panel tab-bar unification** — shared pure game/panel-tabs.ts (tabStripLayout/cycleTabIndex/tabAtPoint) + thin ui/panel-tab-strip.ts widget; lore panel's 6 tabs now render through it pixel-for-pixel, and the bag panel reuses the same dialect. (cb6aed4)
- [x] **Inventory / bag panel** — `Tab` opens a categorized scrollable view of everything in player.inventory via pure game/bag.ts (7 tabs: Seeds/Crops/Fish/Gems/Forage/Kitchen/Supplies, human labels + per-unit gold value from the catalogs); ui/bag-panel.ts via the shared tab strip; world-blocking modal, gameplay-verb block suppressed so w/s scroll. (56713d3)
- [x] **Relationship panel polish** — the hearts panel (`H`) was a bare list; now a real relationships screen via pure relationshipRows(player,time): per-NPC heart pips, next-birthday countdown (daysUntilBirthday), a "loves ..." gift hint, and an engaged/married status chip, sorted by closeness. (58e4319)
- [x] **Tournament ribbon hall (farmhouse wall)** — bronze/silver/gold ribbons now mount as pixel rosettes on the farmhouse exterior wall via pure ribbonHallMounts() (ribbonCounts-driven, best-first, capped per tier, empty on a fresh farm) + render/ribbon-hall-sprite.ts. (d21a9f2)
- [x] **Bench tool rack (farmhouse decor)** — wall-mounted rack on the LEFT half of the farmhouse wall displaying the player's hoe/can/pickaxe/rod, each a distinct pixel silhouette tinted by its tier metal (wood..diamond). Pure toolRackMounts() reads the four tier helpers; empty until any tool passes wood. (37e3bde)
- [x] **Seasonal banner over Maple's shop** — a small sprite banner over the shop that swaps art per season, so the village visibly notices the calendar. Pure render keyed off time.season. (7ca5a24)
- [x] **Animated crop sway / wind** — a gentle 1-2px sinusoidal sway on ripe crop sprites (respecting reduce-motion) so the field feels alive. Pure render, reduce-motion-gated. (7fcc8f5)
- [x] **Empty/loading states pass** — shared game/panel-empty.ts (PANEL_EMPTY_STATES registry + loreEmptyState() resolver) + ui/empty-state.ts two-line widget; fixed the real bug where a filtered-empty lore Rumors tab drew NOTHING; money-log + quest-log retrofit off their hard-coded one-liners; bag panel uses it too. (622673b)
- [x] **Minimap legend → focused-landmark caption** — since there's no mouse, arrow/WASD cycles a "focused landmark" while the minimap is open, panning a caption that names the landmark + its tile, with a steady highlight ring on the pip. Pure cycleFocusIndex() + focusedLandmarkCaption() over minimapMarkers(). (6ffc050)
- [x] **Day-summary sleep overlay polish** — the sleep summary listed gold/harvest/hearts as plain rows; now pure game/day-summary.ts gives each row a category tint + small pixel glyph and derives a "best moment" highlight line that names the standout of the slept day. (ea77e44)
- [x] **HUD scale honoured by new widgets** — sky-dial / weather-strip / almanac-chip / minimap-button should respect getSettings().hudScale like the top bar + stamina bar already do, so accessibility scaling is consistent across the whole HUD. New pure game/hud-layout.ts owns the right-column geometry; at 1.0x it reproduces y=40/66/110 exactly. (236e457)
- [x] **Keybind reference search/filter** — the ? overlay is now an input-capturing modal: typing a letter/digit live-filters the cheat sheet (matching groups+rows stay lit, the rest dims, layout never shifts), green "filter: water_ (1/41)" chip, Backspace/Esc handling. Pure controls.ts bindingMatchesFilter/groupMatchesFilter/matchingGroupIndices/matchingBindingCount. (d5961ee)
- [x] **Festival/birthday confetti micro-interaction** — a brief, reduce-motion-gated pixel-confetti burst on the dawn of a festival or birthday so the special day lands with a beat. Pure overlay particle layer. (cb6f1d5)
- [x] **Onboarding first-run hint card** — a one-time dismissible card on a fresh save pointing at `?` for controls and `9`/`0` for the wayfinding panels. game/onboarding.ts holds content + a seen flag in its OWN localStorage key (survives a save reset); ui/onboarding-card.ts renders rounded key-caps; opens on first boot, dismisses on any key. (6afca1f)
- [x] **Heatmap toast / glance tie-in** — when the crop journal heatmap surfaces, a one-line summary chip ("3 crops dry, 1 about to gold-star") would let the player act without scanning every tile. Pure formatter over qualityHeat(). Shipped as a header line in the heatmap legend. (6510022)
- [x] **Minimap ping legend row** — surface the active minimapPings() reasons as a small legend line under the map so a colour-blind player can read WHY a tile is pinging, not just that it is. Pure UI over the pings array. (abf1091)
- [x] **Weather-strip + sky-dial reduce-motion audit** — the sky-dial cloud + minimap/ping pulses don't currently check reduceMotion; gate the animated cues (keep the static dim/cloud, drop the pulse) when the player has asked for calm. Reads getSettings().reduceMotion. Shipped for the minimap player-dot + ping rings AND the hotbar seed-warning breathe; sky-dial cloud has no animation so it was already calm. (c7800b0)
- [x] **Toast queue reduce-motion** — the toast pills currently fade on their TTL curve regardless of reduceMotion; under calm mode snap them in/out (or hold full-opacity then cut) instead of fading. Small extension to toastAlpha now that the reduceMotion plumbing reaches the HUD. (7152d61)
- [x] **Heatmap summary toast spill** — when the heatmap opens with >=1 dry crop, push the qualityHeatSummary line as a one-shot 'info' toast so the player sees it even without watching the legend corner. Pure heatmapToastSpill() (null on tended/empty field); engine fires it on the `;` closed->open transition. (04d1b97)
- [x] **Sleep-summary kind tints** — absorbed into the day-summary polish slice: each recap row now carries a category (gold/harvest/dishes/hearts) tinted to mirror the toast colour-rail palette, plus a pixel glyph. (ea77e44)
- [ ] **Stamina bar + quest panel reduce-motion** — audit the remaining HUD widgets for any pulse/animation that ignores reduceMotion now that the plumbing exists, so the whole HUD is consistent under calm mode. (Stamina bar low-warning, if any.)
- [ ] **HUD-scale audit for banners + toasts** — birthday/festival banners + the toast stack are still fixed-size; fold them into the hudScale plumbing now that game/hud-layout.ts exists, so EVERY HUD surface scales together. Watch the banner-vs-right-column collision noted in observation #4.
- [x] **Relationship panel gift-readiness chip** — the H panel now shows each candidate's "loves ..." hint; this follow-up checks the player's bag and surfaces a tiny "loved/liked gift" / "gift ready" chip when they're carrying something that NPC accepts (and hasn't been gifted today). Pure giftReadiness() in gifting.ts; threaded onto relationshipRows, tinted by taste. (9c645bd)
- [x] **Almanac row for the player's own milestones** — the `0` almanac now folds the player's own heart-4 NPC hangout dates in as a 'personal' kind (sorted first within a day-tie, rose rail + M tag, HUD chip promotion). Reuses the proven 28-day wrap. NOTE: the compost-finish / wedding-ready-day variant was deliberately SKIPPED — the time model wraps day 1-7 per season with no absolute-day counter, so a reliable "in N days" countdown for those isn't derivable; the (season,day) hangout invites are the clean source. (a4c5038)
- [x] **Day-summary streak line** — the morning recap now carries a one-line "lifetime haul to N crops" continuity note derived from the crop-journal totals, so each dawn ties to the last. Pure continuityLine(summary, lifetimeHarvest) over cropJournal totals; rides the day-summary model. (244b3a0)
- [x] **Ribbon hall hover/aria caption** — a caption in the achievements-panel footer naming the ribbon-hall contents ("3 ribbons on the wall: 1 gold, 2 silver") so the cosmetic is legible to a player who can't see the tiny rosettes. Pure ribbonHallCaption() over ribbonHallMounts(). (27072e1)

### FRONTEND-FIRST refill #2 (added tick #39 — the list above was down to 2 audits; pick the next 5 here)

Still under the standing FRONTEND-FOCUS override. All real UI/UX/visual/gameplay-UI work. The shared widgets shipped this tick (panel-tabs, empty-state) + the tabAtPoint pointer helper enable several of these cheaply.

- [ ] **Bag panel sort / sort-by toggle** — the bag lists each category by descending count; a one-key cycle (by count / by value / A-Z) would help a player hunting a specific item or their richest stack. Pure comparator set over buildBag(); ride a key while the bag is open.
- [ ] **Bag "sell-from-here" affordance hint** — the bag shows per-unit gold value but no action; a footer line ("sell crops at the well, dishes at the inn") or a per-category where-to-sell hint closes the loop between seeing worth and realizing it. Pure formatter, no economy change.
- [ ] **Recipe codex + achievements panels onto the shared tab strip** — both are flat scroll lists; a tab strip (codex: known/cooked/locked; achievements: earned/locked) would unify them with lore + bag and make big lists navigable. Reuses game/panel-tabs.ts — 3rd+4th consumers of the shared widget.
- [ ] **Pointer/hover tab clicks** — the tabAtPoint() helper already exists but no panel wires a mouse. Add canvas pointer handling so clicking a tab strip switches tabs (keyboard stays primary). Pure hit-test is done; needs an engine pointer listener + routing.
- [ ] **Bag panel item glyphs** — each bag row is text-only; a tiny per-category pixel glyph (the crop sprite for seeds/crops, a coin for value, a fish/gem silhouette) in the row pip would make the list scannable like the hotbar. Reuse drawCropSprite + the gem/fish sprite helpers.
- [ ] **Almanac "today" section header** — when events land today the almanac mixes them into the soonest-first list; a subtle "TODAY" divider above the day-0 rows (and "THIS WEEK" / "LATER") would group the agenda visually. Pure section-split over buildAlmanac() output.
- [ ] **Settings panel onto the shared chrome + empty-state audit** — the settings panel predates the panel family; align its row layout + add the shared empty-state vocabulary anywhere it can render nothing. Pure UI alignment pass.
- [ ] **Loading / first-frame state** — on boot before the first update tick the canvas can flash empty; a one-frame "Loading Sunsprout..." card (or a held onboarding) would smooth the cold open. Tiny pure overlay gated on a booted flag.
- [ ] **Bag total-worth breakdown by category** — the bag header shows total stacks + total worth; a per-tab worth sub-figure ("Gems: 412g") on each tab or in the header would let a player see where their money is. Pure aggregate over bagItemsForCategory().
- [ ] **Stamina bar + quest panel reduce-motion (carryover)** — audit the remaining HUD widgets for any pulse/animation that ignores reduceMotion now the plumbing exists. (From the prior list — still open.)
- [ ] **HUD-scale audit for banners + toasts (carryover)** — fold birthday/festival banners + the toast stack into the hudScale plumbing via game/hud-layout.ts. Watch the banner-vs-right-column collision. (From the prior list — still open.)
- [ ] **Panel open/close micro-transition** — panels snap in instantly; a 100-150ms ease (scale-from-95% or fade) on open, reduce-motion-gated, would make the whole panel family feel polished. Pure alpha/scale curve in the shared open() lockout timer that already exists on every panel.

## OPEN BLOCKERS

(none. Recurring observations refreshed after tick #39 (seventh FRONTEND-FOCUS tick, a CLEAN batch — not a resume):
 1. FRONTEND OVERRIDE STILL HOLDING — #33 wayfinding, #34 glanceability, #35 legible-&-accessible, #36 alive-but-calm, #37 cozy-meta-layer, #38 legible-state-&-self-knowledge, #39 consistent-shell are now SEVEN straight genuinely-frontend batches. #39's theme closed structural UI gaps rather than adding more surfaces: the missing inventory/bag screen (the biggest "what do I own" hole), a shared tab-strip + empty-state vocabulary that unifies the whole panel family, a type-to-filter on the controls overlay, and a personal-commitments row on the planner. The FRONTEND roadmap is now DOWN TO 2 stale audit items — REFILLED with 12 fresh frontend-first items below (sort/search in bag + panels, pointer/hover affordances the tabAtPoint helper enables, panel polish, more empty/loading states, micro-interactions). Keep picking from the FRONTEND-FIRST list; the override is still active.
 2. CLEAN-BATCH HYGIENE WORKED AGAIN — origin/main + local main both at #38 (30384c6), so a straightforward 5-fresh-slice batch, no resume reconciliation. The 5-then-gate-once rhythm held: typecheck after each slice + full vitest + build ONCE at the end. 2545/2545 green (+54 from baseline 2491). Build 324.46 kB / 99.30 kB gz, 176 modules (up from 170 — the 4 new pure modules + 2 new widgets).
 3. STALE-LSP-DIAGNOSTIC PATTERN, STILL NOT A REAL ERROR — the `patch` tool's post-edit LSP repeatedly flagged "cannot find name X" for symbols added in the SAME edit (tabStripLayout import, loreEmptyState/drawEmptyState imports, PANEL_EMPTY_STATES, the 4 controls filter fns, the `personal` key on every AlmanacKind Record literal). EVERY ONE was stale — `npx tsc --noEmit` came back exit 0 each time + the final full gate was clean. Lesson holds: trust tsc over the inline LSP echo when the symbol/key was just introduced; re-run tsc to confirm before chasing a phantom fix. Especially the "Property 'X' is missing in type Record<Union,...>" diagnostics after widening a union — those point at the OLD literal line and clear once tsc re-parses.
 4. INPUT-CAPTURING MODAL PATTERN — two slices this tick made a panel an input-capturing modal (bag panel suppresses the gameplay-verb block so w/s scroll without watering; help overlay swallows typed letters as a filter so "h"/"r" don't toggle other panels). The reusable shape: (a) add the panel to the movement `blocked` set, (b) for a panel that shares keys with world verbs, either add it to the `else if (!dialogue && !panel)` gameplay-actions guard OR handle it in an EARLY `if (panel.canAct()) {...; clearJustPressed(); return;}` block right after the onboarding card. The early-return form (help overlay) is the cleanest for a true text-input modal; the guard form (bag panel) is lighter when the panel only needs a/d/w/s. New keybind Tab needed preventDefault in input.ts so canvas focus doesn't escape.
 5. PURE-MODEL + THIN-WIDGET SPLIT HELD FOR ALL 5 SLICES — game/panel-tabs.ts + ui/panel-tab-strip.ts, game/bag.ts + ui/bag-panel.ts, controls.ts filter fns + help-overlay draw, game/panel-empty.ts + ui/empty-state.ts, almanac.ts personal block + panel/chip threading. 54 fresh tests, zero canvas stubs. The shared-widget pattern (one pure layout/registry module, one thin draw widget, multiple panel consumers) is now the dominant frontend shape — tab strip has 2 consumers (lore + bag), empty state has 3 (lore + money + quest, plus bag inline). When a 3rd tab-strip consumer appears, the cycleTabIndex/tabAtPoint helpers are already there.
 6. OPTIONAL-TRAILING-ARG BACK-COMPAT — the almanac slice widened buildAlmanac/almanacHighlight/AlmanacPanel.draw/drawAlmanacChip with an OPTIONAL trailing `player?` arg so all ~6 existing callers + the 21 existing almanac tests stayed untouched while new behaviour layered on. Same shape as last tick's hud-scale/reduce-motion threading. This is the safe way to feed a new dependency into a widely-called pure fn — append optional, default to the old behaviour, thread the real value only at the engine call site. Reach for it before considering a breaking signature change.
 7. ABSOLUTE-DAY MATH IS A REAL CONSTRAINT — the time model (game/time.ts) wraps `day` 1-7 per season and bumps `season` with NO monotonic absolute-day counter. Anything storing a future "finishOnDay" in that wrapped space (compost batches, engagement day) can NOT be turned into a reliable forward "in N days" countdown without ambiguity. The almanac-self-milestones idea was scoped DOWN to the (season,day)-carrying hangout invites for this reason. If a future feature genuinely needs absolute-day arithmetic, the clean fix is to add a monotonic `dayCount` to TimeOfDay (bump it in tick() alongside the wrapped day) + persist it — flagged but NOT urgent.)

## TICK LOG

(append-only; one line per tick)
- 2026-06-19 23:27 PT — 5/5 shipped: persistence (ad5fad6), sleep (965f0fc), weather (6213168), birthdays (75ba0ec), sprinklers (1c03c70). 517/517 tests green. Build 93.69 kB / 30.73 kB gz.
- 2026-06-20 03:40 PT — 5/5 shipped: forage (329d38e), coop (59d5ed7), egg-recipes (be0c711), farm-dog (827ae22), greenhouse (f41d16f). 582/582 tests green (+65). Build 106.41 kB / 34.54 kB gz. New keybinds: Y=forage, N=coop, I=add chicken, J=dog redeem/pet, U=greenhouse.
- 2026-06-20 07:24 PT — 5/5 shipped: crop-quality (dc6da66), tools (b100dd2), festivals (62eff02), mail (82d4375), chest (50443d9). 647/647 tests green (+65). Build 120.37 kB / 38.81 kB gz. New keybinds: ,=hoe upgrade, .=can upgrade, [=read letter, ]=open chest, X=place chest.
- 2026-06-20 15:35 PT — 5/5 shipped: codex (5a09261), journal (78ea1af), achievements (6707f0f), money-log (b712a59), settings (ba82aa4). 697/697 tests green (+50). Build 142.86 kB / 44.53 kB gz. New keybinds: R=recipe codex, ;=crop journal, V=achievements, Q=money log, \\=settings. (Note: tick was a resume — 4 commits had landed locally pre-resume from an earlier interrupted run; this tick finished the settings slice + gate + push.)
- 2026-06-20 18:08 PT — 5/5 shipped: winter (7589e6d), pickaxe (18f6028), quest-log (7d376af), hangouts (9b0e148), farm-cat (a57147b). 763/763 tests green (+66). Build 154.80 kB / 47.68 kB gz. New keybinds: /=pickaxe upgrade, '=quest log, -=adopt/pet cat. Also added Hot Cocoa to the cookbook; Recipe Collector achievement now references RECIPE_KEYS.length instead of a hardcoded 10.
- 2026-06-20 21:17 PT — 5/5 shipped: rod-upgrades (e36f20d), stamina (1897764), cart (991bbbc), auto-restock (c3331d4), lore (2d0a556). 835/835 tests green (+72). Build 171.78 kB / 52.10 kB gz. New keybinds: ==rod upgrade at Maple's, Z=sip best drink, `=lore panel. Cart parks at (16,9) on day 3 of every season 09-18h; E opens the menu. Auto-restock kit hooks into the dawn rollover and the plant verb; lore panel reads inventory live (no separate unlock state). Also extended fishing.ts Rod to accept per-cast bite-window + fish-picker overrides, kept fully backward-compatible.
- 2026-06-20 23:48 PT — 5/5 shipped: decor (78d5e68), spouse (8527af9), board (332b335), seed-extractor (f0fb391), tournament (487425a). 910/910 tests green (+75). Build 185.75 kB / 56.90 kB gz. New keybind: L=seed extractor. New world fixtures: notice board sprite at (19,11); decor palette retints farmhouse only; tournament uses well E-press during day-6 14-18h. Spouse overrides NPC schedule and replaces dialogue with a private greeting. Persistence wired for all five (decor/spouse/board/extractor/tournament). Open issue surfaced: SHOP_ITEMS exists but no shop UI -- buyable items only landable via dev console; recommend a shop modal as the next priority tick.
- 2026-06-21 03:12 PT — 5/5 shipped: shop (0fcb284), bench (70e3e88), scarecrow (48806fb), fancy-eggs (1c41787), owl-post (e93a9b5). 981/981 tests green (+71). Build 207.24 kB / 62.07 kB gz. WORKFLOW CHANGE: this tick commits directly to `main` (origin/main); previous ticks merged feature/autoship. New world fixtures: carpenter's bench at (22,9). New keybinds: { (scarecrow place), } (coop deluxe apply), ~ (owl post at farmhouse). Maple's shop modal closes the longstanding "buy from Maple" vaporware path -- all SHOP_ITEMS are now reachable in-game. Scarecrow boost runs INSIDE farming.harvest() so it stacks on the streak-derived quality without touching streak math. Fancy egg yield uses a deterministic per-(coop,day,chicken) hash so reload-scumming doesn't work; deluxe upgrade lifts the rate from 8% to 18%. Owl post fee (40g) only deducts on confirmed delivery so wasted presses are harmless.
- 2026-06-21 06:15 PT — 5/5 shipped: crop-ribbon (11136b6), animal-happiness (4e9da70), storm (278d3b3), night-fishing (ccf8dc1), stamina-teas (9807913). 1033/1033 tests green (+52). Build 211.38 kB / 63.67 kB gz. NO NEW KEYBINDS THIS TICK — every feature extended an existing flow. Crop ribbon shipped through the ; panel using the time arg now threaded into recordHarvest(). Animal happiness lives in a new pure module animal-happiness.ts (coop happiness + petTipBonus); coopTick now wraps the tier rate through coopFancyRate. Storm fires AFTER greenhouseTick so the greenhouse is the literal shelter. Night-fishing layer multiplies on top of rod-tier bias so gold rod + 2am cast is the compounded sweet spot. Three new stamina teas slot into both RECIPES + STAMINA_RESTORE without per-recipe wiring (drinkBest already iterates the table).
- 2026-06-21 09:16 PT — 5/5 shipped: bath-house (163549f), fish-pond (3aa9bb7), market-discount (b3e19eb), board-rep (8e956dc), npc-routes (f659bac). 1111/1111 tests green (+78). Build 218.94 kB / 66.23 kB gz. NEW KEYBIND: `>` (fish-pond stock/collect). New world fixtures: bath house at (30,7); the existing pond now interactable. SHOP API ADDITIONS: ShopRow gained `basePrice`+`isDeal`; ShopMenu.open() takes optional `time` for the daily deal banner — single legacy test updated. NPC SCHEDULE ADDITION: ScheduleSlot now supports optional `walkTo` + `periodHours`; getCurrentAnchor took an optional minute arg; every NPC has at least one walking slot. Bath house buff persists across reload via SaveSnapshot.player.bath; pond state persists via SaveSnapshot.world.pond. Board reputation is derived from existing BoardState.completedCount so no schema change. Market discount derives from the clock so no schema change either.
- 2026-06-21 12:26 PT — 5/5 shipped: hatchery (78b0cf6), compost (8ea90a6), npc-barks (f4e7957), bath-winter (17d6494), storm-shelter (2f29045). 1189/1189 tests green (+78). Build 232.09 kB / 70.12 kB gz. NEW KEYBINDS: `6` (hatchery context: place/load/claim), `7` (compost context: place/deposit/apply), `8` (storm shelter place). Three new bench recipes: Hatchery Basket (220g+2copper), Storm Shelter (400g+1iron). New Maple's shop kit: Compost Bin (140g). BATH OUTCOME SCHEMA: takeBath's `soaked` outcome gained `pricePaid` + `discounted`; tests/bath-house.test.ts inline fixture updated. STORM SHELTER WIRING: maybeFireStorm now skips sheltered crops + consumes their shelters; StormState.lastMemo + StormOutcome gained `cropsSheltered` + `consumedShelters`; stormFlavorLine reports them. NPC barks live entirely in their own module — no NPC schedule change, just a per-frame proximity check + a deterministic line picker. Fixed flaky weekday-market test (assumed wheat seed wouldn't be the featured row at (0,1) — compost addition shifted the rotation; the test now scans for any >=5g featured row). Hatchery / compost / shelter all persist through the SaveSnapshot.world list.
- 2026-06-21 16:01 PT — 5/5 shipped: shelter-pair (d4d6b9a), rare-fertilizer (4aac877), heritage-chicks (d1f9812), pond-rotation (04b4a6e), spa-pass (e311998). 1242/1242 tests green (+53 this batch). Build 235.40 kB / 71.32 kB gz. NO NEW KEYBINDS THIS TICK — every feature extended an existing flow. New constants: SHELTER_RADIUS_PAIRED + SHELTER_PAIR_RANGE; RARE_FERTILIZER_INVENTORY_KEY + RARE_FERTILIZER_STREAK + RARE_FINISH_DAY_MIN/MAX; HERITAGE_HATCH_RATE + HERITAGE_FANCY_BONUS; SPA_PASS_INVENTORY_KEY + SPA_PASS_PRICE + SPA_PASS_PUNCHES. SCHEMA ADDS: PlacedCoop.heritage?: boolean[]; PlacedHatchery.pendingHeritage / incubatingHeritage; SaveSnapshot.player.spaPass. NEW BATH OUTCOME FIELDS: paidWithPass + passesLeft (existing bath-house tests + bath-winter-discount tests both needed updating). compostTick gained optional `season` arg (default 0) for backwards compat — game.ts passes time.season; existing test fixtures pass season=1 so finishOnDay=3 stays NON-rare. Pip's cart catalog grew one row: Spa Pass at 700g. The rare-day picker (rareFinishDayFor) maps season -> day deterministically: spring=3, summer=6, fall=5, winter=2.
- 2026-06-21 19:10 PT — 5/5 shipped: rare-fert-hud (0b5bec1), barometer (d8f6836), cart-rumor (e267935), heritage-breeding (20085fd), spa-loyalty (87fa150). 1293/1293 tests green (+51 this batch + 1 fix to existing fancy-eggs assertion). Build 238.62 kB / 72.64 kB gz. NO NEW KEYBINDS THIS TICK. New constants: RARE day formatter helpers (rareDayCountdownLine); BAROMETER_INVENTORY_KEY + BAROMETER_PRICE (300g, singleton); BREEDER_EGG_INVENTORY_KEY + HERITAGE_BREEDER_RATE=0.4 + HERITAGE_BREEDER_MIN_HERITAGE=2; PERFUMED_SOAP_INVENTORY_KEY + SOAP_PER_SOAKS=10. SCHEMA ADDS: PlacedCoop.breederEggs?: number; BathState.totalSoaks + soapsGifted; collectEggsDetailed returns `{plain,fancy,breeder}`. SaveSnapshot.player.bath persisted both new fields. Pip's cart catalog grew one row: Brass Barometer at 300g. cart-rumor.ts decor-row filter prevents wallpaper from headlining two seasons in a row. drawWeatherStrip + cartMenu.draw + compostStatusLine all picked up new optional args without breaking existing callers.
- 2026-06-21 22:11 PT — 5/5 shipped: soap-gift (d2d3c82), breeder-tradein (e08d80e), rumor-rebate (1c0bfe8), barometer-warn (0d9ad5a), pond-rim (7da4405). 1333/1333 tests green (+40 this batch). Build 240.44 kB / 73.24 kB gz. NO NEW KEYBINDS THIS TICK — every feature extended an existing flow. New constants: PERFUMED_SOAP_GIFT_KEY (duplicated from bath-house key to avoid circular import); BREEDER_TRADEIN_MULTIPLIER=2 + BREEDER_TRADEIN_PRICE; RUMOR_REBATE_PCT=0.05; POND_RIM_INVENTORY_KEY + POND_RIM_PRICE=450 + POND_MAX_PENDING_RIM=10. NEW CART CATALOG ROW: Stone-Rim Pond Kit at 450g (singleton). NEW SIDE EFFECTS on cart E-press (game.ts): auto-trade breeder eggs BEFORE opening the menu; on cart purchase confirm: rumor-rebate logged separately from the buy. cart-menu.draw: new "TEASED -Ng" chip on headliner rows when `time` is passed (already wired). pondTick + pondStatusLine widened to take optional player; old callers still work. barometerStormWarning slotted into the existing dawn-toast ternary chain as a new terminal branch and into the board interaction toast. All five features wire through existing menus / toasts; no new keybind required.
- 2026-06-22 01:38 PT — 5/5 shipped: pond-ribbon (30d38a1), bench-shopping-list (32d1afb), spa-refill (c37849e), pet-treat (69026c8), shelter-trial (42902a2). 1385/1385 tests green (+52 this batch). Build 244.52 kB / 74.31 kB gz. NO NEW KEYBINDS THIS TICK — every feature extended an existing flow. New constants: SPA_PASS_REFILL_PRICE=500 + SPA_PASS_REFILL_KEY (synthetic cart key); PET_TREAT_KEYS / CAT_TREAT_KEYS as const tuples in cheapest-first order. SCHEMA ADDS: PondState.ribbons?: Partial<Record<FishKey, PondRibbon>>. pondTick widened to take an optional 4th `time` arg; old 2/3-arg callers leave the ribbon untouched. CART_CATALOG grew one row (spa-pass-refill); cart-rumor pool filter widened to exclude it AND decor- rows so Pip never teases a gated buy. CartBuyOutcome widened with 'refilled' + 'refill-not-eligible' branches. canRefillSpaPass() gate: no-bag-pass + zero-punches + totalSoaks>0. treatDog/treatCat consume the lowest-tier tea from the bag (herb-tea -> sunflower-elixir). seedTrialShelter() places a free Storm Shelter on the lowest-(ty,tx) outdoor crop the first time the save faces a storm; game.ts dawn wiring fires it only on the storm scheduled day so non-storm dawns don't waste the freebie.
- 2026-06-22 05:24 PT — 5/5 shipped: pond-overflow-warn (12e5dc9), bath-seasonal-towel (34d06cf), mining-haul (aab5349), tournament-nudge (78ad507), cart-rumor-history (96803cd). 1445/1445 tests green (+60 this batch). Build 248.70 kB / 75.86 kB gz. NO NEW KEYBINDS THIS TICK — every feature extended an existing flow. SCHEMA ADDS: BathState.seasonalSoaks?: Partial<Record<SeasonKey,number>> + BathState.seasonalTowelsGifted?: Partial<Record<SeasonKey,boolean>>; new module src/game/mining-haul.ts (MineHaulState{counts,lastRun}); new module src/game/cart-rumor.ts additions for RumorHistoryState{entries[]} (RUMOR_HISTORY_CAP=4). BathOutcome 'soaked' branch widened with seasonalSoaks/towelEarned/towelKey/towelLabel — four existing bathFlavorLine fixtures in bath-house / bath-loyalty / bath-winter-discount / spa-pass got the 4 new fields. fish-pond.ts gained pondWouldOverflowTomorrow + pondOverflowWarning pure helpers; tournament.ts gained personalBestFor + nextTierToClear + scoreTodayFor + tournamentNudgeLine; cart-rumor.ts gained getRumorHistory + recordRumorVisit + recordRumorBuy + rumorHistoryEntryLine + rumorHistoryLines + rumorHistorySummary. game.ts dawn toast tail chain extended with two new "* tail" suffixes (pondOverflow + haulRecap) layered on top of the regular headline; cart E-press path now stamps the rumor history on open; cart buy path stamps bought=true on the matching headliner buy. cart-menu footer surfaces the rumorHistorySummary beneath the existing next-visit hint. persistence.ts wires mineHaul + rumorHistory + the new BathState fields (seasonalSoaks + seasonalTowelsGifted). Mid-tick disk-scare: /tmp + ~/.npm + ~/.cache filled the disk to 100%, leaked a stray bash error stream into src/game/cart-rumor.ts which was caught + scrubbed; cleared ~2GB via npm cache clean --force + rm -rf on stale puppeteer caches.
- 2026-06-22 09:00 PT — 5/5 shipped: mine-haul-achievement (71b0f71), lore-rumors-tab (66fdd82), compost-recycle (68ff1f3), breeder-cookbook (0811dc9), double-batch (d64a422). 1512/1512 tests green (+67 this batch). Build 250.14 kB / 76.40 kB gz. NO NEW KEYBINDS THIS TICK — every feature extended an existing flow. SCHEMA ADDS: MineHaulState.lifetimeCounts?: Partial<Record<GemKey,number>> (lazy-backfilled, no top-level SaveSnapshot shape change). NEW ACHIEVEMENT: 'cave-veteran' (15 -> 16 badges in the catalog) keyed off lifetimeMiningMilestoneReached. NEW LORE CATEGORY: 'Rumors' added at the tail of LORE_CATEGORIES; loreCompletion explicitly filters Rumors out so the bestiary % isn't dragged around. APPLYOUTCOME WIDENED: ApplyOutcome 'applied' branch gains recycledGold (existing compost / compost-rare tests still pass — they destructure named fields). PARALLEL DISH KEY: `dish-<key>-premium` minted by cookPremium; sellAllDishes walks both regular and premium stacks. STAMINA-TEA SUBSET: STAMINA_TEA_KEYS const exposed alongside the existing STAMINA_RESTORE; a sync test asserts the two never drift. COOKING MODULE GROWING: cooking.ts at 590 lines — observation #5 flags the natural split point if a third variant lands. Pre-tick disk cleanup (npm cache + ~/.cache puppeteer) cleared 2GB; disk-scare did NOT recur this tick.
- 2026-06-22 12:36 PT — 5/5 shipped: premium-codex-section (3a394ea), lore-tab-footer (9e9b389), double-batch-inn-toggle (d97114d), compost-lifetime-ledger (85ccbad), rumors-panel-filter (af25717). 1567/1567 tests green (+55 this batch). Build 255.20 kB / 77.86 kB gz. TWO NEW KEYBINDS THIS TICK (both panel-local): `Shift+Enter` in the cooking menu (single->double-batch path on stamina teas) and `f` in the lore panel on the Rumors tab (cycle all/bought/skipped filter). Global fishing `f` guarded with `!this.lorePanel.isVisible()`. SCHEMA ADDS: Player.premiumCookCounts?: Record<string, number> + Player.compostLedger?: CompostLedgerState (lifetimeRecycledGold + lifetimeBagsApplied) — both lazy-readers, both persisted, both backfill on older saves. NEW ACHIEVEMENT: 'compost-master' (16 -> 17 badges) at COMPOST_MASTER_MILESTONE_GOLD=100. RECIPECODEXROW WIDENED: hasPremium / premiumLine / premiumSellPrice / premiumCookedCount / premiumOwned (the panel's row-height now varies per row — ROW_H=28 for plain rows, ROW_H_PREMIUM=40 for unlocked egg recipes). COOKOUTCOME WIDENED: 'cooked' / 'missing' branches gained mode + yield; new 'not-eligible-double' branch when the player Shift+Enters on a non-tea. recordCook bumps `outcome.yield` times so achievement counts mirror the bowl. New pure formatters: loreTabFooter() (Gems-tab career line), compostLedgerLine() (crop-journal footer line), rumorFilterLabel() (lore-panel filter chip). cooking.ts unchanged this tick (570 lines from last; observation #5 retired — the variants are sitting fine in cooking.ts for now). Disk-scare did NOT recur.
- 2026-06-22 15:52 PT — 5/5 shipped: breeders-bowl (f6db347), cart-rumor-streak (20832ba), mining-milestone-toast (4f50fb8), cart-tea-tradein (69d41cc), owl-post-stamps (de5ead6). 1636/1636 tests green (+69 this batch). Build 257.68 kB / 78.69 kB gz. ZERO NEW KEYBINDS THIS TICK — every feature extended an existing flow (achievements catalog auto-check; cart buy hook; mining strike branch; cart open path; lore panel build). SCHEMA ADDS: Player.owlStamps?: OwlStampBook (counts: Record<npcId, number>) lazy-reader + persistence whitelist. NEW CONSTANTS: BREEDERS_BOWL_MILESTONE=25 (cooking-history.ts); RUMOR_STREAK_MIN=3, RUMOR_STREAK_DISCOUNT_STEP=5, RUMOR_STREAK_MAX_STEPS=2 (cart-rumor.ts — MAX_STEPS calibrated against RUMOR_HISTORY_CAP=4 so the cap lands at the history limit); MINING_RUN_MILESTONES=[3,6,10] as const (mining-haul.ts); TEA_TRADEIN_KEYS=['herb-tea','berry-tonic'] + TEA_TRADEIN_COST=3 + TEA_TRADEIN_REWARD_KEY='hot-cocoa' (cart.ts). NEW ACHIEVEMENT: 'breeders-bowl' (17 -> 18 badges) keyed off totalPremiumDishesCooked >= 25. GAME.TS WIRING: cart buy hook reads streak BEFORE recordRumorBuy (so refund reflects PRIOR run); mining strike branch reads haulCount before+after recordMined; cart E-press path runs tradeStaminaTeas right after tradeBreederEggs. Cart-menu footer gained rumorStreakLine chip above the existing summary. Lore Folk tab description gains " Owl posts: N." tail when nonzero. PERSISTENCE: owlStamps round-trips through serializeGame + applySnapshot with older-save backfill via the lazy reader on first call. CART-OPEN AUTO-TRADE STACK IS NOW THREE-DEEP (tradeBreederEggs -> tradeStaminaTeas -> recordRumorVisit). The mining milestone callout is NOT a dawn-toast tail — it lives on the strike toast as a "  -  <line>" suffix appended to the existing grade+gold message. dispatchOwl now recordOwlStamps inside the `sent` branch so failed dispatches never stamp. Disk-scare did NOT recur.
- 2026-06-22 19:15 PT — 5/5 shipped: owl-fluent-achievement (1ad5725), pulper-achievement (669215a), mining-gold-milestone (9152855), stamina-tea-tradein-hud (73e33ee), inn-forage-tradein (1098227). 1699/1699 tests green (+63 this batch). Build 259.65 kB / 79.37 kB gz. ZERO NEW KEYBINDS THIS TICK — fifth tick in a row without one. Two new lifetime-ledger achievements landed via the now-factory pattern: owl-fluent reads totalOwlStamps off the OwlStampBook (constant OWL_FLUENT_MILESTONE=25, one-line predicate), pulper reads compostLedger.lifetimeBagsApplied (constant PULPER_MILESTONE_BAGS=500, one-line predicate). Catalog grew from 18 to 20 badges. Breeders-bowl + owl-fluent catalog-size tests relaxed from strict equality to ">=" to defend against in-tick growth. NEW MODULE: src/game/inn-trade.ts (~140 LOC; tradeForageForTea + innForageTradeInBalance + canTradeForageForTea + innForageTradeToastLine + innForageTradeInLine HUD chip). Engine wiring: cooking-menu open path (C-press near inn) auto-fires tradeForageForTea BEFORE opening the menu, mirroring the cart-open auto-trade stack. Cooking-menu UI gained a footer chip at y-38 above the keybind hint. NEW MINING CONSTANTS: MINING_RUN_GOLD_MILESTONES=[100,250,500] (mining-haul.ts) with parallel goldMilestoneToastLine labels "Pockets clinking" / "Now we're cooking" / "Cart's full" — deliberately distinct from the count tiers so when both fire on the same strike the player sees two different celebrations. Engine reads haulGold pre+post recordMined alongside haulCount; both tails compose onto the setToast string (count first, gold second). NEW CART HELPER: staminaTeaTradeInLine (cart.ts) three-bucket formatter (empty / "need N more" / "trade ready") drawn into cart-menu footer at y-78 above the rumor-streak chip. SCHEMA ADDS: none — all five features ride existing ledgers or new pure helpers. TRADE-IN TRIO COMPLETE: cart-side breeder eggs (gold), cart-side teas (Hot Cocoa), inn-side forage (Sage Tea) all auto-fire on their respective menu opens. Long pipeline now exists: forage -> Sage Tea -> Hot Cocoa. Cooking.ts at 556 lines unchanged. Disk-scare did NOT recur.

- 2026-06-22 22:37 PT — 5/5 shipped: compost-halfway-nudges (0ed5e51), cart-tea-tradein-inn-nudge (938012f), owl-fluency-tiers (2f6d1ba), mining-haul-ribbon (3657aed), owl-post-fee-discount (f89de0b). 1773/1773 tests green (+74 this batch). Build 261.48 kB / 80.02 kB gz. ZERO NEW KEYBINDS THIS TICK — sixth tick in a row. Compost-halfway-nudges = mutually-exclusive "(Xg to the badge)" + "(N bags to the pulper badge)" tails inside compostLedgerLine; constants COMPOST_MASTER_NUDGE_MIN_GOLD=50 + PULPER_NUDGE_MIN_BAGS=100 deliberately line up so the pulper nudge starts the moment compost-master nudge ends — only ONE tail surfaces at a time. Cart-tea-tradein-inn-nudge = staminaTeaTradeInLine appends " - or trade N forage at the inn for Sage Tea" in the under-the-threshold + empty branches; suppressed at trade-ready + when forage<COST; closes the loop on trade-in trio HUD cross-references. Owl-fluency-tiers = new const OWL_FLUENCY_TIERS=[{min:5,'occasional pen pal'},{min:15,'regular pen pal'},{min:25,'favorite courier'}]; top tier matches OWL_FLUENT_MILESTONE so a global fluent run lights up at least one favorite tag; owlStampLine composes the tier label onto the count; lore.ts goes through owlStampLine() (replaces inline literal); 1-4 plain-count noise floor preserved. Mining-haul-ribbon = MineHaulState.bestRun{count,countDay,gold,goldDay}; resetMineHaul(player, day=0) optional arg; count + gold promoted INDEPENDENTLY (a pure-iron grind keeps only the gold record); haulYesterdayLine appends three "best run ever..." suffix variants (count+gold / for count / for gold) the morning a record fell; haulBestRunLine passive formatter; loreTabFooter Gems composition gains " - best run: ..." tail; persistence wires bestRun with lazy-undefined backfill; engine sleep passes this.time.day. Owl-post-fee-discount = OWL_POST_TIER_DISCOUNT_PCT keyed off the fluency tier label (10/20/30%); favorite-courier saves 12g/send (28g vs 40g); dispatchOwl reads owlPostFeeFor(player,npcId) ONCE for ... [truncated]
- 2026-06-23 02:10 PT — 5/5 shipped: deep-vein-achievement (9e0d6ad), mining-haul-ribbon-nag (c5257ec), compost-halfway-dawn-nudges (09585b8), owl-menu-fee-chip (e08ae4c), tournament-career-recap (f097a53). 1842/1842 tests green (+69 this batch). Build 263.86 kB / 80.84 kB gz. ZERO NEW KEYBINDS THIS TICK — seventh tick in a row. Deep-vein = DEEP_VEIN_COUNT=20 OR DEEP_VEIN_GOLD=1000 single-run thresholds; 21st badge in catalog via existing lazy-ledger pattern; reads bestRun off MineHaulState; both axes tested independently AND the split-record path (different days for count vs gold leaders). Mining-haul-ribbon-nag = RECORD_GAP_NAG_MIN_PCT=50 floor; recordGapNagLine() appended to haulYesterdayLine as "- N% of your best run" tail on close non-record runs; cap at 99% keeps brag tail + nag tail mutually exclusive; picks max(countPct,goldPct) so a near-gold run still surfaces the encouragement; silent on fresh saves / empty runs / record-breaking runs / below-floor runs. Compost-halfway-dawn-nudges = compostHalfwayDawnNudge() one-shot helper with two sticky boolean flags on CompostLedgerState (masterNudgeDawnFired + pulperNudgeDawnFired); wired into engine dawn-toast chain as a fourth tail after pondOverflow + haulRecap; survives reload via persistence; compost-master priority over pulper when both eligible. Owl-menu-fee-chip = owlPostFeeChip() pure helper; per-NPC tier-discount price chip drawn on each owl-menu row in GOLD when discounted / HINT-dim at full price; short tier label ("occasional"/"regular"/"favorite") keeps the chip <30 chars; closes UX gap where tier discount was invisible at decision time. Tournament-career-recap = TournamentCareer aggregate + tournamentCareer walker + tournamentCareerLine four-wording renderer + tournamentNudgeWithCareer composite; engine swaps tournamentNudgeLine for the with-career variant; recap parses entries-map key shape `${season}-${kind}` to recover bestKind without a new field; first-time prompt, singular/plural entry phrasing,... [truncated]
- 2026-06-23 05:42 PT — 5/5 shipped: dawn-toast-composer (8921f72), deep-vein-dawn-brag (7f80a8f), festival-regular-achievement (b8e5700), mine-haul-ribbon-journal-page (edcb4c9), owl-fluency-tier-color (353abf9). 1912/1912 tests green (+70 this batch). Build 266.48 kB / 81.50 kB gz. ZERO NEW KEYBINDS THIS TICK — eighth tick in a row. Dawn-toast-composer = assembleDawnToast(headline, tails[]) pure helper retires the engine's growing string-concat ladder; observation #2 from the last two ticks RETIRED — adding a fifth tail is now a one-line array push instead of a fifth ternary. Deep-vein-dawn-brag = one-shot celebratory dawn-toast tail naming gems / gold of the record run the morning AFTER bestRun crossed either deep-vein threshold for the first time; two sticky flags on MineHaulState (deepVeinBragPending armed inside resetMineHaul on fresh crossing, deepVeinBragFired set inside composer on first read); three wordings depending on which axis tripped (count-only / gold-only / both at once); both flags round-trip through persistence; landed as the FOURTH tail on the new assembleDawnToast composition — validating the refactor on the same tick that introduced it. Festival-regular-achievement = 22nd badge keyed off tournamentCareer().entries == 4, the natural ceiling of the (season,kind) entries map; lazy-ledger pattern, no new persisted counter; tuned at 4 because enterTournament locks out re-entry in the same slot so a higher number would be unreachable; predicate orthogonal to tier (no-prize entries count). Mine-haul-ribbon-journal-page = bestRun gained countComposition + goldComposition Partial<Record<GemKey,number>> fields captured at sleep time; bestRunCompositionLine ("made of: 4 copper, 3 ruby") + bestRunSplitCompositionLine ("count run: 8 copper, 4 iron - gold run: 1 ruby, 1 emerald") + loreTabDetailLine helpers; lore Gems-tab footer grew a secondary detail line below career + ribbon header; panel height grows by 14px when the detail line is non-empty so non-Gem... [truncated]
- 2026-06-23 10:26 PT — 5/5 shipped: owl-post-letter-chain (34d15dd), lore-folk-chain-indicator (2634aca), owl-menu-tier-color-chip (e982c25), tournament-champion-achievement (c052c32), compost-ladder-nudge (6b5bc05). 2004/2004 tests green (+92 this batch). Build 268.33 kB / 82.13 kB gz. ZERO NEW KEYBINDS THIS TICK — ninth tick in a row. Owl-post-letter-chain = OWL_CHAIN_TIERS=[{1,1.0},{2,1.1},{4,1.2},{7,1.3}] consecutive-day same-recipient bonus; new OwlStampBook.chain {npcId,length,lastDay} sticky field; chainBonusMultiplier + getOwlChain + previewChainLength + activeChainLength + recordOwlChain helpers; attemptAutoGift gained optional extraMultiplier arg (defaults 1.0, backward compatible) so the chain bonus composes ON TOP of the existing birthday 8x multiplier (chain-7 + birthday = 8 * 1.3 = 10.4x compounded); dispatchOwl snapshots pending chain length BEFORE attempting gift then bumps AFTER landing so failed sends (no-items / already-today / not-enough-gold) never advance the streak; OwlPostOutcome 'sent' branch widened with chainLength + chainMultiplier; chain breaks on day-skip (chain.lastDay + 1 != day) AND on recipient-swap (chain.npcId != npcId); engine setToast appends " Letter chain xN (+M%)." tail when chainMultiplier > 1; suppressed on length-1 (fresh start = no signal); chain.{npcId,length,lastDay} round-trips through persistence with deep-copy on serialize + apply; older saves predating the chain field lazy-backfill an empty "no chain" object on first getOwlChain read. ENGINE BUG FIX: owl-post money log was hardcoded to -40g regardless of the tier discount; now reads owlPostFeeFor(player, npcId) so favorite-courier sends log the actual 28g paid instead of over-reporting. Lore-folk-chain-indicator = "Chain: N days." tail on lore Folk row description when activeChainLength(player, npcId) >= 2; surfaces only on the ACTIVE chain target (per-NPC isolation); swap or skip drops the tail the same dawn the chain breaks; appears AFTER existing owl-posts tail in the description for tidy progression reading. Owl-menu-tier-color-chip = bronze/silver/gold chip drawn just LEFT of NPC name on each owl-menu row mirroring the lore Folk row chip; shares OWL_FLUENCY_TIER_COLOR palette (no duplicate); ALSO adds pending-chain preview "hearts: N  ·  chain: M" when previewChainLength >= 2 so the player can SEE the bonus before pressing Enter (without it the chain multiplier was invisible until the post-dispatch toast); OwlMenu.draw() gained optional day parameter (backward compat — chain preview suppressed when omitted). Closes the loop on the chip pattern — observation #5 RETIRED, owl-post surface now spans FIVE layers (text + price chip + Folk color chip + menu color chip + chain preview). Tournament-champion-achievement = 23rd badge keyed off tournamentCareer().ribbons.gold >= 3; ORTHOGONAL to festival-regular (Festival Regular counts entries regardless of tier, Champion requires actual gold-tier wins); 3/4 sweet spot — tournament entries are slot-capped at 4 (one per season-kind) so ribbons.gold caps at 4 too, making 3 a meaningful but achievable bar; lazy-ledger pattern reads off the existing entries map, NO new persisted field. Catalog now >= 23 badges. Compost-ladder-nudge refactor = generic ladderNudge(rungs[]) pure helper extracted from compostLedgerLine + compostHalfwayDawnNudge; same get-ahead refactor the dawn-toast composer was last tick — the compost ladder was at 2 rungs in 2 surfaces (4 ternary branches) and would have hit 3-rungs in 2 surfaces (6 branches) before any new compost-tier nudge ships; new LadderNudgeRung interface; compostLedgerLine builds the rung set + calls ladderNudge; compostHalfwayDawnNudge inlines the walker so it can fire one-shot side effects per rung; runtime byte-output unchanged across all 97 existing compost tests; future rungs (compost-master sash, pulper rare-bag tier) drop in as one-line array pushes. KEY PATTERN OBSERVATIONS THIS TICK: (a) lazy-ledger + sticky-flag mechanics now extended from MineHaulState.bestRun ribbons to OwlStampBook.chain — same shape, different domain (b) refactor-ahead-of-pressure pattern proven a second time (dawn-toast last tick, ladder-nudge this tick) (c) closing-the-loop pattern on the owl-post chip surface now reads as a coherent five-layer visual language across two separate UI surfaces.
- 2026-06-23 14:10 PT — 5/5 shipped: owl-chain-tier-dawn-brag (2b7d43e), owl-menu-chain-bonus-chip (1b498f2), compost-master-sash-achievement (0ea7e14), rare-master-achievement (e9fde58), owl-menu-chain-target-sort (739dd4b). 2080/2080 tests green (+76 this batch). Build 270.45 kB / 82.76 kB gz. ZERO NEW KEYBINDS THIS TICK — tenth tick in a row. Owl-chain-tier-dawn-brag = OWL_CHAIN_TIER_LABEL {'1.1':'starting','1.2':'regular','1.3':'favorite'}; chainTierDawnBrag(player) reads + clears OwlStampBook.chainTierBragPending; recordOwlChain arms the flag inside maybeArmChainTierBrag() (private) on a length-BUMP crossing into a higher chainBonusMultiplier tier (compares multipliers not raw lengths so OWL_CHAIN_TIERS stays single source of truth); player-level celebration so a chain-reset between crossing day and next dawn STILL fires the brag — celebrates the crossing that HAPPENED not the live state; persistence round-trips chainTierBragPending via optional-undefined sentinel (older saves backfill cleanly); landed as the FIFTH tail on assembleDawnToast composition validating tick #27's composer refactor at 5 tails (pondOverflow + haulRecap + compostNudge + deepVeinBrag + chainTierBrag). Owl-menu-chain-bonus-chip = chainBonusChip(player, npcId, day) pure helper returns "+N% hearts" when previewed chain length crosses a OWL_CHAIN_TIERS bonus tier (>= 1.1x); owl-menu draws it in BONUS_GREEN sage (#8FCC6A) at left of row after hearts label using ctx.measureText positioning (silenced one pre-existing test-stub TypeError that Feature 2 tripped — stub now returns TextMetrics-shaped fake); mirrors cart-rumor savings chip visual language; payout-first wording ("what do I GET") at decision time without translating chain length to multiplier; closes owl-post chip pattern at six layers — observation #5 from tick #24 fully retired. Compost-master-sash-achievement = 24th badge at lifetimeRecycledGold >= 250 (sits between compost-master 100g and pulper 500 bags); reads same lifetimeRecycledGold field as compost-master so no new persisted counter; VALIDATES ladderNudge refactor on first ship — new rung lands as single array push into compostLedgerLine rather than another pair of ternary branches across two surfaces; sash priority sits between master + pulper in the ladder; existing compost-master-halfway-nudge tests updated to push past the 250g gate (84 rare bags = 252g) when asserting pulper-tail. Rare-master-achievement = 25th badge tracks lifetimeRareBagsApplied SEPARATELY at >= 100 — distinct from pulper which counts regular+rare combined; CompostLedgerState gains lifetimeRareBagsApplied?: number with lazy-backfill 0 for older saves; recordApplied gains optional rare arg defaulting false (backwards compat); applyFertilizer threads its existing rare boolean through; rare bags gated by per-season rare-day window so 100 applied = 15-25 in-game seasons of optimal rare-day timing; regular-only 500-bag spam does NOT earn the badge (predicate cleanly distinguishes). Owl-menu-chain-target-sort = owlCandidateIdsForMenu(player) NEW pure helper hoists active chain target (OwlStampBook.chain.npcId) to position 0 while preserving alphabetical order for non-target rows; OwlMenu.open(player) NEW optional arg snapshots the sorted list into private displayIds so mid-session dispatch doesn't reorder rows under selection; new public candidateIds() method returns live displayIds with alphabetical fallback pre-open; selectedId / selectPrev / selectNext / draw all swap their owlCandidateIds() call for this.candidateIds(); engine game.ts owlMenu.open() call site now passes this.world.player; hoist applies EVEN at chain length 1 (no bonus yet) because length 1 still represents player's most recent activity worth surfacing. Sticky-flag one-shot brag pattern now matured to 3 implementations (deep-vein + chain-tier + 2 roadmap items pending) — flagged a generic `oneShotBrag` extraction as a roadmap refactor pressure item but not extracted yet since we only have 2 implementations in tree (will revisit at 3rd).
- 2026-06-23 18:20 PT — 5/5 shipped: oneShotBrag refactor (4fdd583), compost-master-sash-dawn-brag (9cefb89), rare-master-dawn-brag (4ff8eac), rare-master-journal-nudge (5ff8aa1), owl-menu-chain-target-halo (f05132b). 2146/2146 tests green (+66 this batch). Build 272.35 kB / 83.22 kB gz. ZERO NEW KEYBINDS THIS TICK — eleventh tick in a row. OneShotBrag = generic helper oneShotBrag(carrier, pendingKey, firedKey, render) in dawn-toast.ts; observation #4 from tick #30 RETIRED — the sticky-flag dawn-brag pattern is extracted into a pure module that knows nothing about specific carriers (MineHaulState / OwlStampBook / CompostLedgerState); refactored deepVeinDawnBrag + chainTierDawnBrag onto the helper as zero-behavior-change validations (the only externally visible difference is pending field clears to undefined now instead of false; persistence was already using === true coercion so on-disk shape and predicates unaffected); one existing test assertion (strict-equal-false on deepVeinBragPending) relaxed to toBeFalsy() to match the new contract; pendingKey + firedKey typed as keyof T so callers can't typo a key; render callback receives the actual pending value so numeric-pending arms (chain tier multiplier 1.1/1.2/1.3 as the value) AND boolean arms (deep-vein true/false) both work through the same shape; 16 new tests for the helper itself cover every silence + fires + sticky semantics + re-arming + cross-carrier independence. Compost-master-sash-dawn-brag = one-shot tail when lifetimeRecycledGold crosses 250g for the first time; new sashBragPending + sashBragFired sticky flags on CompostLedgerState; recordApplied snapshots pre-bump gold and arms sashBragPending ONLY on fresh crossing so a save reloaded past the bar doesn't re-trigger; runs through oneShotBrag helper validating the refactor on the same tick that introduced it; persistence round-trips both flags via optional-undefined sentinel; landed as 6th tail on assembleDawnToast composition (pondOverflow + haulRecap + compostNudge + deepVeinBrag + chainTierBrag + sashBrag); wording "Compost Sash earned - Xg recycled across N bags."; rare-master scaffolding shipped same commit because the recordApplied snapshot path naturally covers both crossings in one function — splitting it would leave a broken intermediate. Rare-master-dawn-brag = symmetric one-shot tail when lifetimeRareBagsApplied crosses 100; reads SEPARATE rare-bag counter so a regular-bag grind never accidentally fires it; chained test verifies sash + rare-master independence — piling 100 rare bags clears BOTH milestones simultaneously (84 rare = 252g past sash; 100 rare past rare-master) and firing one brag doesn't clear the other; landed as 7th tail; 12 new tests. Rare-master-journal-nudge = 4th rung in compostLedgerLine ladderNudge + 3rd rung in compostHalfwayDawnNudge for [50, 100) rare bags; new RARE_MASTER_NUDGE_MIN_BAGS=50 + rareMasterNudgeDawnFired sticky flag on CompostLedgerState; reads SEPARATE lifetimeRareBagsApplied counter so the rung is orthogonal to gold/total-bags; priority order master/sash/rare-master/pulper — rare-master sits BEFORE pulper because rare bags are harder to grind; wording "(N rare bags to the rare-master badge)" passive + "Halfway to Rare Master - N rare bags to go." dawn one-shot; ONE EXISTING TEST UPDATED (compost-master-halfway-nudge.test.ts pulper paths) — fixtures that used 84 rare bags now also light up the rare-master nudge; switched to 250 regular bags (0 rare) so pulper rung surfaces alone; added .not.toContain('rare-master') assertions to defend against rung leaking in; validates ladderNudge refactor a third time — sash rung tick #29, rare-master rung now — the helper keeps scaling as a single array push; 16 new tests cover constant + boundaries + singular/plural + priority + orthogonality + dawn-nudge sticky flag. Owl-menu-chain-target-halo = subtle 2px sage-green outline rgba(143, 204, 106, 0.6) on the active-chain-target row drawn in ADDITION to the row-position hoist; halo color matches BONUS_GREEN chip so the two visual cues read as the same "active streak" language; when chain-target row is ALSO selected the halo OVERRIDES the BORDER outline for one strong sage indicator rather than two stacked-overwriting strokes; length-1 chains light up the halo because it's about "you're maintaining this friendship" not the bonus level; new pure helper isActiveChainTarget(player, npcId) in owl-post.ts; 11 new tests cover pure helper across chain states + per-NPC isolation + length-1/length-7 chains + recipient swap moves halo + day skip preserves halo + pure-read semantics + real-dispatch integration + menu draw integration via vi.fn() stub CanvasRenderingContext2D that captures strokeStyle at each strokeRect call to verify the chain-target row uses the halo color. Observation #2 from STATE.md flagged the composer is at 7 tails now — tail grouping refactor lands on the roadmap before the 8th tail arrives.
- 2026-06-23 22:30 PT — 5/5 shipped: dawn-toast-tail-grouping (57909ea), owl-chain-recipient-brag (29fd938), mine-split-record-brag (d6c367e), owl-menu-chain-countdown-chip (f3d95be), vein-connoisseur-achievement (9f4159a). 2217/2217 tests green (+71 this batch). Build 274.84 kB / 83.86 kB gz. ZERO NEW KEYBINDS THIS TICK — TWELFTH tick in a row. Dawn-toast-tail-grouping = assembleDawnToast accepts EITHER ReadonlyArray<string | null | undefined> (legacy flat array) OR DawnToastTailGroups {system?, achievement?} object; system tails join with " · ", achievement tails join with each other using " • " inside one " · "-attached cluster; observation #2 from ticks #30/#31 RETIRED before the 8th tail landed. Owl-chain-recipient-brag = OWL_CHAIN_RECIPIENT_BRAG_LENGTH=25 consecutive days WITH ONE RECIPIENT; maybeArmChainRecipientBrag in recordOwlChain alongside the tier brag; per-recipient sticky map chainRecipientFired so a second 25-day chain to the same NPC stays quiet but a DIFFERENT recipient earns its own brag; pending field carries the npcId so a chain reset between crossing day and next dawn still surfaces the right name; 3 new owlStamps persistence fields; lands as the 8th tail validating the grouping refactor on the same tick. BONUS FIX: chainTierDawnBrag was passing `null` for its firedKey — now passes the canonical chainTierBragFired so the audit flag flows through persistence properly. Mine-split-record-brag = wasSplit pre-update + nowSplit post-update inside resetMineHaul arms splitRecordBragPending only on the FIRST same-day-to-split transition; both records must be non-zero for a state to count as split; splitRecordBragFired audit gates re-firing so merge-back-then-split-again does NOT re-fire; saves already split before the flag existed do NOT spuriously arm because wasSplit is read BEFORE the new resetMineHaul updates run. Owl-menu-chain-countdown-chip = activeChainCountdownChip pure helper returns "Nd streak" when npcId is the active chain target AND day <= chain.lastDay + 1; length-1 streaks DO get the chip filling the "streak exists but no bonus yet" gap; chain-break detection mirrors recordOwlChain semantics; drawn in BONUS_GREEN so halo + bonus chip + streak chip all read as one "active streak" language; positioned via ctx.measureText so chips do not overlap. Vein-connoisseur-achievement = VEIN_CONNOISSEUR_PER_GEM=10 lifetime of EVERY gem type; veinConnoisseurMilestoneReached walks GEM_KEYS and returns false the moment any gem sits below the floor; reads existing lifetimeCounts (lazy-ledger pattern); orthogonal to cave-veteran (lifetime-100-total); 26th badge; 5*10=50 total stays comfortably below the 100-gem cave-veteran bar so both badges track in parallel.
- 2026-06-24 22:06 PT — 5/5 shipped (FRONTEND-FOCUS, breaking a 12-tick backend drift): toast-queue (ade101c), help-overlay (9482b1d), sky-dial (eea66be), minimap (a44b2e6), almanac (9ac8252). 2286/2286 tests green (+69 this batch). Build 290.15 kB / 88.36 kB gz. TWO NEW KEYBINDS: 9 (minimap), 0 (almanac); plus ? (help overlay) — first new keybinds since tick #20. Theme = wayfinding/orientation, the frontend layer's biggest real gaps. Toast-queue = pure ToastQueue FIFO replaces the single toast string + fade timer that clobbered all but the last writer on same-frame messages; each entry lives its own TTL, renderer draws up to 3 newest-first (freshest pill bright-bordered, older ones dim+fade independently via toastAlpha), duplicate-text repeats refresh the newest age instead of walling identical pills; legacy this.toast/this.toastFade fields removed; 17 tests. Help-overlay = controls.ts single source of truth for ~40 keybinds grouped into 7 categories, splitControlColumns balances two columns, HelpOverlay ? renders a centered card with rounded key-caps; test guards no-emoji + that ?/9/0 + every panel toggle stay catalogued; 13 tests. Sky-dial = sky-dial.ts maps time onto sun-by-day/moon-by-night on a horizon-to-horizon arc (arcT 0..1, sine-hump altitude, night arc wraps midnight monotonically), widget draws dotted arc + pixel sun/crescent + phase caption + Nh-Nm-left, right-aligned below the weather strip; 15 tests. Minimap = minimap.ts projects the 40x30 world into a colour grid + landmark markers from world.buildings + cave, MinimapPanel 9 blits grid + glyph pips + pulsing player dot + legend, projectTile keeps everything pixel-aligned; 10 tests assert pond/tilled/building-centre placements vs real worldgen. Almanac = almanac.ts aggregates festivals + birthdays + cart + tournament within a 14-day horizon into one soonest-first list reusing the 28-day calendar wrap, AlmanacPanel 0 renders colour-railed rows with countdown + date stamp + calm empty state; 14 tests. ...
- 2026-06-25 04:40 PT — 5/5 shipped (FRONTEND-FOCUS, 2nd straight): almanac-chip (c7d5179), minimap-pings (56a7485), quality-heatmap (22cade2), sky-dial-weather (7c908fe), hotbar-warning (3c8becd). 2323/2323 tests green (+37 this batch). Build 294.28 kB / 89.71 kB gz. ZERO NEW KEYBINDS — and that's correct now (digit row fully spent; every slice rode an existing surface, see observation #2). Theme = "glanceability": surface info the player had to dig for. Almanac-chip = almanacHighlight()/highlightChipText() reuse buildAlmanac's soonest-first ordering; almanac-chip.ts draws a one-line right-column pill under the sky dial (y=110) ONLY when an event lands Today/Tomorrow, kind-keyed colour rail + green Today text, quiet stretches render nothing; 11 tests. Minimap-pings = minimapPings(player,time) derives pulsing rings from the SAME predicates the rest of the game uses (canTurnIn / cartOpen / tournamentOpen && !alreadyEntered); MinimapPanel.draw now threads player+time, draws expanding rings over the landmark pips + under the you-are-here dot + a solid inner dot for the pulse trough; empty array on a calm day; 7 tests. Quality-heatmap = qualityHeat(streak,growthStages) maps the streak->quality ladder onto five readable bands (dry/building/almost/silver/gold) with care-ramped alpha; quality-heatmap.ts washes every crop tile in WORLD space via a (wx,wy)=>camera.worldToScreen callback (same pattern as drawBarks) + a bottom-left legend, gated on cropJournal.isVisible() so it rides the `;` toggle (no new keybind); 10 tests. Sky-dial-weather = skyWeatherStyle(weather) pure deriver {dimmed,cloud,storm}; the widget dims+greys the sun/moon and tucks a small cloud puff (darker for storms) over it on rain/storm via weatherToday(); 4 new tests (19 in the sky-dial suite). Hotbar-warning = seedWarnLevel(count) none/low/empty + seedWarnPulse(level,nowMs) deterministic sine breathe (empty pulses faster + floors brighter) in new pure hotbar.ts; drawHUD/drawHotbar gained an optional nowMs arg (defaults 0 so existing callers unaffected), pulses an amber border on low/empty seed slots + tints the empty count badge amber, riding over the selection border without fighting it; 9 tests. SIGNATURE CHANGES: MinimapPanel.draw(ctx,world,player,time,w,h) [+player,+time]; drawHUD(...,hudScale,nowMs) [+nowMs optional]; sky-dial-widget imports weatherToday. No persistence/schema changes — every slice is a pure read or a passive draw. Gate: tsc clean, 2323 green, vite build clean.
- 2026-06-25 09:45 PT — 5/5 shipped (FRONTEND-FOCUS, 3rd straight) [#35]: toast-kind-tint (3c32330), hud-scale-right-column (236e457), reduce-motion-cues (c7800b0), heatmap-summary (6510022), ping-legend (abf1091). 2369/2369 tests green (+46 this batch). Build 298.08 kB / 90.92 kB gz. ZERO NEW KEYBINDS. Theme = "legible & accessible": closed the a11y gaps the #33/#34 surfaces opened. toast-kind-tint = ToastKind ('info'|'money'|'hearts'|'achievement') + TOAST_KIND_COLOR + toastKindColor() + classifyToast() heuristic so the ~100 setToast sites do not each pass a kind; renderer draws a 3px left rail per pill. hud-scale-right-column = new pure game/hud-layout.ts rightColumnLayout(scale) owns weather-strip/sky-dial/almanac-chip geometry so they honour getSettings().hudScale; at 1.0x reproduces the historical fixed y=40/66/110 EXACTLY (byte-identical default HUD). reduce-motion-cues = pure playerDotRingAlpha/pingRing/seedWarnIntensity; minimap player-dot + ping rings + hotbar seed-warning go steady under reduceMotion. heatmap-summary = qualityHeatCounts/qualityHeatSummary tally + urgency-ordered "3 dry, 1 about to silver-star" header on the heatmap legend. ping-legend = pingLegend(pings) de-dups active pings into colour-blind {color,reason} rows under the minimap. (NOTE: this batch's TICK LOG line was MISSING from the append-only log — reconstructed during tick #36's resume reconciliation from the pushed commits + the prior STATUS block.)
- 2026-06-25 19:48 PT — 5/5 shipped (FRONTEND-FOCUS, 4th straight) [#36, a RESUME]: crop-sway (7fcc8f5), confetti (cb6f1d5), toast-reduce-motion (7152d61), chimney-smoke (672da0d), shop-banner (7ca5a24). 2418/2418 tests green (+49 across the batch: +20 from the two adopted commits, +29 from the three fresh). Build 302.23 kB / 92.49 kB gz. ZERO NEW KEYBINDS. Theme = "a world that feels alive — calmly". RESUME: origin/main sat at #35 (abf1091) but local main was +2 ahead with two interrupted-but-complete commits (crop-sway + confetti: full tests, typecheck green, never pushed, STATE unreconciled). Adopted both as slices 1-2, shipped 3 fresh to reach a clean 5, gated once, pushed abf1091..7ca5a24, reconciled STATE. crop-sway = pure game/crop-sway.ts sub-2px sine canopy lean for mid/ripe crops, per-tile phase (field ripples), reduce-motion + un-grown stages = flat 0; renderer.draw threads nowMs+reduceMotion (defaulted, back-compat). confetti = pure game/confetti.ts deterministic ~2.6s pixel burst on festival/birthday dawn; engine diffs celebrationDayKey frame-to-frame to arm one burst; skipped under reduce-motion. toast-reduce-motion = pure toastAlphaFor(entry,reduceMotion); calm mode holds full opacity then cuts over TOAST_CUT_MS=80ms vs the 600ms fade — last animated HUD surface that ignored reduceMotion, audit now complete. chimney-smoke = pure game/chimney-smoke.ts responsive hearth (hearthLit() lights only in Winter / cold dark hours <8h or >=18h / rain-storm), particle column rises+drifts+fades, roof apex from drawBuilding geometry, reduce-motion returns a frozen 3-puff thread so the home still reads as lit. shop-banner = pure game/shop-banner.ts per-season ShopBannerStyle (palette + 5x5 symmetric motif + label) + thin render/shop-banner-sprite.ts; engine hangs a striped cloth over the shop roofline keyed off time.season; out-of-range seasons wrap safely. New cross-cutting pattern: responsive ambient FX (derive an on/off predicate from sim state, do not render unconditionally). Flagged: roof-apex geometry now duplicated 2x in the engine — extract buildingRoofApex() if a 3rd roof overlay lands. Gate: tsc clean, 2418 green, vite build 302.23 kB.
- 2026-06-25 23:45 PT — 5/5 shipped (FRONTEND-FOCUS, 5th straight) [#37, CLEAN batch]: relationship-panel (58e4319), day-summary (ea77e44), onboarding (6afca1f), minimap-focus (6ffc050), ribbon-hall (d21a9f2). 2456/2456 tests green (+38 this batch: +10 relationship, +9 day-summary, +6 onboarding, +7 minimap-focus, +6 ribbon-hall). Build 310.47 kB / 94.84 kB gz. ZERO NEW KEYBINDS (digit/letter row fully spent; every slice rode an existing surface). Theme = "home, hearts & a warm welcome — the cozy meta layer": went after relationship/home/first-impression surfaces vs the HUD/world ambience of #33-#36. origin/main + local main both at #36 (d351d61) — straightforward 5-fresh batch, no resume. relationship-panel = rework the H panel from a bare name+heart-strip list into a real relationships screen via new pure relationshipRows(player,time): per-candidate hearts + next-birthday countdown (birthdayCountdownLabel) + "loves ..." gift hint (prettyGiftKey, up to 2 adored) + engaged/married status chip (statusChipLabel), sorted by closeness married>engaged>hearts-desc>soonest-birthday>name; drawHeartsPanel threads this.time at the one call site; legacy heartsSummary kept for back-compat. day-summary = pure game/day-summary.ts summaryRows() builds tinted+categorised recap rows (gold/harvest/dishes/hearts, SUMMARY_ROW_COLOR mirrors the toast rail palette) + bestMomentLine() names the slept day's standout (heart level-up > strong gold GOLD_HIGHLIGHT=100 > heavy harvest HARVEST_HIGHLIGHT=6 > dishes > null); sleep-summary overlay draws a pixel glyph per category (coin/sheaf/bowl/heart) + the highlight in its own soft band. onboarding = game/onboarding.ts one-time welcome content + a "seen" flag in its OWN localStorage key sunsprout.onboarding.v1 (survives a save reset, no schema churn); ui/onboarding-card.ts renders rounded key-caps pointing at WASD/E/?/9/0; game.ts opens it on first boot (shouldShowOnboarding), dismisses on any key after a 220ms lockout swallowing that frame's in...
- 2026-06-26 05:42 PT — 5/5 shipped (FRONTEND-FOCUS, 6th straight) [#38, CLEAN batch]: gift-readiness-chip (9c645bd), ribbon-hall-caption (27072e1), tool-rack (37e3bde), day-summary-continuity (244b3a0), heatmap-toast-spill (04d1b97). 2491/2491 tests green (+35 this batch: +13 gift-readiness across gifting+panel tests, +6 ribbon-caption, +7 tool-rack, +6 continuity, +6 spill — actually +13/6/7/6/6 = 38 cases but +35 net new test count after some shared describe blocks). Build 314.59 kB / 96.39 kB gz. ZERO NEW KEYBINDS (every slice rode an existing surface — H panel, V panel, farmhouse wall, sleep overlay, `;` toggle). Theme = "the farm reads back your story — legible state & self-knowledge": surface what the player already earned but couldn't see. origin/main + local main both at #37 (a647ed9) — straightforward 5-fresh batch, no resume. gift-readiness-chip = pure giftReadiness(player,npcId,day) in gifting.ts; ready only when BOTH the per-day gift gate is open AND pickBestGift finds an acceptable (never disliked) item; carries taste + itemKey; relationshipRows threads giftReady+giftTaste; giftChipLabel/giftChipColor (loved>liked>neutral tint) draw a chip on the H panel's birthday line, loves hint clips short via measureText. ribbon-hall-caption = pure ribbonHallCaption() over ribbonHallMounts() so the count can't drift from the drawn rosettes; "4 ribbons on the wall: 1 gold, 2 silver, 1 bronze" best-first, singular/plural noun, '' on fresh farm; AchievementsPanel footer draws it on its own line above the scroll indicator (panel grows only when shown). tool-rack = new game/tool-rack.ts toolRackMounts() reads tierOf(hoe)/tierOf(can)/pickaxeTier/rodTier into a left-to-right layout tinted by shared TIER_METAL palette (wood..diamond, pickaxe's exclusive diamond shown); [] while every tool is wood, whole kit hangs once any upgrades; render/tool-rack-sprite.ts = peg board + distinct pixel silhouette per tool (hoe/can/pickaxe/rod); engine hangs it on LEFT farmhouse wall (pw*...
- 2026-06-26 11:05 PT — 5/5 shipped (FRONTEND-FOCUS, 7th straight) [#39, CLEAN batch]: panel-tab-strip (cb6aed4), bag-panel (56713d3), help-filter (d5961ee), panel-empty (622673b), almanac-personal (a4c5038). 2545/2545 tests green (+54 this batch: +15 panel-tabs, +16 bag, +8 controls filter, +7 panel-empty, +8 almanac-personal). Build 324.46 kB / 99.30 kB gz, 176 modules (up from 170). ONE NEW KEYBIND: Tab (inventory/bag panel) — first new keybind since the digit row filled at #34. Theme = "one consistent shell — your stuff, your shortcuts, no dead ends": close structural UI gaps rather than add surfaces. origin/main + local main both at #38 (30384c6) — straightforward 5-fresh batch, no resume. panel-tab-strip = new pure game/panel-tabs.ts (tabStripLayout returns one rect per tab as equal floor cells minus a gap; cycleTabIndex + tabAtPoint helpers) + thin ui/panel-tab-strip.ts widget in the shared dark-violet chrome; retrofit the lore panel's 6 tabs through it reproducing the historical strip pixel-for-pixel (origin x+14/y+38, width PANEL_W-28, gap 4, height 26). bag-panel = new pure game/bag.ts classifyBagKey/buildBag/bagItemsForCategory/bagCategoryCounts/bagTotalStacks/bagTotalValue — walks the flat inventory Record once, sorts every non-zero stack into 7 tabs (Seeds/Crops/Fish/Gems/Forage/Kitchen/Supplies) with human labels + per-unit gold value resolved from the catalogs (harvest quality multipliers, premium-dish markup, egg/fish/gem/forage sell prices), anything unrecognised title-cases into Supplies so nothing is invisible; ui/bag-panel.ts renders via the shared tab strip (a/d tabs, w/s scroll, per-tab count sub-labels, total stacks + worth header, calm category-specific empty line); wired on Tab as a world-blocking modal that suppresses the gameplay-verb block so w/s scroll without watering, Tab preventDefault'd in input.ts, documented in controls. help-filter = controls.ts bindingMatchesFilter/groupMatchesFilter/matchingGroupIndices/matchingBindingCount (case-insensitive substring on key glyph + label); the ? overlay becomes an input-capturing modal (handled in an early clearJustPressed+return block right after onboarding) that takes typed letters as a live filter, dims non-matching groups+rows WITHOUT shifting layout, green "filter: water_ (1/41)" chip, Backspace deletes one + Esc clears-then-closes. panel-empty = new pure game/panel-empty.ts (PANEL_EMPTY_STATES registry + loreEmptyState() filter-aware resolver) + ui/empty-state.ts thin two-line widget; FIXES the real bug where a filtered-empty lore Rumors tab drew NOTHING (a void reading as broken); money-log + quest-log retrofit off hard-coded one-liners; all reserve body height for the message+hint. almanac-personal = buildAlmanac folds the player's own heart-4 NPC hangout invites in as a new 'personal' AlmanacKind (sorted FIRST within a day-tie, rose rail + M tag in the panel, HUD chip promotion); buildAlmanac/almanacHighlight/AlmanacPanel.draw/drawAlmanacChip all took an OPTIONAL trailing player? arg so every existing caller + the 21 existing almanac tests stayed untouched; reuses the proven 28-day wrap. NOTE: the compost-finish/wedding-day countdown variant was SKIPPED — time.ts wraps day 1-7 per season with no absolute-day counter (obs #7), so the (season,day) hangout invites are the only reliable personal-deadline source. Two input-capturing-modal patterns landed (obs #4); shared-widget pattern now dominant with tab-strip @ 2 consumers + empty-state @ 3 (obs #5).
