# Session 2026-06-28 03:55 PT — Tick #47 (FRONTEND-FOCUS, 15th straight)

CLEAN batch (not a resume): origin/main + local main both at #46 (320532a).
Shipped 5 fresh slices fa4186b..a1d2772, gated ONCE, pushed.

Theme: **"panels surface their direction + the family finally glides in."**
After 14 ticks of building the panel family (filters, dividers, digests,
glyphs, running balance), this tick gave panels a sense of DIRECTION
(purse-trend, search distribution) and shipped THE last big shared-polish
debt — the open transition.

## 5 slices

1. **money-log-purse-trend (fa4186b)** — purseTrend(player) distils
   runningBalances into {start, end, delta, direction}. end = current gold
   (newest row's balance), start = oldest row's balance minus its own delta
   (the purse just before the window's first logged change), delta =
   end - start (== netChange). null on <2 rows (no span). Panel draws
   "320g -> 412g" right-aligned on the summary stripe, tinted by direction
   (gain green / loss red / flat dim); the in/out/net text clips to leave
   the trend room. No new band. +6 tests.

2. **almanac-today-band (26389c4)** — soft sage wash (rgba 163,215,122,0.10)
   + warm left rail behind the TODAY section's divider + rows, drawn BEFORE
   the text so it reads as a highlight. Pure render over the existing
   almanacSections() split; only when a today-section is present (e.g.
   Spring 7's Planting Fair); spans exactly the reserved group height so no
   layout shift. +3 tests (fill-capturing stub).

3. **bag-search-tab-counts (8a0e9fd)** — bagSearchCategoryCounts(player,
   query) buckets bagSearchResults by category. While `/` search is armed
   the tab strip swaps each tab's stack-count sub for the per-category
   match count, so the player reads the match distribution (typing "ruby"
   concentrates hits under Gems). Counts sum to bagSearchMatchCount,
   all-zero on empty query. +5 tests.

4. **settings-value-preview (793ecc9)** — settingsRowPreview(row, settings)
   discriminated tint/scale/toggle/none descriptor. drawValuePreview paints
   a night-tint swatch (washed at the row's alpha), a HUD-scale "A" glyph
   (sized to the scale), an on/off pip (filled/hollow) right-aligned just
   left of the value text. No row-layout shift. reset/close rows have no
   preview. +6 tests (pure helper + draw smoke).

5. **panel-transition (a1d2772)** — THE last big shared-polish item. Pure
   game/panel-transition.ts panelOpenAlpha(lockoutMs, reduceMotion,
   durationMs=160): smoothstep ramp from PANEL_OPEN_MIN_ALPHA=0.35 -> 1.0
   off the existing open lockout. Wired via ctx.globalAlpha right after each
   panel's ctx.save() (scoped, no leak) into 11 panels: achievements,
   money-log, quest-log, bag, settings, recipe-codex, crop-journal,
   almanac, lore, minimap, help-overlay. reduce-motion snaps solid. The
   help-overlay (no player) gained an optional reduceMotion draw arg; the
   engine threads getSettings().reduceMotion. Hearts panel out of scope
   (stateless draw fn, no open lockout). +9 tests.

## Decisions

- **SKIPPED relationship-gift-glyph-row as redundant.** Refill #8 listed it,
  but the hearts panel's loves-line ALREADY draws giftKeyGlyph in place of
  the bullet (lovedGlyphKey path). A second standalone pip would be
  cosmetic padding, not a new capability — the prompt's "never pad to hit 5"
  rule. Pulled the panel-transition carryover forward instead to keep 5
  genuinely-distinct slices.

## Gotchas

- **Two structural fuzzy-patch line-drops on settings-panel.ts.** Adding the
  SettingsRowPreview union + drawValuePreview method, two patches each
  silently dropped a single line (the `export class SettingsPanel {` header,
  then the `function rowLabel(...) {` header), flooding ~365 stale LSP
  errors. Caught both by re-reading right after the patch + verifying with
  `npx tsc --noEmit` (exit 0 after each restore). Re-confirms obs #3.

## Gate (ONCE, at end)

- `npx tsc --noEmit`: clean.
- `npx vitest run`: 2808/2808 green (+29 from 2779 baseline).
- `npm run build`: 352.59 kB / 107.30 kB gz, 179 modules (+1 for the new
  panel-transition.ts).
- Pushed 320532a..a1d2772 to origin/main, verified landed.

## Next tick

Refill #9 added (8 items). The shared-polish backlog is essentially cleared
— the panel family is mature. Top picks: panel CLOSE fade-out (symmetric to
this tick's open fade), folding the hearts panel into the class-panel +
fade family (it's the one overlay still snapping in), purse-trend sparkline.
