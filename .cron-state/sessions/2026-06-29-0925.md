# Sunsprout cron session — 2026-06-29 09:25 PT (tick #56)

24th consecutive FRONTEND-FOCUS batch. CLEAN (not a resume): origin/main + local main
both at #55 (bbb3415) on entry. Shipped 5 fresh slices e287709..82e84d6, gated once,
pushed. Lock held throughout; released at end.

## Theme
"The open-fade family + the HUD-scale audit, finished." Two long-running family-wide
audits CLOSED this tick. Refill #17 was nearly exhausted (most items flagged
redundant/marginal), so I studied the actual source and found 4 fresh slices off NEW
surfaces + completed the 1 real carryover.

## Shipped (5/5)
1. **almanac-today-chip-glyph** (e287709) — the TODAY "N today" weight chip now leads
   with almanacSectionBusiestKind(today entries)'s 5x5 glyph in TODAY green, completing
   #55's three-divider glyph language (week/later already had it). Pure render reuse of
   the existing helper. +4 tests (Spring 3 stacks cart+birthday -> cart wins the tie;
   draw-smoke for the 1x1 TODAY-green glyph pixels).
2. **modal-open-fade** (63a66ab) — shop/cart/cooking/chest/bench/owl all join
   panelOpenAlpha. The six buy/craft/storage modals were the LAST overlays that snapped
   in while every info panel + hearts (#55) faded. They each already armed a 180ms open
   lockout (the exact fade clock), so this was a free fit. Added a shared
   MODAL_OPEN_LOCKOUT_MS=180 to panel-transition.ts; wired the globalAlpha hook right
   after each modal's ctx.save() (scoped, scrim+panel ease in together). 5 read
   getSettings(player).reduceMotion; chest-menu took a new optional reduceMotion arg
   game.ts threads in. +4 tests (faint first frame / solid settled / solid under calm,
   across all 6 via a globalAlpha-capturing stub).
3. **stamina-warning** (e06ec14) — the HUD stamina bar pulses an amber/red border when
   the pool runs low, mirroring the hotbar seed-stock warning. New pure model in
   stamina.ts (staminaWarnLevel/Pulse/Steady/Intensity/Color). Thresholds tied to ACTION
   COSTS (crit < a mine swing = the costliest action, so the next swing would bounce; low
   < 20) NOT a fraction of max, so a bath-house max buff (cap up to 130) doesn't shift the
   "can't afford to work" line. drawStaminaBar took optional nowMs+reduceMotion
   (defaulted); game.ts threads renderNow + settings. +17 tests (incl. the bath-buff
   invariant + the motion router).
4. **banner-hud-scale** (6185206) — the long-deferred "HUD-scale audit for banners"
   carryover. New pure game/banner-layout.ts scales the birthday+festival ribbons with
   hudScale (height/font/padding/row-Y) so they tuck under the scaled top bar + stack
   cleanly; at 1.0x it reproduces the historical values EXACTLY (h=18/font=11/firstY=36/
   secondY=54). +9 tests (back-compat + scaled growth + no-overlap at every scale).
5. **hotbar-hud-scale** (82e84d6) — the bottom action bar was the LAST fixed-size core
   HUD surface. New pure game/hotbar-layout.ts scales slot/gap/font/Y/startX; the
   fixed-pixel crop + watering-can sprites grow via a ctx-transform anchored at the slot
   centre (the new reusable trick for fixed art in a scaled slot). 1.0x reproduces the old
   values exactly (slot 48/gap 6/font 11/y=canvasH-60/spriteScale 1). +11 tests (incl. a
   guard that the test slot count tracks CROP_KEYS+1).

## Gate (once, at end — GREEN)
- `npx tsc --noEmit`: exit 0
- `npx vitest run`: 3015/3015 pass, 307 files (+45 from baseline 2970)
- `npm run build`: success, 181 modules (+2), 366.61 kB / 111.54 kB gz
- Push bbb3415..82e84d6 -> origin/main verified, local == origin.

## Notes / observations
- obs #3 (stale LSP flood) fired TWICE: a "Cannot find name 'hotbarLayout'" on the hud.ts
  import-add, and a phantom Player/PlacedChest type mismatch on the modal-fade test edit
  (lines unchanged after the patch already fixed them). Both were stale echoes — tsc exit
  0 was truth, re-read confirmed clean. Never trusted the LSP flood or the patch warning.
- NEW LESSON (logged as refill #18 lesson b): a "snap-in vs fade-in" or "fixed-size vs
  scaled" AUDIT across a whole widget family is a rich non-padding vein. Finding the ONE
  surface a shared treatment skipped (the modals for the fade, the hotbar for the scale)
  is a genuine vertical slice, not filler. Look for the next family-wide treatment with a
  straggler.
- Both long-aged HUD/transition debts (banner-scale, modal-fade) are now CLEARED. The
  single most-aged STRUCTURAL carryover is the panel-CLOSE-fade (bigger blast radius —
  touches every panel's close()/draw — so budget + gate hard when picked).
- DEFERRED correctly: panel-CLOSE-fade (too invasive to land safely as one of five right
  before a gate; prior ticks deferred it for the same reason). Refill #18 carries it
  forward as the oldest item + 5 fresh follow-ons the #56 audits opened.

## Roadmap state
Refill #17 closed (almanac-TODAY-chip-glyph shipped; the rest flagged redundant/marginal/
drop). Refill #18 added with 6 items: panel-CLOSE-fade (oldest), modal confirm
press-pulse, stamina-warning toast spill, sky-dial/weather-strip sprite-scale audit,
banner confetti tie-in, toast-stack HUD-scale audit.
