# Tick #53 — 2026-06-29 00:30 PT — FRONTEND-FOCUS (21st straight), CLEAN batch

5/5 shipped, gated once, pushed to main (b4b96c3..14959da).

Theme: "panels finish their pip/arrow digest pairs" — completed the 5 surfaces refill #14 listed, each pairing a per-row/per-divider glyph with its panel's existing header digest.

## Slices
1. **quest-active-remaining** (932e854) — questActiveRemaining(player) counts incomplete quests + summed remaining steps (goal - clamped progress); questRemainingLabel pluralises "3 quests, 11 steps left", "" on a clear board. Drawn dim, right-aligned on the pct row so the board bar reads as a figure of work ahead. +5 tests.
2. **crop-journal-streak-pip** (3a06326) — 4px green pip left of the right-aligned "streak N" text, matching the field-status urgency pips. Pure render.
3. **money-log-day-net-arrow** (fdd2407) — each DAY divider net leads with a 5x5 up/down/flat arrow via existing purseArrowGlyph + drawArrow, net-tinted; rule trimmed to clear glyph. Pure render.
4. **hearts-gift-ready-pip** (5a0b666) — taste-tinted 4px dot left of each gift-ready chip so per-row state mirrors the header tally; dropped near panel edge. Pure render.
5. **almanac-today-chip** (14959da) — almanacTodayCount + almanacTodayChip surface "N today" at 2+ events, right-aligned on the TODAY divider. +3 tests.

## Gate
- npx tsc --noEmit: clean
- npm test: 2931/2931 green (+8 from 2923)
- npm run build: 362.59 kB / 110.37 kB gz, 179 modules

## Notes
- Clean batch, no resume. 2 new test files, no new source files.
- Refill #14 down to 2 marginal carryovers; added refill #15 (settings-legend-caption, quest-next-up-pip, almanac-section-chips, hearts-bday-pip, money-savings-bar, crop-sown-pip, panel-close-fade).
- Most-aged debt unchanged: panel-CLOSE-fade, hearts-into-fade-family.
- No LSP flood (pure-render + single-fn adds).
