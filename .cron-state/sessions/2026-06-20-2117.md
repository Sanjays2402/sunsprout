# Session — 2026-06-20 21:17 PT (tick #6)

Batch goal: 5 vertical feature slices on feature/autoship. Result: 5/5.

## Shipped
1. `e36f20d` feat(rod-upgrades) — copper/iron/gold rods at Maple's. Per-tier bite-window scale (1.0/1.2/1.4/1.6x) + fish-pool bias that down-weights minnows and lifts pike. Costs 250/700/1800g; `=` near the shop spends. Extended `Rod.cast()` to accept optional `biteWindowMs` + `fishPicker` overrides so the FSM stays decoupled. Persistence: `rodTier` field on the save (defaults to wood). 16 new tests.
2. `1897764` feat(stamina) — daily 100-point pool drains on till/water/mine/cast (5/3/8/4). Refills at dawn + on sleep. `Z` sips the best drink in the bag (cocoa +35 > tea +20). HUD bar in the top-left, colour ramps green->amber->red. Spend refunds on no-op (clean facing) so the player never loses energy on a misclick. 19 new tests.
3. `991bbbc` feat(cart) — Pip's travelling cart parks at (16,9) on day 3 of every season (09-18h). Catalog: rare pumpkin seed (220g), premium flower seed (90g), ready-to-drink hot cocoa (80g) and sage tea (35g), brass lantern cosmetic (350g). `E` near the cart opens a modal styled after cooking-menu. Sprite is a chunky wooden bed + pink-striped awning + two wheels. Dawn toast announces the visit. 14 new tests.
4. `c3331d4` feat(auto-restock) — 600g convenience kit Maple lists. Hooks the plant verb (recordLastSeed) and the dawn rollover (dawnRestock) to top the bag's last-planted seed back up to 5. Never overspends — buys exactly as many as the wallet allows. Spends tagged "auto-restock <crop>" in the money log. 12 new tests.
5. `2d0a556` feat(lore) — backtick toggles a 5-tab bestiary (Fish / Gems / Forage / Crops / Folk). Auto-discovery — every render reads live inventory / journal / hearts so there's no separate unlock state to persist. Per-tab progress chips, overall % completion in the title, locked rows show a category-specific teaser ("Wander the grass at dawn."). a/d switch tabs, w/s scroll. 11 new tests.

## Gates
- `npx tsc --noEmit` — clean
- `npm run build` — 171.78 kB / 52.10 kB gz (was 154.80 / 47.68 last tick; +17 kB raw, +4.4 kB gz from 5 features)
- `npm test --run` — 835/835 (+72)

## Notes & risks
- Keybind real estate is now genuinely cramped. `=` and `Z` and backtick land cleanly; only numeric keys, `{`/`}`, F-keys, and `~` remain. Next time a feature wants a new key, the answer should probably be a generic "panel switcher" key.
- The fishing rod refactor split `pickFish` into a per-cast picker. Existing fishing.test.ts cases still pass because the catalog default path is preserved when no opts are passed.
- Stamina spend wraps the actual action; if till/water/mine returns no work (player faced the wrong tile), we refund the cost so the pool only drains on real work. Cast is committed at point of cast — there's no "no fish in this water" check so a refund there could be abused.
- Pip's catalog includes a `cart-lantern` cosmetic key with no current behavior beyond inventory and the "Farm Decorator" achievement counting it. A later tick can wire it into an interior render.
- Lore panel reads `player.hearts[id].lastTalkDay ?? -1` to gate the "Folk" discovery, so any chat at all flips the row. Default hearts state has lastTalkDay = -1 until creditTalk fires.
