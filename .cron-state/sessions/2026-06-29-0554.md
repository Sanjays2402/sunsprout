# Tick #55 — 2026-06-29 05:54 PT — FRONTEND-FOCUS (23rd straight), CLEAN batch

5/5 shipped. origin/main + local main #54 (b84f6fe) -> #55 (c4378a6). Gated once, pushed.
Theme: "threshold tints + glyph echoes + the last overlay joins the fade family".

## Slices
1. money-log-savings-low (5436d20) — purseSavingsLow(split) + SAVINGS_LOW_KEPT=0.2 pure predicate over the existing kept/spent split; the thrift mini-gauge flips its kept-half from sale-green to LOSS-red when the player banked <= 20% of income, so an overspend reads as a colour change, not just a shrinking sliver. null splits never low. +5 tests.
2. almanac-section-busiest-kind (12b4b8e) — almanacSectionBusiestKind(entries) tallies a section's kinds + returns the dominant one (ties break by SUMMARY_KIND_ORDER, personal first); null on empty. THIS WEEK / LATER chips lead with that kind's 5x5 glyph (drawKindGlyph reuse) in its rail colour, so the chip says WHAT, not only how many. +5 tests.
3. crop-journal-sown-pip (6bd1bae) — sownPipState(entry) none(dim)/sown(amber)/harvested(green) ladder; 4px pip leads the "sown N" lifetime figure (text indents 7px), matching the field-status + streak pip language so the journal's accent palette reads across all three digests. +4 tests.
4. settings-legend-glyph-echoes (49d7e7a) — settingsLegendParts(s) breaks the look digest into one part per cosmetic setting tagged with its row; settingsLegendCaption now derives from it (text can't drift). The under-title digest leads each tint/HUD/calm segment with the SAME swatch / scaled-A / on-off pip the row draws (drawValuePreview reuse). +3 tests.
5. hearts-into-fade-family (c4378a6) — STRUCTURAL: converted the stateless drawHeartsPanel() into a HeartsPanel class (open/close/toggle/update/lockoutMs/isVisible/canAct/currentSort/cycleSort) joining panelOpenAlpha + gaining a canAct lockout. game.ts rewired heartsPanelVisible + heartsSortMode onto the instance across 7 sites (import / field / H-toggle / f-cycleSort / update loop / fishing-f guard / draw). The LAST overlay outside the fade family is now in it. Pure relationshipRows / sortRelationshipRows unchanged. +5 controller tests; the 28 relationship-row tests still pass.

## Gate
2970/2970 tests green (+22). tsc exit 0. Build 364.72 kB / 110.97 kB gz, 179 modules.

## Notes
- Clean batch, no resume. 3 new pure helpers (purseSavingsLow / almanacSectionBusiestKind / sownPipState) + 1 caption refactor (settingsLegendParts) + 1 structural class conversion. No new source files; no new modules.
- SKIPPED the redundant "Hearts closeness mini-bar" (the heart strip already draws N/max as 10 discrete pips — a continuous fill bar would duplicate it, obs #5 skip-when-redundant). Pulled the most-aged carryover (hearts-into-fade-family, open since #46) forward as the 5th slice instead.
- obs #3 LSP-FLOOD fired TWICE this tick: (a) the crop-journal sownPipState insert — the fuzzy matcher ATE the ribbonMedalGlyph "/**" opener, leaving a dangling comment + ~100 phantom errors; caught by a re-read, restored with a tight one-line patch, tsc exit 0 confirmed. (b) the hearts class conversion threw a phantom "}" + a flood of "Cannot find name" / "Property does not exist" as the multi-step game.ts rewire landed; every one cleared once all 7 sites were patched + tsc came back exit 0. Lesson re-confirmed: trust tsc (exit 0 is truth), never the LSP echo OR the patch success flag, on a structural edit.
- refill #16 exhausted (4 of 7 shipped + the hearts-fade carryover pulled forward, 1 skipped redundant, 1 carryover = panel-CLOSE-fade); refill #17 added with 7 items. panel-CLOSE-fade is now the OLDEST + ONLY structural carryover, and every panel (incl. hearts) now has the open clock it would invert.
