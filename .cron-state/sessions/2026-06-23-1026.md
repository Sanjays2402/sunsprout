# Tick #24 — 2026-06-23 10:26 PT

5/5 shipped. Five clean, revertible commits on `main`:

- `34d15dd` feat(owl-post-letter-chain): consecutive-day bonus for mailing the same recipient — OWL_CHAIN_TIERS table; chain breaks on skip/swap; chain bonus compounds with birthday multiplier; failed dispatches never advance; ENGINE BUG FIX: money log was over-reporting owl-post fees by ignoring tier discount
- `2634aca` feat(lore-folk-chain-indicator): "Chain: N days." tail on lore Folk row when active chain length >= 2; per-NPC isolation; swap/skip drops the tail
- `e982c25` feat(owl-menu-tier-color-chip): bronze/silver/gold tier chip + pending chain preview on the owl-menu row; closes the loop on observation #5's chip pattern (now spans FIVE layers)
- `c052c32` feat(tournament-champion-achievement): 23rd badge for 3 career gold ribbons; orthogonal to festival-regular
- `6b5bc05` refactor(compost-ladder-nudge): extract generic ladderNudge() pure helper from compostLedgerLine + compostHalfwayDawnNudge; gets ahead of refactor pressure same way last tick's assembleDawnToast did

## Gate

- `npx tsc --noEmit`: PASS
- `npm run build`: PASS (268.33 kB / 82.13 kB gz, +1.85 kB / +0.63 kB gz vs last tick)
- `npx vitest run`: PASS — 2004/2004 tests across 211 test files (+92 vs last tick's 1912/207)

## Highlights

**The owl-post-letter-chain is the centerpiece of this tick.** Same shape as last tick's deep-vein-dawn-brag (sticky-flag round-tripping through persistence), now applied to a per-NPC consecutive-day streak. The chain has clean break semantics: same-day re-press is idempotent (no double-bump), day skip breaks (chain.lastDay + 1 != day), recipient swap resets (chain.npcId != npcId). The bonus tier table (1.0x / 1.1x / 1.2x / 1.3x at lengths 1 / 2 / 4 / 7) caps below the courtship-bouquet's universal-love bonus so a real in-person grind still outpaces a pure owl strategy.

**The ENGINE BUG FIX caught during owl-post wiring is the kind of thing that wouldn't have surfaced without a feature touching this code path.** The engine was hardcoded to `logGold(-40)` on a successful owl-post regardless of the tier discount — a favorite-courier dispatch was over-logging the fee by 12g per send for every player who reached that tier. Now it reads `owlPostFeeFor(player, npcId)` and logs the actual paid amount. Pure side-effect win — no test failed before, but the money log was lying to the player.

**LadderNudge refactor mirrors last tick's dawn-toast composer.** Same get-ahead-of-pressure pattern: the compost halfway-nudge ladder was at 2 rungs in 2 surfaces (4 ternary branches total) and would have hit 3 rungs in 2 surfaces (6 branches) before any new compost-tier nudge ships. Now both call sites are a 1-line walk over a rung array; the next nudge is a 4-line array push, not another nested ternary in two surfaces. The compost-master sash + pulper rare-bag tier roadmap items will land for free on this infrastructure when they ship.

**Owl-post surface now spans FIVE layers.** Observation #5 from last tick (which claimed three layers and pointed to the tier-color chip on the owl-menu as the natural fourth corner) is RETIRED. After this tick:
1. Per-NPC text line on the lore Folk row description (tier label + chain length)
2. Per-NPC price chip on the owl-menu row (fee + tier discount)
3. Per-NPC color chip on the lore Folk row (bronze/silver/gold)
4. Per-NPC color chip on the owl-menu row (mirrors #3)
5. Per-NPC pending-chain preview on the owl-menu row (chain: N)

The roadmap item "owl-menu chain-bonus payout chip" (showing the +10/20/30% on the matching row) is the natural sixth corner — chainBonusMultiplier already exists; the chip just needs to read it and draw a "+10% hearts" tag the same way owlPostFeeChip draws the savings tag.

**Tournament Champion vs Festival Regular orthogonal-bar design.** Festival Regular fires at tournamentCareer().entries == 4 ("you've shown up to every seasonal tournament"). Champion fires at tournamentCareer().ribbons.gold >= 3 ("you've actually WON gold on 3 of them"). The two badges sit at distinct points on the player's progression — Festival Regular is the "show up" badge, Champion is the "actually win" badge. A bronze-only grind earns Festival Regular but never Champion; a 3-gold partial-calendar earns Champion but not yet Festival Regular.

## Observations refreshed

1. **Zero new keybinds this tick — ninth in a row.** Pattern of "ship via existing inputs" is bulletproof. Two digits (9, 0) still free.
2. **LadderNudge refactor landed THIS tick — observation #2 from past ticks (the compost ladder ternary pressure) is RETIRED.** Same get-ahead pattern as last tick's dawn-toast composer.
3. **Chain pattern matured.** OwlStampBook.chain {npcId, length, lastDay} mirrors MineHaulState's deepVeinBragPending/Fired sticky-flag shape. Future per-NPC streak features can copy this exact shape.
4. **Composer / chip pattern dominant.** Three of five features ship via small pure helpers consumed by thin UI layers.
5. **Owl-post surface now five layers.** Sixth corner obvious (chain-bonus payout chip on the menu row).

## Files touched

- `src/game/owl-post.ts` (OwlStampBook.chain + chain helpers + chain wiring in dispatchOwl + OwlPostOutcome shape widened)
- `src/game/gifting.ts` (attemptAutoGift gained optional extraMultiplier arg, backward compatible)
- `src/game/persistence.ts` (chain round-trip via deep-copy on serialize + apply)
- `src/engine/game.ts` (owl-post bug fix on money log + chain tail on setToast + day passed to owlMenu.draw)
- `src/game/lore.ts` (chainTail on Folk row description)
- `src/ui/owl-menu.ts` (tier color chip + pending chain preview + optional day arg to draw)
- `src/game/tournament.ts` (TOURNAMENT_CHAMPION_GOLD_RIBBONS + tournamentChampionMilestoneReached)
- `src/game/achievements.ts` (tournament-champion catalog row + AchievementId union widened)
- `src/game/compost.ts` (ladderNudge + LadderNudgeRung + compostLedgerLine + compostHalfwayDawnNudge refactor)
- `tests/owl-post-chain-bonus.test.ts` (NEW, 32 tests)
- `tests/lore-folk-chain-indicator.test.ts` (NEW, 10 tests)
- `tests/owl-menu-tier-color-chip.test.ts` (NEW, 7 tests)
- `tests/tournament-champion-achievement.test.ts` (NEW, 19 tests)
- `tests/compost-ladder-nudge.test.ts` (NEW, 24 tests)

## Push verified

```
6b5bc05 (origin/main, HEAD -> main) refactor(compost-ladder-nudge): ...
c052c32 feat(tournament-champion-achievement): ...
e982c25 feat(owl-menu-tier-color-chip): ...
2634aca feat(lore-folk-chain-indicator): ...
34d15dd feat(owl-post-letter-chain): ...
17a32fd chore(autoship): tick log + session notes for 2026-06-23 05:42 PT batch
```

`17a32fd..6b5bc05  main -> main` — clean fast-forward, no force, no merge commit.
