# Session 2026-06-23 02:10 PT — Tick #22

## What landed

**5/5 features shipped** — all squash-clean, one commit per slice, pushed to `main`.

1. `9e0d6ad` — **deep-vein-achievement** — 21st badge in the catalog. `DEEP_VEIN_COUNT = 20` OR `DEEP_VEIN_GOLD = 1000` single-run thresholds; `deepVeinMilestoneReached` reads `bestRun` off the existing `MineHaulState` so no new persisted field is needed. Tuning sanity tests assert `>= 2x` of the largest mid-run milestone (`MINING_RUN_MILESTONES` top = 10, `MINING_RUN_GOLD_MILESTONES` top = 500) and below `LIFETIME_MINING_MILESTONE` so the single-run ribbon sits cleanly between the per-strike toasts and the lifetime career badge. 13 tests covering tuning sanity, predicate on fresh / below-threshold / count-only / gold-only / split-record runs, and catalog wiring (the `>=21` badge-count guard already lives on the pulper achievement test which I kept compatible).
2. `c5257ec` — **mining-haul-ribbon-nag** — `RECORD_GAP_NAG_MIN_PCT = 50` floor; `recordGapNagLine()` appended to `haulYesterdayLine` as a `" - N% of your best run"` tail on close non-record runs. Cap at 99% keeps the brag tail + nag tail mutually exclusive — the dawn toast surfaces at most one of the two. `max(countPct, goldPct)` picker mirrors the split-record promotion logic in `resetMineHaul` so a near-gold-record run on weak count still surfaces the encouragement. Silent on fresh saves / empty `lastRun` / record-breaking runs / below-floor runs. 14 tests covering the silence conditions, fires at exactly the floor + above + edge (99%), max(count,gold) picker on near-gold runs, the 99% cap, and brag-vs-nag mutual exclusion.
3. `09585b8` — **compost-halfway-dawn-nudges** — `compostHalfwayDawnNudge()` one-shot helper with two new sticky boolean flags on `CompostLedgerState` (`masterNudgeDawnFired` + `pulperNudgeDawnFired`). Wired into the engine dawn-toast chain as a fourth conditional tail after `pondOverflow` + `haulRecap`. Survives reload via `persistence.applySnapshot`. Compost-master priority over pulper when both simultaneously eligible (unreachable in normal play but hand-craftable). Wording: `"Halfway to Compost Master - 32g to go."` / `"Halfway to Pulper - 314 bags to go."` Symmetric pulper guard requires `lifetimeRecycledGold >= MILESTONE_GOLD` so it can't fire until compost-master is actually earned. 16 tests covering silence on fresh / below-floor / after-milestone, master nudge fires at floor + mid-range + remaining-gold naming, one-shot behavior, pulper nudge fires after master earned + bag-floor reached + singular "1 bag" phrasing, priority order, pulper-suppressed when only master on runway, both fired-flag side effects.
4. `e08ae4c` — **owl-menu-fee-chip** — `owlPostFeeChip()` pure helper drawn on each owl-menu row in `GOLD` when discounted / `HINT`-dim at full price. Surfaces `"32g (-8g, regular)"` using the SHORT tier word (occasional/regular/favorite) without the `"pen pal"` / `"courier"` suffix so the chip stays under 30 chars. The full tier label is already on the Folk lore row. Closes the UX gap where the tier discount was invisible at decision time — previously only visible post-dispatch in the confirmation toast (a feedback loop arriving AFTER the spend). 10 tests covering no-discount baseline, no-discount tag in base chip, discounted chip at every tier floor (price + savings + short label), short-label contract (no "pen pal"/"courier"), per-tier promotion, savings math correctness for every tier in `OWL_POST_TIER_DISCOUNT_PCT`, per-NPC isolation, and the <30-char compactness guard.
5. `f097a53` — **tournament-career-recap** — `TournamentCareer` aggregate (`entries` / `ribbons` / `bestScore` / `bestKind`) + `tournamentCareer(player)` walker + `tournamentCareerLine(player)` four-wording renderer + `tournamentNudgeWithCareer(player, time)` composite. Engine swaps `tournamentNudgeLine` for the with-career variant on the tournament-dawn line so the toast carries IMMEDIATE strategy (`"Carrying 6 - silver needs 8"`) + LONG-TERM view (`"Career: 3 entries - 1 gold / 1 silver. Best: 18 (Spring Flower Show)."`) in one breath. The recap parses the entries-map key shape `${season}-${kind}` to recover `bestKind` without storing a redundant field. Zero-count tiers are skipped from the ribbon list so the line stays compact. 16 tests covering empty-state aggregate, single-bronze, multi-tier across kinds, best-score + best-kind tracker, no-prize entry semantics, singular/plural entry phrasing, zero-tier skip, no-ribbons-yet branch, best-kind label naming, composition (non-tournament-day empty, tournament-day composite, first-time player), and no-double-pay-on-re-read.

## Quality gates

- `npx tsc --noEmit`: clean
- `npm run build`: 263.86 kB / 80.84 kB gz (last tick: 261.48 kB / 80.02 kB gz; +2.38 kB / +0.82 kB gz this batch)
- `npm test`: **1842 / 1842 passing** (+69 this batch over 1773 last tick)
- Push: `14905ac..f097a53  main -> main` confirmed via `git fetch && git log origin/main`

## Patterns hardened

- **ZERO new keybinds — seventh tick in a row.** Nine of the last fourteen features added no new input. Digits 9 and 0 still free.
- **Dawn-toast tail chain grew TWO ways this tick.** `haulRecap` gained a parallel `"- N% of your best run"` branch (mutually exclusive with the brag tail), AND a brand-new fourth conditional tail `[* compostNudge]` lands AFTER `haulRecap` when `compostHalfwayDawnNudge()` fires. New chain shape: `base ($flavorTail) [* pondOverflow] [* haulRecap [- {brag | nag}]] [* compostNudge]`. With another tail or two queued (deep-vein dawn brag, future per-NPC owl ribbon), retiring the chain into a central `assembleDawnToast([...])` is now a real candidate for next tick.
- **CompostLedgerState gained its first BEHAVIORAL state flags.** `masterNudgeDawnFired` + `pulperNudgeDawnFired` are the first non-counter fields on the ledger — sticky boolean one-shots that survive reload via `=== true` coercion in `applySnapshot`. Mirrors the storm-shelter trial one-shot pattern.
- **Owl-post UI gained its first per-NPC ECONOMIC decision surface.** Until this tick the tier discount was post-hoc feedback (toast after spend); now `owlPostFeeChip` lands the per-NPC price + savings in front of the player AT decision time. The chip's GOLD-when-discounted styling is a visual signal that turns the tier system from a passive lore-row tag into a live cost-comparison surface.
- **Tournament career recap finally pulls the entries map into a single readable line.** The map has been sitting on `Player.tournament.entries` since tick #6 with no aggregate reader; `tournamentCareer` + `tournamentCareerLine` close that gap and pair beautifully with the existing `tournamentNudgeLine`. Sets up a clean path to the full leaderboard panel next tick.

## Disk scare

No recurrence. Disk pressure stayed normal throughout the tick.

## Refilled roadmap

Five fresh slice ideas appended now that the five ideas from last tick all landed:
- Deep Vein dawn brag (one-shot dawn tail the morning after the badge is earned)
- Mine haul ribbon journal page (per-gem breakdown of bestRun composition in the Gems lore tab)
- Tournament career achievement (`entries >= 12` "Festival Regular" 22nd badge — leans on the new aggregate)
- Owl-post chain bonus (consecutive-day streak multiplier on heart points)
- Compost-master + pulper dawn nudges priority refactor (extract `assembleDawnToast([...])` — overdue with the chain now at 4 tails)

## Telegram-ready summary

Shipped 5 features cleanly: deep-vein-achievement (21st badge for single-run hauls of 20+ gems OR 1000+g), mining-haul-ribbon-nag (dawn tail "yesterday was 80% of your best run" on close non-record days), compost-halfway-dawn-nudges (one-shot "Halfway to Compost Master / Pulper" dawn toast for players who never open the journal), owl-menu-fee-chip (per-row tier-discount price chip on the owl menu so the discount is visible AT decision time, not just in the post-toast), and tournament-career-recap ("Career: 3 entries - 1 gold / 1 silver. Best: 18 (Spring Flower Show)." tail on the tournament dawn-toast). 1842/1842 tests green (+69), build 263.86 kB / 80.84 kB gz. Zero new keybinds — seventh tick in a row. The owl-menu fee chip turns the tier discount from passive UX into a live cost-comparison surface.
