# Tick 2026-06-21 06:30 PT

## Shipped (5/5)

1. **crop-ribbon (11136b6)** — `recordHarvest()` gains an optional `(season,day)` arg; today's running harvest tally bumps `bestDayHarvest` whenever it overtakes. `CropJournalEntry` exposes `ribbonCount` + `ribbonWhen` ("Fall d4"). Panel renders the ribbon under the streak row in amber. Persistence: cropJournal type widens with optional fields; spread serializer already round-trips them. `ROW_H` 44 → 50 for layout. 16/16 tests on crop-journal.test (+7 new).
2. **animal-happiness (4e9da70)** — new pure module. Coop happiness 0..100 (collect +5, feed +3, cap once-per-day, decays 1/day, capped at 100). Fancy egg rate: tier base + (happiness/100 * 0.06pp). Dog+cat `streakBonus` adds compounding +1/+2/+3 at petStreak 8/12/14. Game.ts: collect path bumps happiness with mood-stamped toast; idle-coop E-press now feeds instead of dead-ending. Dawn rollover decays + pays bonus on top of dog/cat streak. 13 new tests.
3. **storm (278d3b3)** — once-per-season deterministic storm on day [2..6]. Outdoor crops lose a waterStreak point, drought counter bumps; greenhouse crops are safe; forage clears. `maybeFireStorm` is idempotent per (year,season) via persisted `hit` map. Fires AFTER greenhouseTick so the greenhouse re-water happens first → outdoor-only impact. Dawn toast carries memo. 13 new tests, full persistence round-trip.
4. **night-fishing (ccf8dc1)** — hours 22:00-03:59 (wraps midnight) apply a per-fish bias on top of rod-tier bias: minnow x0.4, carp x0.85, bass x1.25, trout x1.75, pike x2.25. `nightAwareFishPick` combines both biases in one weighted pass. Game.ts rod cast swaps `weightedFishPick` for `nightAwareFishPick(p, time, rng)`; toast splits to call out the perk. 8 new tests including a 2000-cast empirical comparison proving night beats noon on trout+pike.
5. **stamina-teas (9807913)** — three new restore-tier recipes: Berry Tonic (+25), Mushroom Broth (+30), Sunflower Elixir (+60). All slot into existing RECIPES + STAMINA_RESTORE → drinkBest tiers them automatically. Sell prices markup above raw ingredients; ingredient mix uses forage + crops. 12 new tests.

## Gate

```
npx tsc --noEmit          → clean
npm run build             → 211.38 kB / 63.67 kB gz (+ 4 kB vs last tick)
npm test                  → 1033/1033 passed (+52)
git push origin main      → 4d21e6f..9807913
```

## Notes

- No new keybinds this tick. Every feature extended an existing flow (`;` panel, E-press on coop, rod cast, dawn rollover, RECIPES catalog). This is a deliberate antidote to the keybind-exhaustion blocker called out in STATE.md.
- `weightedFishPick` is still imported in game.ts but no longer called — leave for cleanup pass.
- Storm uses a year-counter derived from `Object.keys(hit).length / 4` so a multi-year save sees a fresh storm sequence each year without needing an explicit year field on TimeOfDay.
- Animal happiness wraps existing fancy-egg math non-destructively — tests on fancy-eggs and coop modules still green.
- Stamina teas join via two tables (RECIPES + STAMINA_RESTORE). drinkBest sorts by restore amount so the elixir auto-becomes the top pick without any per-recipe wiring.
