# Tick 7 — 2026-06-20 23:48 PT

## Batch shipped (5/5)

| # | Slice | SHA | Notes |
|---|---|---|---|
| 1 | farmhouse decor | `78d5e68` | wallpaper + floor cosmetics sold at Pip's cart; renderer retints farmhouse only |
| 2 | spouse move-in | `8527af9` | post-wedding NPC stands south of farmhouse + daily morning gift + private greeting |
| 3 | quest board | `332b335` | notice board sprite at (19,11) south of well; one weekly per season; nine catalogued |
| 4 | seed extractor | `f0fb391` | Maple kit 350g; L recycles 1 harvest -> 1-2 seeds (deterministic 2/1 yield) |
| 5 | friendship tournament | `487425a` | day-6 contest at well 14-18h; flower/fishing/harvest/cook-off; bronze/silver/gold ribbons |

## Quality gate

- `npx tsc --noEmit` — clean.
- `npm test` — 910 / 910 passed across 125 files. (+75 net vs tick 6.)
- `npm run build` — 185.75 kB / 56.90 kB gz.

## New keybind

- `L` — run seed extractor (consumes 1 of the largest harvest stack, grants 1-2 seeds)

## State surface area

New player-state blocks (all forward-compat in persistence):
- `player.decor` — `{ owned: string[], activeWallpaper: string|null, activeFloor: string|null }`
- `player.spouse` — `{ lastGiftDay: number }`
- `player.board` — `{ activeId, postedSeason, postedDay, completedCount, recent[5] }`
- `player.extractor` — `{ uses: number }`
- `player.tournament` — `{ entries: { "<season>-<kind>": { day, tier, score } } }`

## Cross-cutting touches

- `renderer.draw` now accepts an implicit `decorPalette()` lookup and threads `DecorPalette` into `drawBuilding` — farmhouse-only swap.
- `npcs.updateNPCs` consults `world.player.marriage` and overrides the schedule anchor with the spouse anchor when matched.
- Cart catalog auto-extends with `DECOR_CATALOG` rows and `buyFromCart` routes `decor-` keys through `buyDecor` (no inventory item, just ownership unlock).
- Dawn flavorTail chain extended: `tournamentLine` (priority above pip) and `spouseGift` (lowest priority, tail).
- E-press priority at the well: tournament > sell-all when open and not already entered.

## Observation surfaced (NOT a feature this tick)

`SHOP_ITEMS` in `economy.ts` is a complete catalog but the engine never opens a shop modal. Every "Buy from Maple first" toast is a dead end — kits land in the bag only via dev hacks or test bootstrap. This affects every newer feature including this tick's Seed Extractor (kit gated on `EXTRACTOR_INVENTORY_KEY` ownership) and historic features like Coop Kit, Chest Kit, Greenhouse Kit, Auto-Restock Kit, Chickens, Dog/Cat Tickets, Bouquets. Strong recommendation: a dedicated tick to build a shop menu modal (mirror CartMenu's keybind/select/confirm shape — same panel-style UI, smaller catalog). Flagged in STATE OPEN BLOCKERS.

## Roadmap remaining (4 unstarted)

- Carpenter's bench (craft menu for wood + gems -> placeables)
- Outdoor scarecrow (3-tile +1 quality nudge at harvest)
- Animal yield variety (fancy egg occasional 3x; coop tier upgrade)
- Owl post (one-shot send-gift-now service)

Roadmap will need refill in the next 1-2 ticks (cross 5-remaining floor); good time to brainstorm the next 10-15 cozy systems with Sanjay or pull from the wishlist.
