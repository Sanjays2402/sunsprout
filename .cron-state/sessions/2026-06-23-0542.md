# Tick #23 — 2026-06-23 05:42 PT

5/5 shipped. Five clean, revertible commits on `main`:

- `8921f72` refactor(dawn-toast-composer): assembleDawnToast(headline, tails[]) generic helper retires the engine's growing if/then ladder
- `7f80a8f` feat(deep-vein-dawn-brag): one-shot celebratory dawn-toast tail naming the gems/gold of the record run
- `b8e5700` feat(festival-regular-achievement): 22nd badge keyed off tournamentCareer().entries == 4
- `edcb4c9` feat(mine-haul-ribbon-journal-page): per-gem composition breakdown of bestRun under the lore Gems-tab footer
- `353abf9` feat(owl-fluency-tier-color): per-NPC bronze/silver/gold chip alongside the fluency label on lore Folk rows

## Gate

- `npx tsc --noEmit`: PASS
- `npm run build`: PASS (266.48 kB / 81.50 kB gz)
- `npx vitest run`: PASS — 1912/1912 tests across 206 test files (+70 vs last tick's 1842/201)

## Highlights

**The dawn-toast composer refactor was THE landmark of this tick.** Observation #2 from the last two ticks (the engine's chain of `if (tail) headline = ${headline} · ${tail}` ternaries was inching toward 6 branches) is now RETIRED. The engine's day-rollover branch is one helper call, four tails passed by argument. The deep-vein dawn brag landed on the new infrastructure THIS SAME TICK, validating the refactor immediately — the brag is the 4th tail in the array. Future tails (deep-vein composition recall, split-record brag, the roadmap idea pile) drop in for free as one-line array pushes.

**Composer / composer / chip pattern.** Three of this tick's five features ship via small pure-helper-with-color-output composers that the panel UI consumes:
1. `assembleDawnToast` for the engine's day-rollover branch
2. `loreTabDetailLine` for the panel footer's secondary line
3. `owlFluencyTierColor` for the row's tier chip

Each helper has a single integration point, returns null/empty when there's no signal to surface, and leaves the consumer trivially safe. The pattern keeps the engine + panel UIs thin and pushes complexity into focused, individually-testable modules.

**Lazy-ledger factory extension with ribbon-composition twist.** MineHaulState.bestRun gained two NEW optional fields (countComposition + goldComposition) — the first time a single-field-on-bestRun got a same-record SCHEMA expansion rather than a separate ledger. The composition is captured at the SAME sleep-time site that promotes the aggregate count/gold; persistence deep-copies the new maps so the snapshot doesn't share refs.

**Festival Regular tuning was a happy discovery.** Initial design proposed FESTIVAL_REGULAR_MILESTONE = 12 (one full year of seasonal tournaments), but the entries map's key shape (`${season}-${kind}`) caps at 4 — enterTournament returns 'already-entered' on a re-press in the same slot, even on year 2. So the tuned value is exactly 4: "you've shown up to every seasonal tournament at least once". The unreachable-vs-natural-ceiling caught early via the data model — a higher number would never light up. The achievement is intentionally orthogonal to tier (no-prize entries count).

**Owl fluency surface now spans THREE layers.** Per-NPC text on the lore Folk row description (tier wording), per-NPC price chip on the owl-menu row (cost + savings), and now per-NPC color chip on the lore Folk row (bronze/silver/gold dot). The full progression now reads as a single coherent visual language across two separate UI surfaces. Roadmap idea "tier-color chip on the owl-menu" is starting to feel like the natural fourth corner.

## Observations refreshed

1. **Zero new keybinds this tick — eighth in a row.** Two digits (9, 0) still free. The keyboard inventory is approaching capacity but two digits remain. Worth pondering whether the next tick should DELIBERATELY introduce a new keybind for something that warrants it (tournament leaderboard panel?) rather than continuing to ride existing surfaces forever.
2. **Dawn-toast tail chain refactor RETIRED.** Observation #2 from the last two ticks is permanently gone. The engine's day-rollover branch is one helper call.
3. **Lazy-ledger factory extended with composition.** ~40 min / ~120 LOC / +15-20 tests for a base ribbon feature; composition adds ~20 LOC + 5 tests on top.
4. **Composer / chip pattern dominant.** Three of five features ship via small pure helpers consumed by thin UI layers.
5. **Owl-post fluency surface now three-layer.** Text + price chip + color chip; tier-color chip on the owl-menu would close the loop.

## Files touched

- `src/game/dawn-toast.ts` (NEW)
- `src/game/mining-haul.ts` (composition fields on bestRun, dawn brag, captured at resetMineHaul)
- `src/game/persistence.ts` (round-trip composition + deep-vein brag flags)
- `src/game/achievements.ts` (festival-regular row + import)
- `src/game/tournament.ts` (FESTIVAL_REGULAR_MILESTONE + predicate)
- `src/game/lore.ts` (loreTabDetailLine + LoreRow.tierColor + buildLoreRows owl-fluency wiring)
- `src/game/owl-post.ts` (OWL_FLUENCY_TIER_COLOR palette + owlFluencyTierColor helper)
- `src/engine/game.ts` (import assembleDawnToast + deepVeinDawnBrag, swap ladder for composer)
- `src/ui/lore-panel.ts` (tierColor draw, detail-line draw with conditional height bump)
- `tests/dawn-toast.test.ts` (NEW, 13 tests)
- `tests/deep-vein-dawn-brag.test.ts` (NEW, 15 tests)
- `tests/festival-regular-achievement.test.ts` (NEW, 13 tests)
- `tests/mine-haul-ribbon-journal-page.test.ts` (NEW, 19 tests)
- `tests/owl-fluency-tier-color.test.ts` (NEW, 12 tests)
- `tests/mining-haul-ribbon.test.ts` (relaxed strict toEqual to toMatchObject)

## Push verified

```
353abf9 (origin/main, HEAD -> main) feat(owl-fluency-tier-color): ...
edcb4c9 feat(mine-haul-ribbon-journal-page): ...
b8e5700 feat(festival-regular-achievement): ...
7f80a8f feat(deep-vein-dawn-brag): ...
8921f72 refactor(dawn-toast-composer): ...
b4ad725 chore(autoship): tick log + session notes for 2026-06-23 02:10 PT batch
```

`b4ad725..353abf9  main -> main` — clean fast-forward, no force, no merge commit.
