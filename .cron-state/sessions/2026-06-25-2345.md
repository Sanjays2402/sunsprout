# Tick #37 — 2026-06-25 23:45 PT

**Mode:** 20-minute autonomous BATCH (target 5, gate once). FIFTH consecutive FRONTEND-FOCUS tick (Sanjay's standing override). CLEAN batch — not a resume.

**Theme:** "Home, hearts & a warm welcome — the cozy meta layer." Where #33-#36 built HUD-info and world-ambience surfaces, this tick deepened the relationship / home / first-impression layer: the relationships panel, the morning sleep recap, first-run onboarding, minimap wayfinding caption, and farmhouse-wall trophies.

## Shipped (5/5)

1. **relationship-panel** (58e4319) — Rework the `H` panel from a bare name+heart-strip list into a real relationships screen. New pure `relationshipRows(player, time)` (shared with tests) carries per-candidate hearts, a next-birthday countdown (`birthdayCountdownLabel`), a "loves ..." gift hint (`prettyGiftKey`, up to 2 adored gifts), and an engaged/married status chip (`statusChipLabel`), sorted by closeness (married > engaged > hearts desc > soonest birthday > name). `drawHeartsPanel` threads `this.time` at the one call site; legacy `heartsSummary` kept for back-compat. +10 tests.

2. **day-summary** (ea77e44) — Pure `game/day-summary.ts`: `summaryRows()` builds tinted+categorised recap rows (gold/harvest/dishes/hearts; `SUMMARY_ROW_COLOR` mirrors the toast colour-rail palette) and `bestMomentLine()` names the slept day's standout (heart level-up > strong gold `GOLD_HIGHLIGHT=100` > heavy harvest `HARVEST_HIGHLIGHT=6` > dishes > null). The sleep-summary overlay draws a small pixel glyph per category (coin/sheaf/bowl/heart) and the highlight in its own soft band. Absorbed the separate "Sleep-summary kind tints" roadmap item. +9 tests.

3. **onboarding** (6afca1f) — `game/onboarding.ts` holds the one-time welcome content + a "seen" flag in its OWN localStorage key `sunsprout.onboarding.v1` (survives a Settings > Reset Save, no save-schema churn). `ui/onboarding-card.ts` renders rounded key-caps pointing at WASD/E/?/9/0. `game.ts` opens it on first boot (`shouldShowOnboarding`), dismisses on any key after a 220ms open lockout while swallowing that frame's input (`markOnboardingSeen`), and draws it above the whole HUD. Gating tested incl. null/throwing storage. +6 tests.

4. **minimap-focus** (6ffc050) — Pure `cycleFocusIndex()` (modular wrap, empty-safe) + `focusedLandmarkCaption()` (round fractional building centres to tiles, null when nothing to focus). `MinimapPanel` gained a `focusIndex` cursor + `cycleFocus(world, delta)`; arrow keys / WASD cycle a focused landmark while the map is open, a caption row names it + its tile ("Maple's shop  -  tile 25, 9"), and a steady highlight ring anchors it on the map. The minimap was added to the `blocked` movement guard since it now consumes WASD; focus clears on close. +7 tests.

5. **ribbon-hall** (d21a9f2) — Pure `game/ribbon-hall.ts` `ribbonHallMounts()` derives a farmhouse-wall rosette layout from `ribbonCounts()` (best tier first, even spacing, capped at 3 per tier, empty on a fresh farm). `render/ribbon-hall-sprite.ts` paints each as a pixel medal with two ribbon tails + a sheen. The engine hangs the strip on the wall right of the door, reusing `drawBuilding`'s wall geometry. Tournament wins are now visible trophies. +6 tests.

## Gate (run ONCE at end of batch)

- `npx tsc --noEmit` — clean (also typechecked after each slice).
- `npx vitest run` — **2456/2456 passing** (+38 this batch).
- `npm run build` — green, **310.47 kB / 94.84 kB gz** (was 302.23 / 92.49).

## Push

origin/main was at #36 (d351d61); pushed `d351d61..d21a9f2` (5 commits). Verified landed via `git fetch -q origin && git log --oneline origin/main`.

## Notes for next tick

- **Override still active** — 5 straight FRONTEND ticks. Roadmap still rich (~13 unstarted incl. 4 fresh ideas added: relationship gift-readiness chip, almanac personal-milestone row, day-summary streak line, ribbon-hall aria caption).
- **Refactor pressure building (obs #4):** drawBuilding's stepped-triangle geometry is now copied 3x (roof apex: chimney-smoke, shop-banner) + 2x (wall top: chimney-smoke, ribbon-hall) across the engine render block. Extract a `buildingGeometry(building, camera)` helper before a 6th roof/wall-anchored overlay lands.
- **Movement-guard invariant (obs #5):** any future info-panel that consumes directional keys MUST be added to the `blocked` list (game.ts ~L1080), like the minimap now is.
- **Device-level state pattern (obs #6):** one-time/device preferences go in their own storage key (like `sunsprout.onboarding.v1` / the peerIdentity key), NOT the save snapshot, so they survive a save reset and don't bloat the schema.
