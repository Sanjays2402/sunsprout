# Sunsprout autoship session — 2026-06-21 03:12 PT

Tick #8. Cake (cron) on `main`.

## Shipped (5/5)

1. **shop (0fcb284)** — Maple's shop modal closes the dev-console-only kit-buy gap. Tabbed (Seeds / Kits / Tickets / Misc) catalog built from SHOP_ITEMS; singletons drop off the list once owned. Up/Down picks rows, Tab + Left/Right flip categories, Enter buys, Esc leaves. Money log tags: `shop: ...`.

2. **bench (70e3e88)** — Carpenter's bench fixture at (22, 9) in the village square. Three recipes (scarecrow / brass lantern / stone fence) turn gold + mid-tier gems into placeables. Bench sprite: workbench top, two legs, a saw, plank stack. Money log tags: `bench: ...`.

3. **scarecrow (48806fb)** — Place crafted scarecrows ({ keybind) on grass; crops within Chebyshev radius 3 ripen one tier higher at harvest. The boost applies INSIDE `farming.harvest()` so the streak-derived quality math stays untouched. Persistence forward-compat (scarecrows[] defaults to []).

4. **fancy-eggs (1c41787)** — Each chicken rolls a deterministic per-(coop, day, idx) hash each dawn; basic coops yield 8% fancy eggs, deluxe coops 18%. Fancy eggs sell at 3x. Deluxe upgrade kit (`craft-coop-deluxe`, 700g + 2 iron) crafted at the bench, applied with `}` to an adjacent coop.

5. **owl-post (e93a9b5)** — Press `~` near the farmhouse mailbox to summon the village owl. Pay 40g to dispatch the player's best auto-picked gift to the chosen candidate. Reuses `attemptAutoGift()` so all guardrails (per-day gate, no-disliked, taste ranking) hold. Fee deducts only on confirmed delivery.

## Quality gate

- `npx tsc --noEmit` — green.
- `npm run build` — clean, 207.24 kB / 62.07 kB gz (up 11.5% from 185.75 / 56.90).
- `npm test` — 981/981 green (+71 this tick).

## Workflow change

This is the first tick on the new "commit directly to main" workflow. Previous ticks used `feature/autoship`; that branch was merged in commit 1ef1e5e and is now retired. The end-of-batch quality gate is what protects main.

## Open observations

- Keybind shortage: this tick added 3 single-char hotkeys (`{` / `}` / `~`). The only free chars left are digits 0/2/3/4/6/7/8/9 (hotbar conflicts), `<`, `>`, `?`. Future panels should share a single panel-cycle key.
- CoopSnapshot interface in `persistence.ts` doesn't formally declare `tier` / `fancyEggs` but they round-trip via the existing `{ ...c }` spread. Worth a future tick to formalize the schema.

## Reported to Telegram

5 features shipped, 981 tests green, build 207k/62k gz, pushed to origin/main.
