# Session 2026-06-26 21:25 PT — Tick #41

**FRONTEND-FOCUS, 9th straight. CLEAN batch (not a resume).**
origin/main + local main both started at #40 (51d4ae7). Shipped 5 fresh
slices, gated once, pushed 157a05e..5dd6003.

Theme: **"panels you can scan, filter, and navigate"** — took the
info-panel family one layer deeper into legibility.

## Shipped (5/5)

1. **bag-empty-state** (157a05e) — `BAG_EMPTY_STATES` registry +
   `bagEmptyState()` in game/panel-empty.ts (total over BagCategory). The
   bag panel drops its hand-rolled `emptyLineFor()` switch + the
   now-orphaned `DIM` const for the shared two-line `drawEmptyState`
   widget, so all panels speak ONE empty-state dialect. Each tab points at
   the verb that fills it (buy seeds / cast / mine / forage / cook).
   +4 tests.

2. **money-log-rail** (5b30b9e) — pure `classifyMoneyEntry()` buckets each
   ledger row sale/purchase/reward off the signed delta + START-anchored
   reason patterns: negative = purchase; `^well:`/`^inn:`/`^fishing
   `/`^mining `/`^cart: breeder trade` = sale; else reward. The anchoring
   keeps "cart: rumor rebate" + "cart: rumor streak" as rewards, not the
   breeder-trade sale. 3px left rail per row tinted sale=green /
   reward=violet / purchase=amber (mirrors the toast colour-rail); day
   stamp + reason nudged right to clear it. +4 tests.

3. **journal-harvest-bar** (6616834) — `maxLifetimeHarvest()` shared
   denominator + `harvestBarSegments()` largest-remainder normal/silver/
   gold split that always sums exactly to the bar length; a non-zero tier
   is guaranteed >=1px (stolen from the widest segment) so a lone gold
   star never vanishes in a big normal pile. Faint full-width track +
   tinted segments in each row's bottom-right clear space (BAR_W 84 /
   BAR_H 5, well clear of the left-anchored ribbon). +6 tests.

4. **almanac-filter** (5dd57c4) — `AlmanacFilter` all/village/birthdays/
   personal COMPLETE cycle (village = festival+cart+tournament).
   `nextAlmanacFilter` + `almanacFilterLabel` + `applyAlmanacFilter` pure;
   the 3 non-all filters PARTITION the kind space exactly (asserted).
   Panel resets to 'all' on open, `f` cycles, violet "filter: village
   (3/7) - f to cycle" chip (visible even on 'all'), filter-emptied list
   ("Nothing village in the next two weeks.") distinguished from a quiet
   calendar. `FOOTER_H` band added for the chip. Engine wires `f` while
   the `0` panel is open + guards the global fishing `f` against the
   almanac (same shape as the lore guard). Controls catalog notes
   "(f to filter)". +12 tests (new almanac-filter.test.ts).

5. **codex-sections** (5dd6003) — `codexSections()` groups buildCodex rows
   into Cooked / Ready / Undiscovered divider sections (cooked->known->
   locked, empty omitted) via the almanac-sections pattern. Panel draw
   refactored into `drawRow` + `drawSectionHeader` helpers; SECTION_H
   folded into the height calc. SHIPPED as SECTIONS not tabs because the
   codex is a non-blocking read-while-walking overlay where a/d would
   fight player movement. +5 tests.

## Gate (once, at the end)
- `npx tsc --noEmit` — clean (also after each slice)
- `npx vitest run` — **2599/2599 green** (+31 from baseline 2568)
- `npm run build` — 331.87 kB / 101.78 kB gz, 178 modules (UNCHANGED
  module count — every slice extended an existing module, no new files)

## Push
- 51d4ae7..5dd6003 -> origin/main, verified local == origin == 5dd6003.

## Notes / lessons
- **Non-blocking overlays can't take a/d tab nav** (obs #4): the bag is
  movement-blocking so it owns a/d + `f`; codex/achievements/journal/money/
  hearts are non-blocking read-while-walking floating overlays, so a/d
  there fights movement. Codex shipped as discovery SECTIONS instead of
  tabs. For a panel-local `f` on a non-blocking overlay (almanac filter),
  guard the global fishing `f` against the panel being open.
- **Shared-divider pattern is the new workhorse** (obs #5): almanacSections
  (tick #40) seeded the `xSections(rows)` shape; #41 used it twice
  (codexSections + the journal mini-bar is a cousin). Refill #4 queues
  achievements + quest-log + money-log day-groups as the next consumers.
- **Anchored-regex reason classification** (obs #6): money-log leans on
  the sign + START-anchored reason patterns; the anchor is load-bearing
  for the cart rebate-vs-trade disambiguation.
- **patch fuzzy-match mangled two test files** (obs #3) when reinserting a
  describe block adjacent to an existing one — it deleted the wrong it()
  bodies + left a dangling brace. Caught both via tsc + re-read before
  gating. Lesson: anchor adjacent-block inserts on a UNIQUE preceding
  line, not a multi-line old_string spanning the existing block.
- Refill #3 ran down to 3 open items; added a fresh **refill #4** (15
  frontend items). Override still active — keep picking frontend.
