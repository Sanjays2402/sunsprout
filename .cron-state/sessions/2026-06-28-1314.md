# Tick #49 — 2026-06-28 13:14 PT

**FRONTEND-FOCUS, 17th straight. CLEAN batch (not a resume).**
origin/main + local main both at #48 (021c56f) at start; shipped 5 fresh
slices 379d18b..d33cf41, gated once, pushed. 220 features now on main.

## Theme: "panel glyphs carry meaning, not just decoration"

The info-panel family already had glyphs/dividers/filters/digests; this tick
made the GLYPHS carry information — colour-coded by type, gauged to a scale,
named at their extremes, warning when dangerous.

## Shipped (5/5)

1. **quest-reward-glyph-tint** (379d18b) — `rewardGlyphColor(kind, done)` +
   `REWARD_GLYPH_COLOR` (coin gold / crate green / star violet, the toast-rail
   palette) + `REWARD_GLYPH_DONE_COLOR`. The quest-log panel threads the
   per-kind colour into `drawRewardGlyph` so the reward TYPE scans by hue while
   active; a completed quest drops every pip to one dim tint. Reward TEXT keeps
   the row earned/active tint. +5 tests.

2. **bag-count-bar** (f22dd82) — `maxBagRowCount(rows)` + `bagCountBarWidth(
   count, max, width)` shared-scale gauge (mirrors the crop-journal harvest
   mini-bar; non-zero >=1px, fullest fills the track, clamped against
   overflow). A lilac fill under each bag row's `x{count}` sized to the fullest
   stack in the CURRENTLY shown slice (active tab or `/` search matches). +9.

3. **almanac-kind-glyph** (2caf25d) — `almanacKindGlyph(kind)` pure 5x5
   `[col,row]` bitmap per kind (cake / tent / cart / rosette / heart). Panel
   paints each cell in the kind rail colour where the B/F/P/T/M letter tag sat;
   title column unshifted. Model owns the shape, panel owns the pixels (same
   split as shop-banner / bag-glyph). +5.

4. **money-log-sparkline-extremes** (49b5df5) — `purseSparklineExtremes(player)`
   walks the same chronological balance series `purseSparkline` plots and
   reports the high+low vertices (normalised x/y matching the points + raw
   gold value, first-occurrence tie-break, null on <2 rows). Panel draws a
   bright GAIN-green peak pip + a dim LOSS-red low pip; the newest-endpoint dot
   draws LAST so it sits on top of any coincident extreme. +6.

5. **settings-reset-danger-glyph** (d33cf41) — `settingsRowPreview` gains a
   discriminated `danger` descriptor carrying `resetArmed` (optional arg
   defaults false, non-armed callers byte-identical). `drawValuePreview` paints
   a warning triangle + bang, dim while idle -> CONFIRM_BORDER red once the
   two-step confirm is armed. Existing `reset -> 'none'` test updated to
   `danger` + a new armed-draw smoke test. +3 net.

## Gate (once, at the end)

- `npx tsc --noEmit` — exit 0
- `npx vitest run` — 2869/2869 green (+28 from baseline 2841)
- `npm run build` — 358.00 kB / 109.01 kB gz, 179 modules (UNCHANGED — every
  slice extended an existing module; FOUR new test files + 1 extended
  settings-value-preview, no new source files)

## Notes / lessons

- ZERO new keybinds — every slice rode an existing panel surface.
- STRUCTURAL-EDIT LSP PHANTOMS RE-CONFIRMED (obs #3): a patch restoring the
  deleted `whenLabel`/`dateLabel` bodies in almanac.ts threw a phantom
  `'}' expected`; the `rewardGlyphColor` import threw `Cannot find name`. Both
  cleared by `npx tsc --noEmit` exit 0. Trust tsc, not the LSP flood. ALSO: my
  first almanac patch accidentally clobbered the `whenLabel`/`dateLabel` bodies
  (the fuzzy match ate the function interiors) — caught on re-read, restored in
  the next patch. Re-read after every structural patch.
- Refill #10 ran down to 2 open (panel-CLOSE-fade + hearts-into-fade-family);
  added refill #11 (8 items). The crop-journal harvest-bar count-tag item was
  marked MARGINAL/skip — the row already prints n/s/g as text above the bar.
- The two most-aged debt items (panel-CLOSE-fade, hearts-into-fade-family) are
  the oldest carryover and worth picking next tick if a clean approach lands.
