# Tick 2026-06-21 12:26 PT — autoship batch session notes

Shipped 5/5 features. All committed directly to `main`, batch pushed at end of tick.

## Features
1. **hatchery** (78b0cf6) — `src/game/hatchery.ts` + bench recipe + persistence. 1-tile basket placed next to a coop. `6` loads a fancy egg → 5-day incubation → chick auto-routes to nearest free coop. pendingChicken when every coop is full. 20 new tests.
2. **compost** (8ea90a6) — `src/game/compost.ts` + Maple's shop + persistence. 140g 1-tile placeable. Context-sensitive `7` key: deposit normal-tier harvests / apply fertilizer / place. floor(crops/4) bags after 3 days; +2 waterStreak per bag. Silver+gold harvests stay in bag. 18 new tests.
3. **npc-barks** (f4e7957) — `src/game/npc-barks.ts`. Per-frame proximity check (within 2 tiles) fires a deterministic one-liner above the NPC's head; per-(day,hour) dedup + 12s per-NPC cooldown. 4-5 lines per NPC. Bubble fades after 3s. 10 new tests.
4. **bath-winter** (17d6494) — `src/game/bath-house.ts` extension. `bathPriceFor(time)` returns BATH_FEE * 0.75 in Winter. Outcome gained `pricePaid` + `discounted`. Money log + flavor line distinguish the rate. 12 new tests; existing 16 still green.
5. **storm-shelter** (2f29045) — `src/game/storm-shelter.ts` + bench recipe + storm.ts wiring + persistence. 400g+1iron one-shot lean-to; protects Chebyshev-1 crops from the next storm; consumed on impact. stormFlavorLine reports sheltered + consumed counts. 18 new tests; existing storm tests still green.

## Quality gate
- `npx tsc --noEmit`: clean
- `npm run build`: 232.09 kB / 70.12 kB gz (was 218.94 / 66.23 last tick)
- `npm test`: 1189/1189 (+78 from 1111)
- One pre-existing flaky test (weekday-market discountedPrice) fixed: assumed (0,1) rotation wouldn't pick wheat seed; compost-bin catalog addition shifted the rotation. Test now scans for any >=5g featured row.

## New keybinds
- `6` — hatchery (context: load egg / claim chick / place)
- `7` — compost (context: deposit / apply / place)
- `8` — storm shelter (place)

## New bench recipes
- Hatchery Basket — 220g + 2 copper
- Storm Shelter — 400g + 1 iron

## New Maple shop kit
- Compost Bin — 140g

## Schema additions
- `BathOutcome.soaked` gained `pricePaid` + `discounted` (required) — one inline test fixture updated.
- `StormState.lastMemo` + `StormOutcome.fired` gained optional `cropsSheltered` + `consumedShelters`.
- `SaveSnapshot.world` gained optional `hatcheries`, `composts`, `shelters` arrays. All forward-compat (empty default for older saves).

## Notes for next tick
- Letter keys are saturated. Digit row now has `1-8` taken; `9` and `0` are still free.
- The compost loop's `7` key is heavily context-sensitive — future composting features (rare fertilizer roll, week-long batch, etc.) should layer on the same key rather than claim a new one.
- The hatchery's `6` is similarly multimodal — placing / loading / claiming all flow through one keypress.
- Storm shelter currently is consumed on the storm even if it covers crops with `waterStreak === 0` (i.e. crops it didn't really protect). Worth a follow-up: only consume when the shelter actually saved a non-trivial streak day.
