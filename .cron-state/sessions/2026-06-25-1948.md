# Session 2026-06-25 19:48 PT — Tick #36 (FRONTEND-FOCUS, 4th straight, a RESUME)

## Theme: "a world that feels alive — calmly"

Every slice either adds ambient/world life or completes the reduce-motion
calm-mode audit, so the new liveliness never fights accessibility.

## What happened: a resume

Walked in to find `origin/main` at #35 (abf1091, the toast-kind-tint ->
ping-legend "legible & accessible" batch) but **local `main` was +2 ahead**.
A prior run (#36a) had been interrupted mid-batch: it committed two solid,
fully-tested frontend slices to `main` locally but never pushed them and
never reconciled STATE.md:

- `7fcc8f5` crop-sway (gentle wind on grown crop canopies, +12 tests)
- `cb6f1d5` confetti (festival/birthday dawn burst, +8 tests)

Both were on the correct cron identity, typecheck-green, test-green. The
right call was to **adopt** them as slices 1-2 of this batch rather than
revert or redo, then ship 3 fresh slices to reach a clean 5.

## 5 slices shipped (all on main, pushed abf1091..7ca5a24)

1. **crop-sway** (7fcc8f5, adopted) — pure `game/crop-sway.ts`; sub-2px
   sine canopy lean for mid/ripe crops; per-tile phase so the field
   ripples not marches; reduce-motion + seed/sprout stages return flat 0.
   `renderer.draw` threads `nowMs`+`reduceMotion` (defaulted, back-compat).

2. **confetti** (cb6f1d5, adopted) — pure `game/confetti.ts`; deterministic
   ~2.6s pixel burst on the dawn of a festival/birthday; engine diffs
   `celebrationDayKey` frame-to-frame to arm one burst on arrival; skipped
   wholesale under reduce-motion like rain/snow.

3. **toast-reduce-motion** (7152d61, fresh) — pure
   `toastAlphaFor(entry, reduceMotion)`; under calm mode the pills hold
   full opacity their whole life then cut over `TOAST_CUT_MS`=80ms instead
   of the 600ms fade. The **last** animated HUD surface that ignored
   reduceMotion — the calm-mode audit for the HUD is now complete. +8 tests.

4. **chimney-smoke** (672da0d, fresh) — pure `game/chimney-smoke.ts`.
   Responsive hearth: `hearthLit()` lights the wisp only in Winter, the
   cold dark hours (<8h or >=18h), or when the weather waters crops
   (rain/storm). `chimneySmoke()` particle column rises + drifts + fades;
   engine derives the roof apex from `drawBuilding` geometry. Reduce-motion
   returns a frozen 3-puff thread so the home still reads as lit. +13 tests.

5. **shop-banner** (7ca5a24, fresh) — pure `game/shop-banner.ts` owns the
   per-season `ShopBannerStyle` (palette + 5x5 symmetric motif bitmap +
   label); `render/shop-banner-sprite.ts` is the thin draw layer (striped
   cloth on a cross-beam). Engine hangs it over Maple's shop roofline keyed
   off `time.season`; out-of-range seasons wrap safely. The village finally
   notices the calendar. +8 tests.

## Gate (once, at the end)
- `npx tsc --noEmit` — clean
- `npm test` — 2418/2418 green (+49 vs the #35 baseline of 2369)
- `npm run build` — 302.23 kB / 92.49 kB gz

## Notes for next tick
- **Resume protocol worked**: always `git log --oneline origin/main...main`
  first. If local is ahead of origin with green work, fold it into the
  current batch and only ship the remainder needed to reach 5.
- **Calm-mode HUD audit is DONE.** The one remaining idea-list item
  ("stamina bar reduce-motion") is speculative — the stamina bar has no
  animation today, nothing to gate. New animated cues must take a
  reduceMotion arg from birth.
- **New pattern: responsive ambient FX.** chimney-smoke gates on sim state
  (`hearthLit`) rather than rendering unconditionally. Future ambient
  layers (night lamplight, shop-window glow, festival bunting) should do
  the same so the world feels reactive, not painted-on.
- **Tech-debt flag:** roof-apex geometry
  (`sy + floor(ph*0.4) + 2 - floor(ph*0.55)`) is now duplicated in 2 engine
  blocks (chimney + banner), copied from `drawBuilding`'s private math.
  Extract a `buildingRoofApex(building, camera)` helper if a 3rd
  roof-anchored overlay lands.
- FRONTEND OVERRIDE still active — 4 straight frontend batches. Roadmap
  still rich (~14 unstarted frontend items). Good next picks: inventory/bag
  panel, day-summary sleep overlay polish, sleep-summary kind tints,
  onboarding first-run hint card, tournament ribbon hall.
