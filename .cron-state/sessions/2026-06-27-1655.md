# Session 2026-06-27 16:55 PT — Tick #45 (FRONTEND-FOCUS, 13th straight)

## Outcome
5/5 shipped. CLEAN batch (no resume): origin/main + local main both at #44
(f93d9a3) on entry. Shipped 6936174..70fdb36, gated once, pushed to
origin/main. 2741/2741 tests green (+45). Build 345.61 kB / 105.52 kB gz,
178 modules (unchanged — every slice extended an existing module).

## Theme
"The last panels learn to filter + look past their own edges." Extended the
proven panel-local `f`-filter pattern to the two remaining flat read panels
(codex, quest-log), gave the lore panel a completion digest header, taught
the almanac to peek past its 14-day horizon, gave the hearts panel a sort
toggle. All 5 picked from refill #6 of the FRONTEND-FIRST roadmap.

## Slices (one commit each)
1. **recipe-codex-filter** (6936174) — RecipeCodexFilter all/cooked/ready/
   undiscovered + CODEX_FILTERS + cycleCodexFilter + codexFilterLabel +
   applyCodexFilter (pure, isolates one RecipeDiscovery; 'all' = fresh copy,
   catalog order preserved). Panel: filter resets to 'all' on open, narrows
   rows+sections, header tally stays whole-book, filter chip, "no X recipes
   - press f" note. game.ts: R-open routes `f`, codex joins fishing-`f`
   guard. tests/recipe-codex-filter.test.ts (+11).
2. **quest-log-filter** (1863477) — QuestFilter all/active/done +
   applyQuestFilter. Panel: displayItems feeds questLogSections the FILTERED
   rows (fixed-BODY_H scroll sees fewer items), header counts + %-summary
   stay whole-board, filter chip, filtered-empty note distinct from
   truly-empty. game.ts: `f` + guard. tests/quest-log-filter.test.ts (+10).
3. **lore-completion-summary** (f503743) — loreCompletionSummary()/Line()
   over loreProgress: "discovered 18/40 (45%) - 2 tabs complete", Rumors
   excluded. Replaces the bare "N% discovered" stat in the same header slot,
   no layout change. tests/lore-completion-summary.test.ts (+9).
4. **almanac-look-ahead** (f5e5e51) — almanacLookAhead(time, filter='all',
   player?) scans the full 28-day wrap for the soonest entry PAST the 14-day
   horizon matching the filter + almanacLookAheadLine() "next: ... in N
   days". Panel empty branch draws it. tests/almanac-look-ahead.test.ts (+7).
5. **relationship-sort-toggle** (70fdb36) — RelationshipSortMode
   closeness|birthday + sortRelationshipRows() returns a re-sorted COPY for
   DISPLAY. drawHeartsPanel computes the summary from the CANONICAL closeness
   order then displays the sorted copy. Sort chip + game.ts heartsSortMode +
   `f` cycle + guard. tests/relationship-sort.test.ts (+8).

## Notes / lessons
- The panel-local `f`-filter is now a SIX-panel family; the fishing-`f`
  guard chain is six clauses deep. Shape fully settled — see obs #4.
- Honest-digest invariant held everywhere: header digest over the WHOLE
  list, body shows the filtered slice. Sharpest case = relationship-sort,
  where a SEPARATE display sort preserves the canonical builder order the
  summary reads (obs #5).
- Dead-code check before slice 4: scripted that the unfiltered 14-day
  almanac is NEVER empty (cart every 7 days), so made almanacLookAhead
  filter-AWARE to earn its keep on a filtered empty (obs #6).
- Stale-LSP-echo fired 3x (loreCompletion swap, almanacLookAhead signature
  widen mid-sequence, SORT_CHIP const) — all stale, tsc exit 0 each time.
- Refill #6 ran to 3 open items → added refill #7 (8 items). Settings-chrome
  + panel-transition are the most-aged debt — flagged to pick next.
