# Session 2026-06-22-0900

5/5 shipped — 17th batch tick, 85 features now on `main`.

## Features

1. **mine-haul-achievement (71b0f71)** — `MineHaulState.lifetimeCounts: Partial<Record<GemKey, number>>` is the new career counter that NEVER resets on sleep (the per-run `counts` still does). `recordMined` bumps both maps on every strike. `lifetimeHaulCount` + `lifetimeHaulGold` + `lifetimeMiningMilestoneReached` pure helpers. `LIFETIME_MINING_MILESTONE = 100`. New `cave-veteran` achievement in the catalog (15 → 16 badges) wired off `lifetimeMiningMilestoneReached(getMineHaul(p))`. Persistence carries the field; older saves load with `{}` and accrue from there.
2. **lore-rumors-tab (66fdd82)** — `LORE_CATEGORIES` gains `'Rumors'` at the tail (Fish/Gems/Forage/Crops/Folk/Rumors). `buildLoreRows` walks `getRumorHistory(player).entries` newest-first; each row is "discovered" because Pip has explicitly teased it. The description carries the bought/skipped state. `loreCompletion` explicitly filters Rumors out so the bestiary % is unaffected by the visit log. `loreProgress` buckets Rumors separately so the tab still surfaces its own per-tab count.
3. **compost-recycle (68ff1f3)** — `ApplyOutcome 'applied'` branch widened with `recycledGold`. `COMPOST_RECYCLE_REGULAR = 1g`, `COMPOST_RECYCLE_RARE = 3g`. `applyFertilizer` credits `player.gold` directly when the gold field exists (legacy fixture compat preserved). Engine: 7-press apply path now `logGold(p, recycledGold, 'compost recycle', day)` + "(+Ng recycled)" tail on the existing toast.
4. **breeder-cookbook (0811dc9)** — `PREMIUM_SELL_MULTIPLIER = 1.5`. `recipeHasEgg` predicate, `canCookPremium` + `cookPremium` swap one breeder egg for one regular egg in any egg-bearing recipe (omelet / pumpkin custard / mushroom skillet / berry tart / hot cocoa / sunflower elixir). Premium dish lives in `dish-<key>-premium` (parallel inventory key). `sellAllDishes` bundles both regular and premium stacks. `premiumCookLine` codex hook ("Premium (breeder egg swap): Xg (+Yg)").
5. **double-batch (d64a422)** — `DOUBLE_BATCH_INGREDIENT_MULT = 2`, `DOUBLE_BATCH_DISH_YIELD = 3`. `STAMINA_TEA_KEYS` canonical set kept in sync with `STAMINA_RESTORE` (a test asserts the two never drift). `canCookDoubleBatch` + `cookDoubleBatch` gated to the five stamina teas. The bonus dish piles into the same `dish-<key>` slot so the drinkBest path is unaware. `doubleBatchLine` codex formatter ("Double batch: 2x ingredients -> 3 dishes (saves Yg/dish in raw cost).").

## Tests

+67 this batch:
- `mining-haul-lifetime.test.ts` — 10 tests (zero defaults / parallel run+lifetime bump / reset-doesn't-clear-lifetime / lifetimeHaulGold sum / threshold flip / older-save backfill / catalog shape / below threshold / unlock / reset-survival + idempotent re-tick)
- `lore-rumors.test.ts` — 9 tests (category registration / empty default / per-entry capture / newest-first ordering / bought-vs-skipped description / count semantics / completion exclusion / per-tab progress / row-id uniqueness)
- `compost-recycle.test.ts` — 10 tests (constant sanity / regular recycle outcome / rare recycle outcome / gold credit / rare gold credit / no-credit on failure / no-credit on no-bags / multi-apply stacking / rare-then-regular sequence / gold-less legacy player)
- `cooking-breeder-premium.test.ts` — 18 tests (recipeHasEgg coverage / premiumDishInventoryKey + premiumSellPrice integrity / multiplier intent / canCookPremium gating across egg-count axis / cookPremium consumption + output + refusal / sellAllDishes integration / premiumDishesValue / premiumCookLine formatter)
- `cooking-double-batch.test.ts` — 14 tests (STAMINA_TEA_KEYS membership / sync with stamina.ts / isStaminaTea coverage / canCookDoubleBatch gating / cookDoubleBatch 3x mint / same dish-key bonus / failure semantics / non-tea refusal / regular-vs-double cost comparison / back-to-back batches / formatter wording / multiplier sanity)

Plus +6 incidental tests in already-existing files (existing achievement / cooking / compost suites stayed green; the 1512 total reflects vitest's net count after the +67 here).

## Quality gate

```
npx tsc --noEmit                                      0 errors
npm run build      dist/assets/index-CgnYJjH2.js     250.14 kB / 76.40 kB gz
npm test           Tests  1512 passed (1512)         176 files
```

## Push

`1cea397..d64a422  main -> main` — 5 feature commits + this session note as a separate chore commit.

## Notes

- **Parallel dish-key pattern established.** `dish-<key>-premium` is the first sibling beneath the existing `dish-<key>`. The only consumer that needed widening was `sellAllDishes` — it now walks both keys separately. cook() still mints only the regular key; cookPremium mints only the premium key. If a future tick adds a third variant (e.g. `dish-<key>-discount`) the pattern stays clean: one new mint site, one new walk in sellAllDishes. **Recorded as observation #3 in STATE.md.**
- **Cooking module is getting fat.** cooking.ts is now ~590 lines. The natural split point is `cooking.ts` (recipes + cook()) vs `cooking-variants.ts` (canCookPremium / canCookDoubleBatch + their state). I held off this tick because a single new module would land both variants together and the deferred split keeps the existing test fixtures pointing at one import path. If a third variant ships, the split becomes mandatory. **Recorded as observation #5 in STATE.md.**
- **No new keybinds this batch.** Every feature hooks an existing flow. Premium and double-batch sit behind pure helpers a future inn-cook menu will surface as toggles; the achievements and lore tabs leverage panels that already exist; compost-recycle layers onto the 7-press path with no new input.
- **Dawn-toast tail chain stayed flat.** Last tick warned the chain was getting dense; this tick I deliberately picked features that didn't add new tail-lines so the chain has a chance to settle. The future `assembleDawnToast([(priority, line)])` refactor still feels right but isn't blocking yet.
- **MineHaulState carried the new field internally.** No SaveSnapshot top-level shape change for `lifetimeCounts` — the optional field lives inside MineHaulState which the snapshot already serializes. Backfill on load: `cur.lifetimeCounts = snap...lifetimeCounts ? {...} : {}` so older saves get an empty map and start counting from there. **Recorded as observation #4 in STATE.md.**
- **STAMINA_TEA_KEYS sync test caught one drift risk.** The new const has to track STAMINA_RESTORE; I added a bidirectional sync assertion so if a future tea is added to stamina.ts but not STAMINA_TEA_KEYS, the test fails fast. Cheap insurance.
- **Disk-scare did NOT recur.** Tick prelude cleared ~2GB via npm cache + ~/.cache puppeteer cleanup. The cron sat at 1.7GB free throughout the tick. If disk ever drops below 1GB at prelude, a `df -h` precheck would be the right cron-prelude addition.
