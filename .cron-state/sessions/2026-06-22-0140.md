# Sunsprout autoship — 2026-06-22 01:40 PT (tick #15)

## Batch summary
- 5/5 features shipped to `origin/main`, batched commit + single gate pass.
- 1385/1385 tests green (+52 this batch). Build 244.52 kB / 74.31 kB gz. tsc clean.

## Commits (lowest -> highest on `main`)
- `30d38a1` feat(pond-ribbon)         track heaviest single-day pending count per species
- `32d1afb` feat(bench-shopping-list) surface exact gold/gem gap on dimmed bench rows
- `c37849e` feat(spa-refill)          Pip sells a discounted spa-pass refill at the cart
- `69026c8` feat(pet-treat)           stamina teas bump dog/cat pet streak after the daily pet
- `42902a2` feat(shelter-trial)       first storm of a save auto-spawns a trial shelter

## Feature notes

### pond-ribbon (30d38a1)
- Mirrors the crop-journal ribbon flow.
- New `PondState.ribbons?: Partial<Record<FishKey, PondRibbon>>` (each entry = best single-day pending count + season/day).
- `pondTick` gained an optional 4th `time` arg. Legacy 2/3-arg callers leave the ribbon untouched; the live `game.ts` dawn call passes `this.time` so the ribbon advances on the right calendar day.
- New `recordPondRibbon` pure comparator, new `pondRibbonFor` accessor, new `pondRibbonLine` formatter, new `POND_SEASON_NAMES` lookup.
- `pondStatusLine` appends `[ribbon: N in a day - Season dD]` when the stocked species has a record.
- 12 new tests in `tests/pond-ribbon.test.ts`. All 14 pond-rim + 18 fish-pond tests still green.

### bench-shopping-list (32d1afb)
- New pure helper `recipeShoppingList(player, recipe)` returns plain-language gap copy:
  - gold only: `"need 120g more"`
  - gem only: `"need 1 more Iron Chunk"` / `"need 2 more Iron Chunks"`
  - both: `"need 350g more, need 1 more Iron Chunk"` (gold-first)
  - craftable: `""` (caller skips the draw)
- `bench-menu.ts` draws the hint at the bottom-left of every dimmed row in amber-orange (#E89C5A).
- Tests assert hint SHAPE (regex `/^need 1 more Iron \w+$/`) so a future gem rename doesn't break the pin.
- 7 new tests in `tests/bench-shopping-list.test.ts`.

### spa-refill (c37849e)
- New `SPA_PASS_REFILL_PRICE = 500` (vs the 700g full pass) + new synthetic `SPA_PASS_REFILL_KEY` in `cart.ts`.
- Added one CART_CATALOG row right after the Spa Pass row.
- `buyFromCart` special-cases the refill: credits `SPA_PASS_PUNCHES` directly into `getSpaPass(player).punchesLeft` — no inventory key, no save-schema change.
- New `canRefillSpaPass(player)` gate: no unredeemed bag pass + zero current punches + `bath.totalSoaks > 0` (proves they used a pass before).
- `CartBuyOutcome` widened with `'refilled'` and `'refill-not-eligible'` (reason `'no-pass' | 'still-has-punches'`).
- `cart-rumor` pool filter widened to exclude `SPA_PASS_REFILL_KEY` (mirrors the `decor-` exclusion) so Pip never teases a gated buy.
- `game.ts` cart-confirm gained two new toast branches.
- 12 new tests in `tests/spa-pass-refill.test.ts`.

### pet-treat (69026c8)
- New `treatDog(world, player, time)` + `treatCat(world, player, time)` — symmetric API, identical outcome union.
- New `PET_TREAT_KEYS` / `CAT_TREAT_KEYS` as-const tuples in cheapest-first order (herb-tea -> berry-tonic -> hot-cocoa -> mushroom-broth -> sunflower-elixir).
- Gates: pet owned + within PET_RADIUS + petted today + streak < cap + tea in bag.
- Picks the lowest-tier tea first so the player keeps heavy-hitters for the stamina pool. Bumps streak by +1 clamped at PET_STREAK_CAP.
- `game.ts` wires the treat call into the EXISTING `already-today` branch of J/-. No new keybind.
- Toast variants: `"Treat for the dog (herb-tea). Streak N (+Mg tomorrow)."`, at-cap, and a refreshed already-today line that nudges the player to bring tea.
- 13 new tests in `tests/pet-treat.test.ts`.

### shelter-trial (42902a2)
- New `seedTrialShelter(world, player)` in `storm-shelter.ts` — first storm of a save auto-spawns one shelter on the lowest-(ty,tx) outdoor crop.
- Gates: zero placed shelters + empty `storm.hit` map + non-empty `world.crops` + `canPlaceShelter` passes.
- `game.ts` dawn block runs it BEFORE `maybeFireStorm` and ONLY on the storm scheduled day (`stormScheduledDay(player, season)`) so a non-storm dawn never wastes the freebie.
- The storm-impact toast appends `" (Pip left a free trial Storm Shelter on your field overnight — consumed by the storm.)"` when the trial fired this dawn — the player learns the system by experiencing its payoff.
- Deterministic ordering (lowest ty, then lowest tx) so reload-scumming is harmless.
- 8 new tests in `tests/storm-shelter-trial.test.ts`. All 18 storm-shelter + 13 storm tests still green.

## Quality gate
- `npx tsc --noEmit` -> clean
- `npm run build` -> 244.52 kB / 74.31 kB gz (was 240.44 / 73.24)
- `npm test` -> 166 files / 1385 tests passing in 8.58s

## State snapshot
- Branch: `main` (commits go straight to `origin/main`)
- Cron identity: `Cake (cron) <51058514+Sanjays2402@users.noreply.github.com>`
- Push verified: `origin/main` HEAD = `42902a2`
- 75 autoship features now landed across 15 ticks.

## Open observations / forward notes
- pondTick is now a 4-arg function with the trailing two args optional (`player?`, `time?`). Eight ticks back it took 2 args. The widening pattern is the right play for cozy feature growth — no callers broke and the optional args carry the new capabilities.
- The cart-rumor filter is becoming the canonical "this row shouldn't headline" list. If a future catalog row is gated by ANYTHING (singleton ownership, prereq state, etc), it should join the `decor-` + `SPA_PASS_REFILL_KEY` filter.
- `treatDog` and `treatCat` are word-for-word the same API. If a third pet ever ships, factor the body into a shared `treatPet(petState, player, treatKeys, capValue, perStreakBonus)` helper.
- `seedTrialShelter` is the first time a feature has placed a world entity on the player's behalf. Pattern can extend to other "tutorialize" moments (free fertilizer drop on the first compost-empty day, free chick on the first hatchery-empty week, etc) — all gated on `hit-map empty` style flags.

## Up next (5 cleanest from the roadmap)
- Mining cart return — visible cart on the mine entrance counts gems carried home this run.
- Crop dyes — pumpkin -> orange, flower -> red at the bench; tints the player tunic cosmetic.
- Tournament leaderboard — running totals across seasons; tab in the lore panel.
- Mail-order seed pack — auto-restock extension: rare-seed sub drops one Pip-tier seed per season.
- Stamina training trail — daily village run loop raises max stamina once per week.
