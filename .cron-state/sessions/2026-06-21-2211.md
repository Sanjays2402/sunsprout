# Session — 2026-06-21 22:11 PT (tick #14)

## Outcome

5/5 features shipped, 1333/1333 tests green (+40 this batch), build 240.44 kB / 73.24 kB gz. Push landed on origin/main as 7da4405.

## Features

1. **soap-gift (d2d3c82)** — Perfumed Soap now a universal "loved" gift across every candidate, mirroring the BOUQUET_KEY rule. Cleanest fit: extend tasteOf + pickBestGift so the soap is auto-picked by attemptAutoGift + dispatchOwl with zero touch on per-candidate loved lists. Duplicated the string literal in hearts.ts to dodge a hearts->bath-house circular import; new test asserts the two stay in sync.

2. **breeder-tradein (e08d80e)** — Pip auto-buys breeder eggs at 2x fancy-egg price (72g each) when the cart menu opens. Cozy approach: trade fires BEFORE the menu shows on the E-press, so the player never has to learn a new keybind or a new sub-menu. Money log tags the trade. breederTradeInLine handles singular/plural copy.

3. **rumor-rebate (1c0bfe8)** — 5% gold-back when the player buys the headliner Pip teased last visit. The arithmetic uses (season + 3) % 4 which is congruent to (season - 1) mod 4, feeding the same `(season + 1) % 4` term used by the existing rumor picker — single-sourced wraparound. cart-menu rows now sport a "TEASED -Ng" chip when the row matches the headliner. Floor-rounding so tiny buys never owe partial gold.

4. **barometer-warn (0d9ad5a)** — barometerStormWarning(player, time) returns "Storm tomorrow|in 2 days — line up shelters now." when a barometer is mounted AND a storm sits in the 2-day forecast. Surfaced as a new terminal branch in the dawn-toast ternary (after pondAdded) AND appended to the village board interaction toast. Gives the barometer a second job beyond extending the forecast strip.

5. **pond-rim (7da4405)** — Stone-Rim Pond Kit at Pip's cart, 450g singleton. Raises POND_MAX_PENDING from 6 to 10. pondTick + pondStatusLine widened to take optional player (legacy callers still cap at 6). game.ts dawn pondTick + nothing-pending interactPond branch both pass the player so the upgrade matters.

## Gates

- npx tsc --noEmit: clean
- npm run build: 240.44 kB / 73.24 kB gz (was 238.62 kB / 72.64 kB last tick)
- npm test: 1333/1333 across 161 files

## Notes / blockers

- No new keybinds this tick. Every feature slotted into an existing flow.
- The PERFUMED_SOAP_GIFT_KEY duplication in hearts.ts is intentional — pulling the constant from bath-house creates a cycle (bath-house imports stamina; hearts is a pure data module everyone imports). The new test pins the two strings together.
- pondTick + pondStatusLine signatures widened with an optional player arg. Old callers continue to work; the rim cap only activates when the player is threaded through.
- cart purchase flow gained a rumor-rebate side effect: gold is added AFTER buyFromCart returns, logged as a separate "cart: rumor rebate (...)" row, and the toast must use `postBuyGold` (local var) not `out.remainingGold` (pre-rebate).
- cart-menu E-press now runs tradeBreederEggs BEFORE opening the menu — only wired in the game.ts handler today. If a future caller hits cartMenu.open() another way (e.g. dev console), they'd need to mirror the trade step.

## Next tick suggestions

Pool refilled with 8 new ideas in STATE.md including breeder-egg cookbook tie-in, pond fish ribbon, cart rumor history panel, storm shelter free trial, pet treat, and Maple weekend market. Combined with carry-over ideas (mining cart return, crop dyes, festival ribbon, stable, stamina training, tournament leaderboard, dynamic shop refinements, mail-order seed pack, bench shopping list, roaming bard) plus the new spa pass refill idea — plenty to pick the next 5 from.
