# Session 2026-06-22-0524

5/5 shipped — 16th batch tick, 80 features now on `main`.

## Features

1. **pond-overflow-warn (12e5dc9)** — `pondWouldOverflowTomorrow(state, player?)` + `pondOverflowWarning(state, player?)` pure helpers in `fish-pond.ts`; game.ts dawn block appends "Pond is brimming with X (N/cap) — collect by tomorrow or lose K." as an "* tail" on the regular headline. Honours stone-rim cap (rim owner with 9 pike sees no nag; no-rim with 5 minnow does, since 5+2 > 6).
2. **bath-seasonal-towel (34d06cf)** — `BathState` gains `seasonalSoaks` + `seasonalTowelsGifted` per-`SeasonKey` maps. Crossing `SEASONAL_TOWEL_SOAKS=5` in a season mints `towel-{season}` cosmetic in the bag. `BathOutcome 'soaked'` widened with `seasonalSoaks` / `towelEarned` / `towelKey` / `towelLabel`. `bathFlavorLine` appends "The bath house slides a Fall Towel across the counter — 5 soaks this season." on the gifted soak. Persistence wires both maps.
3. **mining-haul (aab5349)** — new `src/game/mining-haul.ts` with `MineHaulState{counts, lastRun}`. `recordMined(p, gem)` bumps the per-gem count on every successful M-press strike. `resetMineHaul(p)` snapshots → `lastRun` + clears `counts` on B-press sleep. `haulYesterdayLine` reads `lastRun` to surface "Yesterday's mine haul: 2 copper nugget, 1 cave ruby (worth 156g)." as a third optional dawn-toast tail. Persistence snapshots `mineHaul` so reload mid-run keeps both the counter and the prior recap.
4. **tournament-nudge (78ad507)** — `personalBestFor(player, kind)` scans `entries[]` for matching kinds and returns the best score+tier. `nextTierToClear(score)` pure mapping (`bronze`/`silver`/`gold`/`null`). `tournamentNudgeLine(p, t)` composes "Carrying 6 — silver needs 8. (PB: silver at 12.)" — switches to "Carrying nothing yet — bronze needs 3. (no PB yet.)" on fresh / "maxed (gold) — go for the cleanest sweep" at the ceiling. Game.ts dawn tournament-line builds `base + nudge` so off-tournament days still skip cleanly.
5. **cart-rumor-history (96803cd)** — `RUMOR_HISTORY_CAP=4` ring buffer of `RumorHistoryEntry{season, itemKey, label, buyPrice, bought}`. `recordRumorVisit(p, season)` captures on cart-menu open (idempotent on repeat opens within the same visit; appends a new entry across seasons; trims oldest at the cap). `recordRumorBuy(p, season, itemKey)` stamps `bought=true` on the matching season's headliner buy. Cart-menu footer surfaces `"Headliners: 2/4 bought."` beneath the existing next-visit hint. Persistence wires `rumorHistory`.

## Tests

+60 this batch:
- `pond-overflow-warning.test.ts` — 11 tests (empty / stocked-but-empty / below-cap / over-cap / at-cap-already / rim path / boundary cap-yield+1 / wording)
- `bath-seasonal-towel.test.ts` — 10 tests (inventory keys / labels / counter increment / gifts on milestone / no double-gift / different-season different-towel / before-milestone / flavor line both branches / no-time backwards compat)
- `mining-haul.test.ts` — 12 tests (default shape / recordMined increments / count + gold helpers / reset semantics / empty-tally idempotence / lastRun overwrite / haulYesterdayLine wording + catalog order / haulStatusLine)
- `tournament-nudge.test.ts` — 13 tests (nextTierToClear at every threshold / null at gold / personalBestFor scanning across keys / scoreTodayFor wrapper / tournamentNudgeLine all four wording branches)
- `cart-rumor-history.test.ts` — 14 tests (default empty / capture + idempotence / append + cap rotation / no-match no-op buy / match flips bought / idempotent stamp / entry-line formatter / newest-first ordering / no-history line / summary count)

Plus 4 existing bath-flavor-line fixtures (bath-house / bath-loyalty / bath-winter-discount / spa-pass) gained the 4 new BathOutcome fields to satisfy the widened type — all existing assertions remain green.

## Quality gate

```
npx tsc --noEmit                                      0 errors
npm run build      dist/assets/index-pQEyGXOM.js     248.70 kB / 75.86 kB gz
npm test           Tests  1445 passed (1445)         171 files
```

## Push

`17741df..96803cd  main -> main` — 5 feature commits + this session note as a separate chore commit.

## Notes

- **Dawn-toast tail chain is getting dense.** The headline now reads `base ($flavorTail) [* pondOverflow] [* mineHaulRecap]`, plus the existing `(rain watered N)`-style ternary chain inside `flavorTail`. Two more ticks of this pattern and an `assembleDawnToast()` helper that takes an array of `(priority, line)` pairs becomes the cleaner refactor than a longer if-chain. **Recorded as observation #2 in STATE.md.**
- **Disk-scare mid-tick.** `/tmp + ~/.npm + ~/.cache` filled to 100% during a patch verifier write, leaking a stray bash error stream into `src/game/cart-rumor.ts`. Caught immediately on the post-write verification (the verifier flagged the size mismatch). Scrubbed via a follow-up patch; cleared 2GB via `npm cache clean --force` + `rm -rf` on stale `puppeteer` / `chromium` caches under `~/.cache`. **Worth keeping an eye on next tick** — if it recurs, add a `df -h` precheck to the cron prelude.
- **No new keybinds this batch.** Every feature hooked an existing flow (M-press strike / B-press sleep / E-press cart / dawn block). Digits `9` and `0` still free; letter keys still exhausted.
- **BathOutcome widening pattern carries.** Two ticks in a row I've widened `BathOutcome 'soaked'` with new fields, and the same fix-up across the four existing `bathFlavorLine` fixtures works each time. If I widen it a third time, worth flipping these test fixtures into a `buildSoakedFixture(overrides)` helper.
- **All five features land via existing surfaces.** No new world fixtures (no new sprite to place), no new menus, no new keybinds. The dawn toast did most of the heavy lifting — three of the five features added a tail line there. Cart menu got one footer line. The bath flavor toast got one extra tail clause. That's the entire surface area touched this tick.
