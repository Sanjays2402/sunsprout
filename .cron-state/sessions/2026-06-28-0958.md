# Tick #48 — 2026-06-28 09:58 PT

**Batch:** CLEAN (not a resume). origin/main + local main both at #47 (6481b69) on entry.
**Theme:** "panels show their shape + scan like the bag" — 16th consecutive FRONTEND-FOCUS tick.
**Shipped:** 5/5. Pushed 9c5597f..479b502 to origin/main.

## Slices

1. **money-log-purse-sparkline** (9c5597f) — `purseSparkline(player)` walks `runningBalances` into a normalised polyline (x 0..1 oldest->newest, y 0..1 min-max over the window). N rows -> N+1 vertices: pre-window start then each row balance, ending at current gold, so the line spans exactly `purseTrend.start..end`. Flat window pins every vertex to 0.5 (no /0); null on <2 rows in lock-step with purseTrend. Panel draws a direction-tinted box just left of the endpoints text on the summary stripe (faint baseline + polyline + newest-point dot); in/out/net text clips ahead of the reserved cluster. +7 tests.
2. **almanac-filter-pips** (8aa8135) — `almanacFilterKinds(filter)` returns the admitted kinds in the stable SUMMARY_KIND_ORDER, derived from the SAME FILTER_KINDS map applyAlmanacFilter uses (test asserts pip set == kept set per filter). Panel draws KIND_STYLE swatches left of the centred filter chip text as one cluster. +7 tests.
3. **quest-reward-glyphs** (db61342) — `questRewardGlyphs(reward)` emits one pip per formatReward segment in order (gold coin -> crate per items entry -> cosmetic star); pip count == comma-segment count asserted. Threaded onto QuestLogEntry. Panel draws hand-placed pixel silhouettes left of the reward line, tinted to earned/active. +8 tests.
4. **crop-journal-bar-legend** (688bf0a) — `harvestBarLegend()` tier order + `hasHarvestBars(entries)` gate (== maxLifetimeHarvest>0, asserted to agree with a drawn bar). Panel draws a swatch+label key right-aligned on the field-status band (reserved even on a bare-but-harvested field), reusing TEXT_COLOR/SILVER/GOLD so the key matches the bars. +6 tests.
5. **bag-sort-direction** (479b502) — `bagSortDirection(mode)` -> desc for count/value, az for name, mirroring sortBagItems comparators (tests assert against the actually-sorted rows). Panel draws a down-chevron / A->Z glyph right of the sort-chip label. +5 tests.

## Gate (once, at end)
- `npx tsc --noEmit` → exit 0
- `npx vitest run` → **2841/2841 green** (277 files, +33 from 2808 baseline)
- `npm run build` → 356.04 kB / 108.44 kB gz, 179 modules (UNCHANGED — every slice extended an existing module; 5 new TEST files, no new source files)

## Notes
- ZERO new keybinds — every slice rode an existing panel surface.
- Stale-LSP fired on each panel-method add (drawSparkline / drawRewardGlyph / drawHarvestLegend / drawSortDirection) + the SPARK_* const block — all phantom "Cannot find name" / "method does not exist" errors that tsc exit-0 cleared. Followed obs #3: trust tsc, not the LSP flood.
- One real type-propagation fix: adding `rewardGlyphs` to QuestLogEntry broke a synthetic literal in tests/quest-log-filter.test.ts's `rowOf` helper — added the field there. And the quest-reward-glyphs test needed `startingQuests()` to seed quests (a fresh World() has none).
- Refill #9 ran to 3 open items (panel-CLOSE-fade, hearts-into-fade-family, settings-swatch-extras still open); added refill #10 with 8 fresh frontend items.
