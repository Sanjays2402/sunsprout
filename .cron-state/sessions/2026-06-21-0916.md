# 2026-06-21 09:16 PT - Tick #10 batch

5/5 shipped to `main`.

| feature | SHA | files |
|---|---|---|
| bath-house | 163549f | src/game/bath-house.ts, tests/bath-house.test.ts, src/engine/game.ts, src/game/persistence.ts |
| fish-pond | 3aa9bb7 | src/game/fish-pond.ts, tests/fish-pond.test.ts, src/engine/game.ts, src/game/persistence.ts |
| market-discount | b3e19eb | src/game/weekday-market.ts, tests/weekday-market.test.ts, src/game/shop.ts, src/ui/shop-menu.ts, src/engine/game.ts, tests/shop.test.ts |
| board-rep | 8e956dc | src/game/board-reputation.ts, tests/board-reputation.test.ts, src/engine/game.ts |
| npc-routes | f659bac | src/game/npc-route.ts, tests/npc-route.test.ts, src/game/npcs.ts |

## Gate

```
npx tsc --noEmit          -> clean
npm run build             -> 218.94 kB / 66.23 kB gz (136 modules, 95ms)
npx vitest run            -> 141 files / 1111 tests passed (+78 vs last tick)
```

## Push

```
a325b94..f659bac  main -> main
```

## Notes for next tick

- **Free keybinds are nearly exhausted.** This tick added one (`>`). A future tick should ship a panel-cycle key that demotes some singletons to subviews under one shortcut.
- **Shop UI now takes time.** ShopMenu.open(player, time?) — passing time gives the deal banner. If a future caller forgets time, the menu silently falls back to base prices (graceful degradation).
- **NPC schedule walks are deterministic.** Pure function of (hour, minute, period). No per-frame state to persist. Renderer-side updateNPCs already eases toward the anchor each frame — the walking just feeds it a moving target.
- **Bath house buff is the first stamina-cap manipulator.** Future systems that touch `stamina.max` (mining permits? training trail?) must be careful to layer on the buff rather than overwriting it. Current `setStamina()` accepts arbitrary values but doesn't touch max.
- **Pond's `>` key was an opportunistic grab** — the `<` and `?` keys remain free. The pond stocks the MOST-ABUNDANT fish in the bag, which is a reasonable default; a follow-up tick could let the player pre-select by facing.

Cron tick released the lock; next tick fires per cadence.
