# Tick #31 — 2026-06-23 18:20 PT

**TICK STATUS:** 5/5 features shipped. 2146/2146 tests green (+66 this batch). Build 272.35 kB / 83.22 kB gz. Pushed to origin/main.

## Features (5)

1. **`oneShotBrag` generic helper** (4fdd583)
   - Extracted the sticky-flag dawn-brag pattern from observation #4 of tick #30 — three+ achievements in tree shared near-identical `*Pending` / `*Fired` / clear-on-read bookkeeping.
   - New `oneShotBrag(carrier, pendingKey, firedKey, render)` in dawn-toast.ts. Pure module; carrier-key typing prevents typos.
   - Refactored deepVeinDawnBrag + chainTierDawnBrag onto the helper as zero-behavior-change validations.
   - Relaxed one existing assertion `toBe(false)` → `toBeFalsy()` to match new contract (helper clears to undefined, not false).
   - 16 new tests for the helper itself.

2. **Compost-master-sash dawn brag** (9cefb89)
   - One-shot dawn-toast tail the morning AFTER `lifetimeRecycledGold` crosses 250g for the first time.
   - New `sashBragPending` + `sashBragFired` sticky flags on `CompostLedgerState`.
   - `recordApplied` snapshots pre-bump gold and arms ONLY on fresh crossing.
   - Runs through `oneShotBrag` helper — validates the refactor on the same tick that introduced it.
   - Landed as 6th tail on assembleDawnToast composition.
   - Wording: `"Compost Sash earned - 252g recycled across 84 bags."`
   - 11 new tests. Also includes rare-master scaffolding (helper + flags + recordApplied arming) because the snapshot path naturally covers both crossings in one function.

3. **Rare-master dawn brag** (4ff8eac)
   - Symmetric one-shot tail when `lifetimeRareBagsApplied` crosses 100.
   - Reads SEPARATE rare-bag counter so a regular-bag grind never accidentally fires it.
   - Landed as 7th tail on assembleDawnToast.
   - Chained test verifies sash + rare-master independence — piling 100 rare bags clears BOTH milestones simultaneously and firing one brag doesn't clear the other.
   - 12 new tests.

4. **Rare-master journal-line nudge** (5ff8aa1)
   - 4th rung in compostLedgerLine ladderNudge + 3rd rung in compostHalfwayDawnNudge for [50, 100) rare bags.
   - Reads SEPARATE `lifetimeRareBagsApplied` counter so the rung is orthogonal to gold-recycled and total-bags-applied.
   - Priority order: master / sash / rare-master / pulper. Rare-master sits BEFORE pulper because rare bags are the harder grind to advertise.
   - New `RARE_MASTER_NUDGE_MIN_BAGS=50` + `rareMasterNudgeDawnFired` sticky flag.
   - Validates ladderNudge refactor a third time (sash rung tick #29, rare-master rung now).
   - ONE EXISTING TEST UPDATED: `compost-master-halfway-nudge.test.ts` fixtures that used 84 rare bags now also light up the rare-master nudge. Switched to 250 regular bags so the pulper rung surfaces alone.
   - 16 new tests cover constant + boundaries + singular/plural + priority + orthogonality + dawn-nudge sticky flag.

5. **Owl-menu chain-target row halo** (f05132b)
   - Subtle 2px sage-green outline `rgba(143, 204, 106, 0.6)` on the active-chain-target row, drawn in ADDITION to the row-position hoist.
   - Halo color matches BONUS_GREEN chip so the two visual cues read as the same "active streak" language.
   - When the chain-target row is ALSO selected, the halo OVERRIDES the BORDER outline for one strong sage indicator.
   - Length-1 chains light up the halo (about maintaining friendship, not about bonus level).
   - New pure helper `isActiveChainTarget(player, npcId)` in owl-post.ts.
   - 11 new tests including menu draw integration via vi.fn() stub CanvasRenderingContext2D that captures strokeStyle at each strokeRect call.

## Observations

### Refactor cycle complete: dawn-toast composer → ladderNudge → oneShotBrag

Three get-ahead refactors in three ticks:
- **Tick #27:** `assembleDawnToast(headline, tails[])` — retired the engine's growing chain of `if (tail) headline = ${headline} · ${tail}` ternaries
- **Tick #28:** `ladderNudge(rungs[])` — retired the ternary tower in `compostLedgerLine`
- **Tick #31 (this):** `oneShotBrag(carrier, pendingKey, firedKey, render)` — retired the sticky-flag dawn-brag boilerplate

All three follow the same shape: when a pattern hits its 3rd implementation, extract it as a pure helper. The refactor lands BEFORE the 4th and 5th implementations would have piled on. Each extraction also includes a same-tick validation by adding the next implementation on the new helper (no "build a tool and never use it" risk).

### Dawn-toast composer at 7 tails — tail grouping is the next refactor

Sequence: pondOverflow · haulRecap · compostNudge · deepVeinBrag · chainTierBrag · sashBrag · rareMasterBrag. At 7 the morning toast is still readable but the next tail (owl-chain-recipient brag in the roadmap) pushes us into "wall of tails" territory. Concrete proposal on the roadmap: collect achievement-tier tails into a single composed group with a visual separator (e.g. `•` vs the existing `·`) so the morning toast reads as `headline · system tails · achievement chip [brag1 • brag2 • brag3]`.

### Compost surface now spans 5 tiers

- (a) The bin itself (placeable + deposit + finish-tick)
- (b) Journal-line passive recap (compost master + bags)
- (c) Journal-line passive nudge tail (4 rungs: master / sash / rare-master / pulper halfway runways)
- (d) Dawn-toast one-shot nudge (3 rungs: master / rare-master / pulper halfway dawn one-shots)
- (e) Dawn-toast one-shot brag (2 brags: sash + rare-master earned celebrations)

Both PASSIVE nudges (always visible in window) and CELEBRATORY brags (one-shot on crossing) for the achievement tier set. Next observation: the journal-line and dawn-nudge surfaces both walk the same rung shape but each picks ONE rung to fire. A "show me ALL eligible rungs" view (compost ledger panel from the roadmap) would close the "everything I'm grinding" overview gap.

### Zero new keybinds — 11 ticks in a row

Every feature this batch extended an existing surface. Digits 9 and 0 still free. The compost-ledger-panel roadmap item would naturally bind under '\`' or another digit since the journal-line tail is approaching information density.

## Notable test patterns

- **vi.fn() stub CanvasRenderingContext2D**: First time we've used vi.fn() to mock canvas APIs and capture strokeStyle / strokeRect interactions. Same pattern would work for testing any future row-halo / chip / visual indicator without needing a real canvas (jsdom doesn't provide one).
- **Chained sash + rare-master independence test**: Piling 100 rare bags clears BOTH milestones simultaneously. The test verifies firing one brag doesn't clear the other — proves the sticky-flag pairs are truly independent per-achievement.

## Lock release

`rmdir /tmp/sunsprout-cron-tick.lock` runs as the final action.
