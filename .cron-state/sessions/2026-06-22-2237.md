# Session 2026-06-22 22:37 PT — Tick #21

## What landed

**5/5 features shipped** — all squash-clean, one commit per slice, pushed to `main`.

1. `0ed5e51` — **compost-halfway-nudges** — `compostLedgerLine` gains two mutually-exclusive "to badge" tails. `(Xg to the badge)` while `50 <= lifetimeRecycledGold < 100`; `(N bags to the pulper badge)` while `100 <= lifetimeBagsApplied < 500`. The pulper nudge starts the moment compost-master nudge ends (`PULPER_NUDGE_MIN_BAGS = COMPOST_MASTER_MILESTONE_GOLD`) so the journal line always surfaces at most one nudge at a time. Singular phrasing for "1 bag" on the pulper tail. 15 tests including the constant relationships and a "never both nudges at once" mutual-exclusion test.
2. `938012f` — **cart-tea-tradein-inn-nudge** — `staminaTeaTradeInLine` appends a " - or trade N forage at the inn for Sage Tea" tail in the under-threshold + empty buckets so a player at the cart who only sees "need more teas" also learns the inn route. Suppressed when the player can already trade at the cart OR has zero eligible forage. Quantifies the Sage Tea yield: "3 forage -> Sage Tea" / "6 forage -> 2 Sage Tea". 11 tests covering the four buckets + suppression contracts + berry+herb forage threshold mix.
3. `2f6d1ba` — **owl-fluency-tiers** — per-NPC `OWL_FLUENCY_TIERS = [{min:5,occasional pen pal}, {min:15,regular pen pal}, {min:25,favorite courier}]`. Top tier matches `OWL_FLUENT_MILESTONE` so a player who lands the global fluency badge also lights up at least one per-NPC favorite tag. `owlStampLine` composes the tier label onto the count: "Owl posts: 18 (regular pen pal)." `lore.ts` now goes through `owlStampLine()` instead of inlining the literal so the tier label surfaces in the Folk row description for free. 1-4 plain-count noise floor preserved. 16 tests.
4. `3657aed` — **mining-haul-ribbon** — `MineHaulState.bestRun{count,countDay,gold,goldDay}` captured at sleep via `resetMineHaul(player, day)`. Count + gold promoted INDEPENDENTLY so a pure-iron grind that breaks the gold record without touching count still promotes only the gold half. `haulYesterdayLine` appends three record-fell suffix variants ("count + gold!" / "for count!" / "for gold!") the morning a record fell — silent on every non-record dawn. `haulBestRunLine` passive formatter. `loreTabFooter` Gems composition gains " - best run: N gems / Xg (day D)" tail. Persistence wires `bestRun` with lazy-undefined backfill for older saves. Engine sleep path passes `this.time.day` so the ribbon captures the day. 19 tests.
5. `f89de0b` — **owl-post-fee-discount** — `OWL_POST_TIER_DISCOUNT_PCT = {occasional pen pal: 10, regular pen pal: 20, favorite courier: 30}` keyed off the fluency tier label so the favorite-courier path pays 28g/send instead of 40g. `dispatchOwl` reads `owlPostFeeFor(player, npcId)` ONCE for both the gold check AND the deduction so the same value is reported in the not-enough-gold `need` field that gets charged on success. `ui/owl-menu.confirm()` snapshots the fee BEFORE `dispatchOwl` so the toast surfaces the actual paid amount (pre-stamp tier) rather than the just-bumped post-stamp tier. The HUD `Send a gift across the valley for 40g.` title line stays on the BASE fee deliberately — the discount is per-send, not a banner. 13 tests covering the discount table sanity, per-tier math, isolation, the deduction path, the not-enough-gold reporting, and the post-stamp tier-crossing order-of-ops.

## Quality gates

- `npx tsc --noEmit`: clean
- `npm run build`: 261.48 kB / 80.02 kB gz (last tick: 259.65 kB / 79.37 kB gz)
- `npm test`: **1773 / 1773 passing** (+74 this batch over 1699 last tick)
- Push: `d1e42f2..f89de0b  main -> main` confirmed via `git fetch && git log origin/main`

## Patterns hardened

- **ZERO new keybinds — sixth tick in a row.** Eight of the last thirteen features added no new input. Digits 9 and 0 still free.
- **Lazy-lifetime-ledger pattern continues to factory new tails.** Both compostLedgerLine + owlStampLine were extended this tick via pure-tail composition. The 50g/100-bag/5/15/25-stamp thresholds all live as constants on top of the existing ledger reads — NO new persisted field for the nudge or tier work.
- **MineHaulState.bestRun is the FIRST schema growth on the mine haul state since lifetimeCounts.** resetMineHaul gained an optional `day=0` arg so unit fixtures that don't care still pass; persistence + engine sleep are the only two callsites that pass the day. Older saves backfill bestRun=undefined cleanly.
- **The fluency-tier work spans BOTH cosmetic (Folk row label) AND economic (owl fee) layers.** First time a per-NPC ledger gained a real gameplay-incentive hook — the fluent-with-the-owl badge was previously purely cosmetic; now the path to it saves the player 8-12g per send. Tests cover the order-of-ops contract: the stamp lands AFTER the deduction so the very-next send carries the new rate, not the current one.
- **Dawn-toast tail chain GREW this tick** (broke a five-tick plateau): the mining-haul-ribbon record-fell suffix layers AFTER haulRecap in the existing chain, so the order is now `base ($flavorTail) [* pondOverflow] [* haulRecap [- best run ever for X!]]`. Still no central assembleDawnToast([...]) refactor — each tail still string-concats on its own.

## Disk scare

No recurrence. Disk pressure stayed normal throughout the tick.

## Refilled roadmap

5 fresh slice ideas appended now that the four ideas from last tick all landed this tick — mining-haul-ribbon-achievement (21st badge, lazy-ledger pattern on bestRun.count/gold), mining-haul-ribbon-dawn-nag-prep, owl-post-tier-badge-color, compost-master-halfway-nudge-dawn-surfacing, pulper-halfway-nudge-dawn-surfacing, owl-post-per-NPC-ribbon. The dawn-surfacing nudges are the natural next "ledger -> dawn toast" extensions; the per-NPC ribbon parallels the mining-haul ribbon at the owl-post layer.

## Telegram-ready summary

Shipped 5 features cleanly: compost-halfway-nudges (journal "(22g to the badge)" / "(266 bags to pulper)" tails), cart-tea-tradein-inn-nudge (cart chip points at the inn forage route when player short on teas), owl-fluency-tiers (per-NPC "favorite courier" / "regular pen pal" / "occasional pen pal" tag at 25/15/5 owl posts to one recipient), mining-haul-ribbon (all-time best single-run record + "best run ever for gold!" dawn brag + Gems-tab ribbon footer), and owl-post-fee-discount (favorite-courier paths save 12g/send via 10/20/30% tier discounts on the courier fee). 1773/1773 tests green (+74), build 261.48 kB / 80.02 kB gz. Zero new keybinds — sixth tick in a row. The fluency system gained its first real gameplay-incentive hook beyond cosmetic: the path to a favorite courier now saves real gold per send.
